# AGENTS.md — Skillz Wiki

## Purpose

This repo is a public wiki and installable skill library for pentesting, red-team, and bug-hunting workflows. Prefer durable, agent-ready guidance for recon tooling and replayable exploit validation over generic documentation or one-off notes.

## Agent workflow

- Read this file at the start of each task.
- Update this file whenever you learn something important about the repo, workflow, build, taxonomy, or collaborator preferences.
- After every meaningful repo update, create a git commit and push it to `origin` unless the user explicitly tells you not to.
- Use clear, non-interactive git commands and keep commit messages specific to the change.

## Recursive self-improvement

Follow the [Recurse.bot guide](https://recurse.bot/) approach: treat `AGENTS.md` as the project memory for future agents.

- Record wins to repeat and mistakes to avoid.
- Capture exact build, test, and publish commands that were actually verified.
- Note project-specific taxonomy decisions and stable public paths.
- Record collaborator preferences that materially improve future handoffs.
- Keep entries concise, concrete, and easy to scan.

## Writing conventions

- Keep links clickable and explicit in Markdown.
- Write every skill so it can be copied into an agent bundle with minimal trimming.
- Prefer concise steps over long narrative paragraphs.
- Include prerequisites, inputs, command patterns, output handling, and safety notes on skill pages.
- Separate confirmed behavior from inference or speculation.
- De-emphasize triage, mitigation, and defensive advisory framing unless the user explicitly asks for it.

## Collaborator preferences

- Keep the public site centered on offensive security: penetration testing, red-team, and bug-bounty operator workflows.
- Prefer recon tools, attack-path discovery, proof-of-concept validation, and concrete exploit chains over defensive-response or mitigation content.
- Do not publish blue-team guidance, defensive SecOps runbooks, incident-response playbooks, alert triage, or mitigation-first advisory pages unless Dean explicitly asks for that framing.
- Favor proven, replayable pathways with explicit prerequisites, commands, and validation boundaries.

## Content structure

- **Skills**: installable, tool-specific guides for agents
- **Recon**: cross-tool workflows for discovery, prioritization, and target shaping
- **Exploit Paths**: specific, replayable attack chains for authorized validation
- **Templates**: reusable reporting skeletons and output formats
- **Notes**: taxonomy, source tracking, and editorial guidance
- **Blog**: launch posts and major updates
- Legacy alert or defensive-reference pages may remain in the repo, but they are not the main navigation model.

## MkDocs / GitHub Pages lessons learned

- Use `theme.custom_dir` for template overrides; do not add a non-MkDocs `overrides:` key to `mkdocs.yml`.
- Keep the Pages workflow strict-friendly; config warnings should be treated as build failures.
- Keep the landing page updated with a manual "Recent entries" section capped at 10 links.
- Maintain `docs/feed.xml` manually when major launch posts or release-style updates are added.
- The chosen custom domain is `skillz.wiki`; keep `mkdocs.yml`, `docs/feed.xml`, and `docs/CNAME` aligned with it.
- Archived reference pages that stay outside the main nav will show up as informational "not included in nav" lines during `mkdocs build --strict`; that is expected while the public taxonomy stays narrower than the repo contents.
- As of March 26, 2026, GitHub warns that `actions/checkout@v4`, `actions/configure-pages@v4`, `actions/setup-python@v5`, and `actions/upload-artifact@v4` are still on Node.js 20; revisit the workflow before GitHub's Node 24 switchover dates become urgent.

## Verified commands

- `python -m pip install -r requirements.txt`
- `mkdocs build --strict`
- `npm run test:sources` checks the WebLogic batch source paragraph renders precise clickable links and that each external source returns HTTP 200 with the expected advisory/CVE token.
- `python3 scripts/check_duplicate_alert_ids.py` currently reports pre-existing duplicate CVE/GHSA references but exits 0; use it as an advisory hygiene check, not a strict publish gate until the historical duplicates are resolved.
- On hosts where `python`/global `mkdocs` are unavailable or PEP 668 blocks global installs, use a disposable venv: `python3 -m venv /tmp/skillz-wiki-venv && /tmp/skillz-wiki-venv/bin/python -m pip install -r requirements.txt && /tmp/skillz-wiki-venv/bin/mkdocs build --strict`.

## Maintenance rules

- When adding a new skill or notable playbook, update:
  - `mkdocs.yml` nav when the page belongs in the primary surface
  - `docs/index.md` recent entries when the addition is notable
  - `docs/blog/index.md` and `docs/feed.xml` for major launches
- Keep page paths stable once they are linked publicly.
- Prefer one focused skill page per tool over sprawling kitchen-sink references.
- Agentic C2 / implant research frameworks belong under `docs/skills/` when the page is framed as an authorized lab workflow with explicit controller/listener trust boundaries and safety constraints.
- Maintain `docs/notes/editorial-checklist.md` as the publishing gate.
- Maintain `docs/notes/source-index.md` as the canonical source seed list.
- The current public taxonomy is `Skills`, `Recon`, `Exploit Paths`, `Templates`, `Notes`, and `Blog`.
- For hourly news-driven updates, only promote items that can become offensive operator guidance: recon technique, exploit path, validation workflow, tool usage, bypass pattern, or bug-hunting heuristic. Do not turn generic advisories into blue-team alerts.
- NLTK `pathsec` network-validator follow-ups belong on the July 31 library/tenant/commerce page when special-use destination classes or validation-to-connect drift survive strict mode. MCP URL/path follow-ups belong on the August 6 agent authority page; prove only with owned no-content peers, disposable roots, and denied network/file sinks.
- MCP memory/history/manifest and Git-wrapper follow-ups belong on the August 6 agent authority page when domain, history, library, project-source, or revision fields reach generated filenames or shell-backed Git operations. Trace retained IDs across every create/read/save/delete lifecycle sink and compare REST versus MCP entry points with disposable roots plus denied file/process recorders; never open sibling files or execute Git/shell payloads.
- AWS Nitro Enclave/KMS research is promotable when attestation, CMK identity, encryption context, IAM role, response fields, replay state, or enclave-initiated TLS cross an untrusted parent boundary. Prove with disposable keys, synthetic ciphertexts, mocked KMS responses, and patched request sinks; never decrypt production data, mutate live key policy, intercept shared traffic, or infer SDK RCE from an article that withholds exploit details.
- Trail of Bits skill-distribution scanner-bypass research was promoted as a reusable agent skill supply-chain testing workflow rather than an alert; keep future skill-marketplace content framed as authorized scanner/ingestion coverage testing with inert canaries.
- GitHub Advisory REST API batches can publish adjacent advisories slightly out of order; before closing an hourly run, re-check the newest page for unprocessed IDs that belong with an already-created batch page and update that page instead of creating a duplicate.
- VulDB-style GHSA waves often contain sparse product-specific SQLi/XSS/auth-bypass/router/memory-safety/DoS entries; promote only those with reusable operator patterns (for example agent tool-path command injection, installer SSRF, log-viewer command injection, or IPC boundary checks) and mark the rest processed without publication.
- Trail of Bits RSS works at `https://blog.trailofbits.com/feed/` and `https://blog.trailofbits.com/index.xml`; `https://blog.trailofbits.com/feed.xml` returned 404 during the 2026-06-08 hourly scan.
- ProjectDiscovery RSS works at `https://projectdiscovery.io/blog/rss` and `https://projectdiscovery.io/rss.xml`; `https://projectdiscovery.io/blog/rss.xml` and `https://projectdiscovery.io/feed.xml` returned 404 during the 2026-06-09 hourly scan.
- ProjectDiscovery Neo benchmark follow-ups are promotable as methodology updates when they add replayable fixture design, scanner miss/false-positive classification, or sink-validation evidence patterns; Faraday-style SSRF case studies should be framed around final-destination proof with owned callbacks, not metadata/internal probing.
- Brutecat Security RSS works at `https://brutecat.com/rss.xml`; promote durable Google/API bug-hunting techniques as methodology pages, with PII/recovery-flow content bounded to authorized owned-account testing.
- When a late GitHub updated-feed wave contains IDs adjacent to an already-published same-day batch (for example ONNX or pyLoad follow-up advisories), update the existing batch page and mark those IDs processed instead of creating a duplicate alert.
- Check Point IKEv1 VPN auth-bypass KEV was promoted as an operator validation page because it offers durable VPN perimeter recon and authentication-boundary testing guidance; keep future perimeter VPN KEVs focused on IKE/protocol reachability, lab/customer-approved validation, and post-auth access boundaries.
- Puma PROXY protocol v1 advisories are worth promoting when source-IP spoofing is tied to HTTP keep-alive/trusted edge decisions; pair availability-only parser issues with that trust-boundary workflow instead of publishing standalone DoS guidance.
- Sanitized rich-text placeholder injection in transactional email is promotable when it gives bug hunters a reusable trust-confusion workflow; frame it as user-controlled profile/order data rendered as trusted system messaging, not as XSS unless script execution is independently proven.
- Arista EOS tunnel decapsulation KEVs are promotable when they reveal protocol-confusion at a VXLAN/GRE/decap-group boundary; frame validation around configured decap IP reachability, unexpected tunnel protocol acceptance, and controlled canary forwarding rather than broad network probing.
- Net::IMAP raw-argument advisories are promotable when they expose a reusable protocol-injection boundary: untrusted criteria/fetch/search fragments crossing into IMAP literal framing and server capability negotiation. Pair adjacent parser-state DoS with command-injection guidance rather than publishing availability-only pages.
- Cisco SD-WAN crafted-file root KEVs are promotable as post-auth appliance validation workflows when they cover `netadmin` file/CLI handling and control-plane-to-edge blast radius; avoid framing as unauthenticated RCE or suggesting production config changes.
- Svelte SSR attribute-spread and DOM-clobbering advisories are promotable when they expose a reusable object-key-to-DOM-attribute trust boundary; frame validation around user-controlled spread keys, pre-hydration event timing, and form/input clobbering preconditions rather than generic XSS or resource-stress testing.
- MCP Java SDK wildcard-CORS advisories are promotable when event-stream or streamable-HTTP transports leak session bootstrap data to arbitrary browser origins; pair sparse CORS findings with browser-relay validation boundaries rather than generic header-hardening guidance.
- Ray Dashboard path traversal advisories are promotable as AI/ML developer-infrastructure file-boundary checks when they involve unauthenticated or weakly segmented dashboard static routes; keep validation to synthetic canaries and avoid reading secrets, notebooks, model weights, or cloud credentials.
- samlify/OpenMetadata/MVT batches are promotable when they expose reusable identity, workflow-secret, and analyst-tool input boundaries; keep SAML proofs to signed canary attributes, OpenMetadata evidence to redacted field presence, and MVT traversal proofs to disposable synthetic backups.
- vLLM/LMDeploy model-loading advisories are promotable when revision pins, `code_revision`, side artifacts, or `trust_remote_code` defaults create reusable AI inference supply-chain boundaries; keep proofs to inert model/config canaries and avoid loading untrusted code on production workers.
- Anyquery `clear_plugin_cache` traversal is promotable as a SQL-exposed filesystem boundary when it demonstrates cache-root escape via `os.RemoveAll`; prove only with disposable canary directories outside the plugin cache.
- OpenTelemetry Operator TargetAllocator `ServiceMonitor`/`PodMonitor` file-backed auth is promotable when tenant monitor writes cause collector pod-local files to be forwarded as scrape bearer auth; prove with synthetic mounted canary files and never capture live service-account tokens in wiki/report evidence.
- Fission advisory clusters are promotable when they reveal serverless tenant boundaries across router internal routes, storage service archive CRUD, function service-account token exposure, or builder command control; prove only with canary functions, archives, and namespace-scoped test environments.
- Litestar host/CSRF advisories are promotable when they expose reusable client-controlled framework trust boundaries: missing `Host` plus `X-Forwarded-Host` fallback, or cookie-sourced CSRF tokens rendered as trusted template markup.
- Crawlee sitemap SSRF and FrankenPHP Unicode path-splitting advisories are promotable as crawler ingestion and upload-to-script-execution boundary checks; keep proofs to canary URLs/files and avoid internal-service or production command-execution payloads.
- Claude Code Action project MCP advisories are promotable when PR-controlled `.mcp.json` or similar agent config can execute in a privileged CI context; prove with inert canary MCP commands only, never secret exfiltration. Baileys/message-SDK event spoofing is promotable when protocol input crosses into trusted bot/business events; keep proofs to owned accounts and canary sinks.
- PDM/Python package-manager file-write advisories are promotable when malicious wheels, lockfile/package sources, or repository-local symlinks cross into filesystem writes outside the project/install root; keep proofs to disposable canary files and avoid overwriting shell startup files, credentials, or production config.
- Keycloak forced-browsing advisories are promotable when disabled feature flags leave alternate versioned account/API route families reachable; frame validation around positive/negative route controls with disposable authenticated users, not generic auth bypass.
- Keycloak backchannel logout and UMA CORS follow-ups are promotable on the existing Keycloak boundary page when session-derived placeholders such as `application.session.host` let refresh-token parameters steer server-side backchannel HTTP requests, or unvalidated JWT claims influence CORS origin decisions before signature validation. Keep proofs to synthetic clients, owned callbacks, invalid canary JWTs, and header/callback decision tables; never target metadata/internal services or collect real tokens.
- Undertow request-smuggling advisories are promotable when they expose reusable front-end proxy vs origin parser differentials; frame validation around single-connection canaries, raw-byte evidence, and harmless route/cache/auth-boundary effects rather than production desync or cross-user targeting.
- Netty DNS/Redis codec advisories are promotable when user-controlled protocol tokens cross into encoders/decoders without RFC grammar, length, delimiter, or CRLF validation; frame proof around lab-only canary domains, mock resolvers, disposable Redis instances, and string-to-wire or command-boundary mismatch evidence.
- Guzzle PSR-7/Guzzle Services advisories are promotable when they expose reusable PHP integration boundaries: user-controlled URL/Host material crossing into PSR-7 raw serialization or server-request parsing, or untrusted scalar fields crossing into modeled XML request bodies. Keep proofs to canary headers, parsed-host mismatch tables, and mocked XML bodies; do not claim standard Guzzle client exploitation unless raw serialization/forwarding is actually reached.
- Meta Ads MCP/OpenZeppelin codegen/hapi Wreck/Traefik/PDM plugin advisories are promotable when they expose privileged automation trust boundaries: unauthenticated MCP HTTP tool execution with loaded operator tokens, user metadata crossing into executable generated tests, credential headers leaking across redirect authority/scheme/port, `StripPrefix` route auth evaluated before path normalization, or repository-controlled `.pdm-plugins` executing before CLI parsing. Keep proofs to inert tokens, local callbacks, generated canary statements, synthetic routes, and disposable plugin marker files.
- Spring Cloud Config and Sharp advisories are promotable when user-controlled paths, keys, disks, or storage object selectors break the binding between an approved config/entity namespace and the file/secret/object actually returned. Keep proofs to synthetic config files, disposable Secret Manager canaries, and non-sensitive Laravel Storage objects; never read production secrets or unrelated customer files.
- Kolibri/Hapi inert/Keycloak/Flowise/Arc advisories are promotable when they expose reusable boundaries: reflected SSRF through remote-facility proxies, static-file string-prefix confinement escapes, IdP verification proof reuse across upstream identities, vector-store CRUD without role checks, or public Go `pprof` runtime state. Keep proofs to canary callbacks/files/accounts/vector stores and redacted debug-route access; avoid internal service probing, real account linking, production document access, heap token capture, or CPU profiling on shared systems.
- CodeIgniter4 `ext_in` upload-validation advisories are promotable when MIME-derived extension checks can be paired with original filename preservation and public/executable upload paths; keep proofs to benign GIF-like canaries with dangerous-looking extensions and do not publish shell payloads or production execution attempts.
- CodeIgniter4 follow-ups belong on the existing upload-boundary page when `is_image`/`mime_in`, `UploadedFile::move()`, `deleteBatch()` WHERE binds, or forwarded HTTPS headers cross into stored extensions, final paths, SQL structure, or secure-transport decisions. Use marker uploads, denied move/query sinks, scratch rows, and trusted-vs-untrusted peer decision tables; never write outside disposable roots, delete retained data, or infer RCE/auth bypass without the next sink.
- Ivanti Sentry KEVs are promotable when exploitation hinges on unmanaged, externally reachable appliance endpoints; frame validation around product ownership, managed-vs-unmanaged state, mTLS/Neurons restricted-access boundaries, and lab/customer-approved canaries rather than publishing RCE payloads or creating production admin accounts.
- GeoServer DB2 datastore JNDI, WsgiDAV prefix share-root escapes, OpenFGA cache-key collisions, DevGuard public-asset writes, Filament relation scope mismatches, and Aurora PostgreSQL wrapper search-path privilege issues are promotable when they expose admin-to-runtime, filesystem, authorization-cache, cross-tenant, UI-to-server validation, or DB privilege boundaries; keep proofs to inert callbacks, marker files, synthetic tuples/assets/relations, and benign database canaries.
- Trail of Bits short-sleeve RSA research was promoted as weak public-key recon, not as exploit code: focus on scoped public-key corpus collection, badkeys/offline weak-key checks, product-lineage clustering, and proofs that stop before private-key use.
- Trail of Bits Patch the Planet field reports are promotable when they yield durable operator workflow, such as agent-guided fuzzing campaigns with sanitizer-backed replay, reachability validation, build/corpus evidence, and explicit rejection of impossible harness states.
- Patch the Planet goal-driven follow-ups belong on the existing agent-guided fuzzing page; define one testable vulnerability outcome per agent, separate coverage from finding work, red-team lazy completion paths, gate historical seeds, and require independent impact/PoC review plus a human duplicate check.
- Budibase/SwiftNIO/LangGraph/Chisel advisory clusters are promotable when they expose reusable outbound-fetch, HTTP bridge parser-differential, checkpoint tenant-scope, or tunnel ACL enforcement boundaries; keep proofs to canary callbacks, raw-byte lab backends, synthetic checkpoints, and disposable listeners.
- PeopleSoft PeopleTools Updates Environment Management KEVs are promotable when primary sources identify unauthenticated HTTP access to critical management functions; keep validation to route-level auth gating, version evidence, and inert canaries, never update/deployment/environment-changing actions.
- TYPO3/Budibase/GeoServer/Appsmith advisory waves are promotable when they expose backend file-mount escapes, local storage-to-deserialization chains, low-code datasource secret relay, unauthenticated webhook schema mutation, app-scoped builder-to-global-admin escalation, host-field SSRF, CSRF route-matcher bypass, GeoServer XML/entity or absolute-path file boundaries, or reset-link origin trust. Keep proofs to marker files, canary credentials/callbacks, synthetic schemas/users, lab-only paths, and owned-account emails.
- esbuild/mise/Tomcat/Radius/TYPO3 waves are promotable when they expose reusable build-tool, repository trust, virtual-host auth, controller confused-deputy, or backend CMS authorization boundaries: registry environment to native binary execution, Windows dev-server backslash traversal, project config self-trust, TLS SNI vs HTTP Host mTLS mismatch, Deployment annotation to cross-tenant delete, or TYPO3 form/file/record controls crossing into SQL/admin-capable actions. Keep proofs to inert binaries, marker files, sandbox hooks, owned vhosts, synthetic Kubernetes resources, and disposable TYPO3 objects.
- File Browser/Fleet/Fabric.js/Cordova waves are promotable when they expose reusable command allowlist, future-grant, prefix-cleanup, public-share rule-rebase, archive separator traversal, scoped-root symlink, cursor-oracle, serialization, or mobile bridge boundaries: first-token allowlist checks followed by raw shell execution, pre-created shares to later files, path-prefix share deletion across users, public directory shares bypassing descendant deny rules, zip/tar entries preserving Windows traversal separators, in-scope symlinks reaching out-of-scope canaries, `ORDER BY` plus cursor pagination leaking joined-table canaries, Fabric canvas data crossing into trusted SVG DOM insertion, or InAppBrowser web content dispatching predictable native callback IDs. Keep proofs to inert command markers, disposable files/shares, deny-rule markers, archive listings, symlink canaries, seeded Fleet canaries, harmless SVG markers, and test Cordova callbacks; skip pure public-login DoS unless paired with an operator workflow.
- Admidio and Aurora PostgreSQL wrapper advisories are promotable when they expose reusable secret-export, credential-log, or database privilege boundaries: CSRF missing on SSO PKCS#12 private-key export, debug logs containing session/auto-login bearer values, or low-privilege Aurora wrapper/search-path flows executing canary functions with another RDS user's permissions. The Admidio export-CSRF proof already lives in the 2026-06-05 TinyMCE/skillctl/Admidio batch; avoid duplicating it and add later Admidio items as adjacent log/credential-boundary updates. Keep proofs to synthetic SSO keys, disposable sessions/log windows, and benign database canary schemas; never export production keys, collect real user cookies, or mutate production database objects.
- Jupyter Server `allow_origin_pat` advisories are promotable when regex origin validation uses prefix semantics (`re.match`) instead of full-origin matching, because that yields a reusable browser-origin trust-boundary workflow. Keep proofs to owned suffix-origin canaries and harmless status/version endpoints; never collect notebooks, terminals, tokens, model files, datasets, or cloud credentials.
- Angular SSR hydration `TransferState` advisories are promotable when predictable state container IDs can be DOM-clobbered before client bootstrap and poison HTTP transfer-cache responses. Frame validation around user-controlled pre-bootstrap markup/IDs, synthetic JSON canaries, and cache-hit evidence; do not forge real user/session/payment data or call it XSS unless an executable sink is independently proven.
- Gitea/LangChain4j/HAPI FHIR/Avo/agent/WebSocket advisory waves are promotable when they expose reusable API/UI authorization drift, token flag route-family bypasses, repository previewer metadata-to-DOM sinks, vector metadata-to-SQL construction, alternate XML helper XXE, admin association attach-policy drift, template-loader traversal, approved-host agent fetch channels, document-id normalization auth bypasses, tenant WebSocket room joins, or dev-server HMR proxy interception. Keep proofs to disposable orgs/repos/secrets, synthetic vectors/XML callbacks/templates/URLs/notes/org ids/cookies, and never exfiltrate live CI secrets, patient data, environment variables, user notes, or tenant events.
- Open WebUI Playwright/RAG URL-loader redirect advisories are promotable as browser-loader SSRF boundary updates when validation checks only the initial URL before `page.goto()` follows redirects; update the existing Open WebUI RAG/SSRF page rather than creating duplicate pages, and keep proofs to owned redirectors plus synthetic internal canary services only.
- Docker MCP Gateway/OpenClaw/http-proxy-middleware/Karate/Piscina/Gotenberg/Strimzi/ZITADEL waves are promotable when metadata, workspace env, proxy body/router transforms, mock request data, polluted prototypes, document resources, Kubernetes custom-resource fields, or IdP URLs cross into host execution, backend routing, worker imports, outbound fetches, cross-namespace RBAC, or tenant/network boundaries. Keep proofs to disposable images/workspaces/backends/mock vars/worker canaries/doc callbacks/namespaces/test users; never mount real host paths, exfiltrate secrets, query internal production services, or read live Kubernetes Secrets.
- PraisonAI-style agent platform waves are promotable when unauthenticated UI/Jobs/AgentOS/AgentTeam/recipe APIs, MCP/SSE transports, approval workflows, webhooks/bots, file abstractions, or SSRF guards cross into tool invocation, dangerous-tool approval, out-of-root file access, forged trusted events, or redirected/rebound outbound fetches. Update the existing same-day agent boundary batch when adjacent to other agent/proxy items; keep proofs to inert tools, disposable files, owned callbacks/channels, and lab users only.
- OpenTelemetry Collector, BBOT, Kirby, Podman, Nodemailer, python-statemachine, MCPVault, undici, and Heimdall waves are promotable when telemetry labels or webhooks cross into privileged API calls, recon scanner inputs write/fetch/extract outside intended bounds, CMS proxy/API/bootstrap logic crosses role or client-IP boundaries, image metadata mutates host paths, raw email content bypasses file/URL guards, SCXML import evaluates expressions, vault path filters miss OS canonicalization, proxy agents drop TLS pinning, or forwarding headers steer identity-aware proxy decisions. Keep proofs to synthetic spans/webhooks, temp scanner outputs, lab CMS roles, disposable host dirs, owned callbacks, inert SCXML expressions, canary vault paths, lab CAs, and quoted/malformed forwarding-header canaries.
- Splunk Enterprise PostgreSQL sidecar KEVs are promotable when unauthenticated network-reachable sidecar endpoints can create or truncate files; frame as a sidecar/control-plane authentication-boundary workflow. Keep validation to route/auth evidence and, only in approved labs, disposable canary files outside Splunk config/index/app/log paths; never publish arbitrary-file-write payloads or perform production truncation.
- Crawl4AI/AgenticMail-style advisory waves are promotable when exposed crawler/AI-browser APIs, request-supplied browser launch config, crawler download filenames, inbound-mail bridges, or cross-agent task queues cross into SSRF, runtime execution, file writes, privileged agent resume, or task mutation. Update the existing same-day agent/proxy boundary batch when adjacent; keep proofs to owned callbacks, inert browser flags, disposable download markers, owned mailboxes, and lab tasks only.
- OpenClaw policy-state waves are promotable when active memory scope, exported-session rendering, chatops reactions, bootstrap pairing, shell allowlists, command wrappers, or hostname canonicalization cross into global config mutation, unsafe link preservation, unauthorized action triggers, widened pairing scopes, execution-policy bypass, or host allowlist confusion. Update existing same-day agent/proxy batches and keep proofs to inert marker keys, lab chat/devices, decision tables, and owned host canaries.
- Late same-day waves covering Signal K SSRF, PHP JWT/JWE algorithm or padding boundaries, OTP provisioning URI mass assignment, Gemini MCP CLI command construction, OpenClaw chat/env/exec/recovery controls, and Daytona sandbox volume traversal are promotable when they expose reusable URL-connector, token-parser, enrollment, MCP-wrapper, agent policy-state, or sandbox-host filesystem boundaries. Keep proofs to owned callbacks, disposable keys/tokens, synthetic OTP URIs, fake `@file` targets, temp PATH/env canaries, lab chat identities, marker-only config/memory evidence, and disposable host canary directories.
- NNEF/model-parser, Kirby/Grav/Jupyter render, undici shared-cache, and DOMPurify state advisories are promotable when uploaded models, CMS/editor content, notebook exports, request canonicalization, or mutable sanitizer config cross into parser memory safety, trusted rendering, cross-user cache disclosure, or process-global policy changes. Keep proofs to inert malformed models, lab CMS/notebook canaries, two-user local cache markers, and same-process sanitizer harnesses.
- Tilt HUD, Network-AI, and Canto SDK advisories are promotable when developer dashboards, WebSocket streams, debug handlers, agent allowlists, empty default secrets, or SDK path variables cross into unauthenticated resource control, state disclosure, process-memory surfaces, shell execution, MCP tool invocation, or bearer-tokened same-host route confusion. Keep proofs to lab dev projects, inert command markers, fake MCP/API tokens, mocked upstream APIs, and route/stream canaries; never dump real pprof heap data, trigger production Tilt resources, or collect live API keys.
- JCE, LiteSpeed cPanel plugin, and Cisco SD-WAN Manager KEVs are promotable when editor-profile creation, shared-host symlink handling, or appliance upload APIs cross role and filesystem boundaries; keep proofs to anonymous-route evidence, disposable tenant canaries, and lab-approved SD-WAN file markers, never web shells, root-escalation chains, tenant files, or production appliance config paths.
- CakePHP Authentication backslash open-redirect advisories are promotable as updates to the existing return URL scheme-bypass methodology; frame them as browser/redirect-client parser differentials where validation happens before backslash and encoded-separator normalization.
- Gogs/OpenCTI/motionEye/Paymenter waves are promotable when reverse-proxy identity headers, ingestion URLs, GraphQL introspection filters, media preview filenames, local camera config permissions, or ticket attachment storage cross into auth bypass, SSRF, schema discovery, file read, credential-hash exposure, or executable public uploads. Keep proofs to disposable users, owned callbacks, synthetic GraphQL tenants, lab marker files/configs, and inert upload canaries; skip nearby file-listing DoS unless it unlocks a non-availability operator workflow.
- AVideo API signup permission self-grant and mise `.tool-versions` Tera execution advisories are promotable as updates to existing boundary pages when CAPTCHA-only registration accepts capability fields or repository-local tool-version files execute templates without trust prompts. Keep proofs to disposable accounts, lab CAPTCHA flows, inert capability checks, sandbox repos, temp-file markers, and no live credentials/media/streams.
- Kimai follow-up waves should update the existing 2026-07-14 Kimai/FacturaScripts/App Store page when route-family permission drift, pre-2FA API session acceptance, parent/child object authorization, default `APP_SECRET`, CSRFable business shortcuts, or timesheet revocation boundaries are adjacent; keep proofs to marker-only Kimai labs and redact cookies, signed links, TOTP material, billing data, and export-template bodies.
- FacturaScripts and OpenCost follow-up waves should update the existing 2026-07-14 Kimai/FacturaScripts/OpenCost/App Store page when raw static-file URL prefixes, REST filter-key SQL construction, TOTP-only login steps, or unauthenticated `/serviceKey` writes cross into ERP document/API or cloud-cost credential boundaries. Keep proofs to synthetic files, disposable API rows, fake service keys, lab `CONFIG_PATH` markers, and route decision tables; never read invoices, backups, password hashes, logkeys, API keys, or real cloud credentials.

- Agentic-Flow/ouroboros-ai/agent-coderag/Entire CLI/Bedrock AgentCore/JupyterLab/DotVVM/NL Portal/Guzzle waves are promotable when agent tool parameters, repo-local env/config/wrappers, checkpoint metadata, package-install helpers, package metadata, framework auth filters, public resolvers, or proxy URLs cross into shell execution, agent policy changes, filesystem writes, package-manager control planes, trusted UI links, inert authorization, privileged-token SSRF, or plaintext proxy legs. Update same-day developer-control batches where adjacent; keep proofs to inert commands, disposable repos/files, owned package indexes, lab roles, redacted headers, and mocked proxies.
- Buildah/runc/OpenAM/XWiki/ComfyUI waves are promotable when remote build contexts, container special directories, IdP browser/callback/query parameters, wiki macro rendering, or AI manager config paths cross into outside-context file inclusion, host filesystem setup, OIDC-origin DOM, SSRF, LDAP filters, privileged macro execution, or alternate-channel config mutation. Keep proofs to disposable build contexts, temp host dirs, lab realms/users, owned callbacks, harmless wiki markers, and fake ComfyUI config keys; update existing product-specific pages for adjacent AVideo payment/dotfile items rather than duplicating them.
- Entire CLI checkpoint restore path traversal lives in the existing 2026-06-19 developer agent/dashboard/SDK/framework control-boundary batch; future checkpoint metadata restore/write advisories should be grouped with agent repository-trust guidance and proven only with disposable repos plus temp canary files.
- OpenTofu/go-getter, Nokogiri JRuby `NONET`, and MCP Toolbox updated waves are promotable when IaC source URLs, XML schema loaders, OAuth introspection responses, or MCP protocol-version handlers cross into local-file reads, outbound callbacks despite network-deny expectations, third-party/malformed token acceptance, or scope bypass. Update same-day developer-control batches where adjacent; keep proofs to disposable canary files, owned XML callbacks, fake introspection endpoints, and canary database tools only.
- AlchemyCMS nested-page, Hugo symlink/renderer, and OpenZeppelin Contracts Wizard waves are promotable when alternate CMS tree APIs, static-site build files, Markdown info strings, or generator metadata cross into restricted draft content disclosure, build-user file reads, renderer attribute injection, or generated source-code injection. Update same-day developer-control batches where adjacent; keep proofs to lab pages, disposable symlink canaries, harmless DOM markers, and inert generated declarations only.
- JupyterLab Git, Stanza, containerd CRI, Parse Server, Symfony UX, and py7zr waves are promotable when shared repo metadata, case-insensitive path aliases, model safe-load fallbacks, checkpoint/image metadata, batch/relation/auth/upload routes, signed browser state, autocomplete wildcards, or archive symlink chains cross into notebook UI execution, excluded path reads, unsafe pickle, node/device/log/image state, API control drift, CSRF/state replay/query oracles, or outside-root writes. Keep proofs to harmless DOM markers, synthetic excluded dirs, inert model checkpoints, lab clusters, disposable users/relations/uploads, seeded entities, and temp extraction roots; skip adjacent parser memory-safety/DoS items unless a specific authorized exploit workflow exists.
- http4k/Traefik/Allure/VCR.py/dbt MCP/qbee/TinaCMS/Craft/Grafana Operator/Crossplane/CycloneDX/Blocky waves are promotable when framework defaults, Ingress secret resolution, report artifacts, cassette files, MCP OAuth context routes, archives, CMS editor/import fields, operator CRs, package signatures, workspace args, or DNSSEC caches cross into auth replay, cookie/host/signature confusion, auth fail-open, local file read, unsafe YAML/codegen execution, token metadata disclosure, outside-root writes, cluster privilege, mutable verified installs, shell execution, or validation-cache confusion. Keep proofs to disposable creds/tokens/repos/reports/clusters/registries/zones, owned callbacks, inert markers, synthetic outside files, and mock resolvers.
- Outerbase/Langflow/Mailpit/Miniflux/wiki-extension waves are promotable when dashboard widgets, AI workflow file nodes, response/history IDs, link-preview URL parsers, redirect policies, embed classes, or provider/error text cross into token-bearing render origins, server file reads, tenant data access, SSRF canonicalization bypass, privileged navigation, or stored HTML. Keep proofs to two-user labs, synthetic files/responses, owned callbacks/domains, inert DOM markers, and avoid live tokens, internal services, production datasets, or secret files.
- SurrealDB/Anki/agent-memory/MCP/search/backup waves are promotable when desktop local HTTP servers, iframes, database graph/index/analyzer/JWKS features, secret loaders, SDK signers, tracing middleware, CI collaboration metadata, SOAP clients, corpus trainers, cross-realm bulk actions, MCP UI resources, agent memory IDs, localhost transports, tenant-blind memory maintenance, notebook previews, LiveQuery ACLs, URL readers, or environment backups cross into privileged APIs, authorization oracles, file reads/writes, SSRF, command execution, DOM sinks, stale realtime access, or unauthenticated approvals. Keep proofs to disposable profiles, two-tenant/realm labs, canary DB rows, owned callbacks, mocked clouds, inert workflow markers, temp files/directories, and harmless DOM markers; skip adjacent parser-only DoS/memory-safety items unless they unlock a replayable operator workflow.
- Agent/secret-store/identity/render waves are promotable when agent runtime paths, backup manifests, vault path filters, framework secret-directory loaders, CMS previews, SVG/icon loaders, recipe manifests, caveated authorization, secret-store namespaces/LDAP/lease routes, package/render workers, MCP HTTP servers, or CoreWCF SAML/WS-Security/local-transport validators cross into outside-root files, destructive control planes, nested restricted dirs, out-of-tree config reads, same-origin DOM sinks, auth bypass, command execution, unauthenticated tool access, token replay, proof-key bypass, algorithm downgrade, signature substitution, or local IPC peer-identity confusion. Keep proofs to disposable workspaces, fake packages, lab namespaces, synthetic claims, harmless DOM markers, symlinked canary secret dirs, lab sockets/pipes/users, and inert commands; skip pure crash/memory-safety items without a reusable validation workflow.
- Network-AI adjacent advisories are promotable on the existing agent boundary page when restore IDs, approval inboxes, backup manifests, sandbox paths, or environment files cross into outside-root copies/deletions or unauthenticated high-risk action approval. Keep proofs to disposable env dirs, marker-only external backups, inert pending actions, and route-state evidence; never approve live shell/write/spend operations.
- Glances/OpenDJ/Spinnaker-style control-plane advisories are promotable when virtualization inventory labels, XML-RPC browser trust/Host/CORS controls, JMX/RMI management listeners, deployment YAML descriptors, or finance-app sync secret routes cross into command construction, monitoring-data exposure, unauthenticated Java deserialization, privileged CI/CD class loading, or non-admin secret-name oracles. Keep proofs to inert VM labels, owned browser-origin decision tables, synthetic secret names, lab-only serialization canaries, synthetic pipeline descriptors, and no production command execution, telemetry dumping, real bank credential enumeration, or directory/cloud data access.
- Budibase/Gogs/skillctl/Nuxt/automation waves are promotable when public low-code routes, chat identity handoffs, builder OAuth URLs, webhook redirects, skill metadata, unsigned webhook triggers, URL navigation helpers, SCIM patch keys, or enrollment-token stores cross into stored datasource credentials, account identity binding, SSRF, outside-root Git/filesystem effects, workflow execution, parser-normalized redirects/script schemes, process-global prototype mutation, or mesh identity claims. Keep proofs to disposable buckets, owned chat identities/callbacks/redirectors, scratch skill repos, inert workflow markers, route decision tables, synthetic SCIM properties, and fake enrollment rows.
- Budibase/Gogs/Actual late waves are promotable on the existing Budibase/Gogs automation boundary page when DNS-rebinding outbound fetches, PWA ZIP symlinks, public webhook body mass assignment, mirror-setting local repository imports, GET-based org team changes, attachment UUID downloads, notebook preview re-rendering, CSV exports, or disabled OpenID sessions cross into internal fetches, outside-root reads, cross-workspace automation, local repo import, owner role changes, private files, same-origin DOM sinks, spreadsheet formulas, or stale access. Keep proofs to owned DNS/callbacks, synthetic marker files/repos/attachments/notebooks/CSV rows, disposable orgs/workspaces/users, and harmless session endpoints; skip Gogs issue-index DoS unless paired with non-availability impact.
- Jupyter Enterprise Gateway and Payload/Cloudinary signing advisories are promotable when user-controlled kernel environment IDs or CMS upload parameter sets cross into Kubernetes pod security contexts or server-signed cloud media operations. Keep proofs to disposable kernels/namespaces, numeric normalization decision tables, throwaway Cloudinary folders/assets, and owned callback URLs; never read notebooks, service-account tokens, customer media, or production Cloudinary secrets.
- Jupyter Server sibling-prefix traversal follow-ups should update the existing May 5 Jupyter/vm2/token page when `root_dir` containment relies on string-prefix checks; prove only with synthetic sibling-prefix directories and Contents API canaries, never real notebooks, datasets, credentials, terminals, model artifacts, or cloud config.
- Langflow monitor API advisories are promotable as updates to the existing Langflow/AI-workflow boundary page when `flow_id`, message IDs, or session IDs cross into another user's transactions, build artifacts, or history mutation. Keep proofs to two-user lab flows with synthetic prompt/build/session markers; never collect real prompts, model responses, production chat history, or tenant data.
- BoxLite and Alist storage/sandbox advisories are promotable when sandbox mount flags, OCI layer symlink extraction, multi-user file operation names, or storage-driver TLS defaults cross into writable host mounts, outside-root writes, sibling-user storage operations, or attacker-controlled provider endpoints. Keep proofs to disposable directories, owned registries/TLS endpoints, synthetic marker files, and two-user lab storage mounts; never target real home dirs, credentials, customer files, provider tokens, or production storage.
- Gogs follow-up waves with org/team APIs, namespace path traversal, LFS dedupe, repo upload symlinks, Git smart-HTTP service confusion, settings API role drift, milestone DOM sinks, rebase-merge argument injection, migration redirects, collaborator owner modes, or `redirect_to` parser differentials should update the existing Budibase/Gogs automation boundary page rather than spawning duplicate Gogs pages. Keep proofs to disposable orgs/repos, synthetic LFS blobs, inert branch-name commands, owned redirectors, and lab users; skip pure SSH/file-listing DoS unless paired with data access, auth bypass, or code execution.
- AVideo Meet metadata, motionEye follow-up chains, mise local credential/task includes, Glances AMP config, and OctoPrint upload-handler advisories are promotable when anonymous connection headers, media/action/config routes, repo-local tool metadata, monitoring plugin config, or reserved upload handoff fields cross into privileged DOM, hash-as-secret admin auth, shell execution, command runners, or host-file moves. Update existing product pages where present; keep proofs to lab meetings, disposable camera configs/actions, temp mise repos/data dirs, temp Glances configs, and synthetic OctoPrint canary files.
- UniFi OS and Lantronix EDS5000 KEVs are promotable when network-reachable appliance management routes, path selectors, auth-failure logging, or diagnostic/input fields cross into unauthorized state changes, file access, or command wrappers; keep proofs to owned/lab appliances, inert command markers, synthetic canary files, route/role matrices, and no production controller configs, account changes, secrets, or root payloads.
- AWS Bedrock AgentCore `install_packages()`-style advisories are promotable as agent code-interpreter package-install boundaries when package-name arguments cross into pip flags, alternate indexes, requirements-file selectors, sandbox file reads, or environment exposure; update package-control batches and prove only with disposable sandboxes, owned package indexes, fake env markers, and inert packages.
- Prefect `GitHubRepository` reference injection is promotable as a workflow-orchestrator repository-control boundary when branch/tag/reference fields cross into `git clone` option parsing. Update the existing 2026-06-19 framework/report/IaC/MCP/CMS/package-control page; prove only with disposable Prefect workspaces, owned repositories, fake credentials, dry-run argument logs, and inert callback markers.
- Snipe-IT/Mattermost/Camel K/Algernon/Jackson waves are promotable when self-permission APIs, selectlist endpoints, integration action URLs, team IDs, CRD namespace selectors, Host-derived served directories, polymorphic type IDs, or deserialized socket-address fields cross into higher-privilege control planes, filesystem roots, class instantiation, or DNS callbacks. Keep proofs to disposable users/teams/namespaces, inert API routes, marker files/classes, and owned DNS canaries; skip generic weak-hash, timing-enumeration, XSS, and resource-only Jackson edge cases unless paired with a stronger operator workflow.
- Concrete CMS waves are promotable when marketplace package routes, REST JSON block APIs, file-usage dialogs, or composer template selectors cross into CSRF state changes, serialized block state, hidden page/file reference disclosure, or path traversal. Keep proofs to lab package IDs, marker blocks, synthetic files/pages, and canary templates; never force production package installs, enumerate real private pages, read secrets, or publish executable PHP payloads.
- beets/media-library web UI advisories are promotable when imported media metadata crosses into raw client-side templates or HTML insertion sinks; keep proofs to disposable media files and harmless metadata canaries, never operator libraries, browser storage, local files, or persistent payloads.
- Hono JSX/n8n/Flowise/Picklescan waves are promotable when object keys, low-code workflow metadata, org selectors, default token secrets, import bundles, or scanner allowlists cross into SSR attributes, browser forms, SQL identifiers, OAuth secrets, tenant metadata, path/SQL sinks, or unsafe pickle loads. Keep proofs to harmless HTML markers, lab forms, scratch databases, redacted synthetic OAuth configs, disposable tenant tokens/imports, and offline scanner canaries.
- OliveTin/OpenAM/Concrete CMS waves are promotable when action-runner argument prefixes, shared templates, validation RPCs, IdP storage attributes, legacy SOAP federation endpoints, public calendar widgets, OAuth integration labels, or numeric layout fields cross into command construction, action enumeration, identity-store writes/deserialization, private event disclosure, or trusted login/visitor HTML. Keep proofs to inert action markers, lab binding IDs, disposable identity records, synthetic calendars/events, and harmless DOM markers.
- Concrete CMS follow-up waves should update the existing Concrete CMS boundary page when file IDs, Express entry IDs, conversation attachments/messages, download-password flows, inverted or missing CSRF checks, package/core/log update routes, bulk group assignment, calendar/survey blocks, summary templates, OAuth account-state checks, profile mass-assignment fields, trusted render names/legacy pagination, or RSS feed URLs cross into restricted files/forms/events/surveys/messages, package execution, group membership changes, hidden-page metadata, inactive-user API tokens, auth-control drift, DOM sinks, or SSRF. Keep proofs to lab users, synthetic files/forms/calendars/surveys/messages, harmless package/log markers, owned callbacks, and route/token decision tables.
- OpenAM push-registration follow-ups should update the existing OliveTin/OpenAM/Concrete CMS page when Push Notification SNS callbacks, CTS predicate blobs, token-read paths, shared-store namespaces, or OAuth/OIDC rows cross into class loading, Jackson construction, forged lab tokens, or token-family confusion. Keep proofs to lab realms, disposable users/clients/scopes, inert class-loading markers, and synthetic CTS rows; never mint production tokens, dump CTS, or load gadget chains.
- Claude Code follow-ups should update the existing Claude Code Action/Baileys page when agent CLI helpers, copy/export commands, predictable temp paths, world-readable response files, or symlink handling cross into local cross-user disclosure or file writes. Keep proofs to two-user lab hosts, synthetic response markers, disposable symlink targets, and fixed-version negative controls; never read real prompts, credentials, reports, or overwrite real dotfiles/configs.
- Neotoma/i18next/justhtml-style waves are promotable when authenticated graph APIs omit tenant filters after session checks, missing-translation routes persist untrusted nested keys into object path setters, or sanitized HTML code/pre text is converted into Markdown that reopens raw HTML parsing. Keep proofs to two-user synthetic graph records, inert prototype markers in disposable processes, and harmless DOM markers; never brute-force entity IDs, poison production prototypes, or deploy persistent script payloads.
- LangGraph/Braket/Lemur/chi/Cisco UCM/Windchill waves are promotable when checkpoint stores, SDK identifiers, job-result buckets, empty permission `Need`s, forwarded IP headers, appliance HTTP routes, or PLM deserialization cross into worker execution, path confusion, pickle loading, certificate control-plane access, trusted client-IP decisions, SSRF/file-write, or RCE boundaries. Keep proofs to disposable checkpoints, mock SDK paths, marker-only job results, read-only lab users, harmless IP-gated routes, owned callbacks, and inert deserialization canaries; never read prompts/secrets, create real CAs, bypass customer controls, write production appliance files, or run public RCE payloads.
- Go `x/crypto/ssh`/Lemur/ImageMagick follow-ups are promotable on the existing agent/proxy/appliance boundary page when SSH-agent constraints, certificate restrictions, ACME/CRL/OCSP URLs, JWT algorithm metadata, role membership APIs, or image filename parsing cross into remote key use, authz drift, SSRF, token confusion, role rewrite, or policy-bypassed file reads. Keep proofs to throwaway SSH keys, lab MFA flows, owned callbacks, fake JWT material, disposable roles, and symlinked marker files; skip nearby panic/OOM/parser-only items unless a non-availability operator workflow exists.
- Keycloak WebAuthn, token-introspection, and Account API follow-ups are promotable on the existing Kolibri/Hapi/Keycloak page when browser-side credential policy, realm/client `notBefore` combinations, or `--features-disabled=account,account-api` route families cross into accepted credentials, active-token introspection, or reachable versioned account endpoints. Keep proofs to lab realms, disposable users/tokens/passkeys, timestamp decision tables, and route-family comparisons; do not claim unauthenticated access when advisories require a permissioned caller.
- MindsDB-style AI/data platform upload advisories are promotable when multipart filenames, connector uploads, handler installers, or package-manager workflows cross from authenticated file upload into outside-root writes or later code loading. Keep proofs to disposable outside-root canary files and lab-only inert package markers; never overwrite production virtualenv packages, shell startup files, credentials, notebooks, model weights, or service configuration.
- Fluentd/observability pipeline advisories are promotable when untrusted log tags, placeholders, monitor APIs, or bucket/object ingestion cross into file paths, outbound HTTP destinations, plugin instance variables, or collector decompression controls. Keep proofs to scratch files, owned callbacks, fake plugin secrets, disposable buckets, and bounded lab payloads; never publish production RCE chains, internal metadata probes, real secrets, or resource-exhaustion tests.
- Incus/container-platform waves are promotable when image roots, templates, exec-output paths, backup metadata, S3 multipart object names, compression arguments, restricted-project settings, or client image trust cross into host file reads/writes or command execution. Keep proofs to disposable lab hosts, throwaway projects, temp canary directories, inert command markers, and patched negative controls; never read host secrets, overwrite config, touch tenant volumes, or execute production commands.
- PortSwigger Research RSS works at `https://portswigger.net/research/rss`; initial bootstrap promoted HTTP Anomaly Rank, WebSocket Turbo Intruder, and request-smuggling false-positive discipline as one reusable HTTP/WebSocket triage methodology rather than a dated alert.
- Hysteria UDP ACL, pydantic-ai SSRF canonicalization, cdxgen Maven scanning command construction, Apptainer path-prefix matching, and Remark42 image-proxy MIME advisories are promotable when they expose session-vs-packet authorization, IPv6 transition-address routing, repository path-to-command, container path allowlist, or upstream-header-to-same-origin-render boundaries. Keep proofs to lab relay sessions, owned callbacks, disposable Maven repos, temp container paths, and harmless DOM markers; skip adjacent Hysteria crash/DoS without non-availability operator value.
- MCP server, Relyra SAML, CakePHP/static helper, PHP HTTP/2, Pterodactyl Wings, Backpropagate UI, and nono-py waves are promotable when exposed transports, OAuth scopes, SAML signatures, template/static paths, HTTP/2 framing, panel file APIs, dashboard auth flags, or sandbox policies cross into unauthenticated tool calls, write-capable memory/spend tools, assertion forgery, filesystem traversal, request smuggling, host-file operations, unauthenticated dashboards, or policy bypass. Keep proofs to inert MCP tools, fake tokens, lab SAML users, synthetic files, single-connection canaries, disposable panel servers, route matrices, and policy decision tables.
- Statamic/php-weasyprint/Hackney/Aimeos/pnpm/Cargo waves are promotable when CMS helper endpoints, renderer URL/file/output/binary options, HTTP-client string-to-wire handling, admin proxy validation, lockfile hashes, cache keys, or registry credentials cross into restricted resource disclosure, unsafe method dispatch, SSRF/local-file/PHAR/config execution, header injection, credential-forwarding, DNS-rebinding SSRF, or supply-chain cache/credential confusion. Keep proofs to disposable CMS resources, owned callbacks, synthetic local files, inert renderer binaries, raw request captures, fake tokens, scratch registries, and marker packages; skip pure DoS/parser-exhaustion items unless paired with a non-availability boundary.
- MCP Atlassian DNS-rebinding SSRF follow-ups belong with the July 7 agent/deploy/SSRF/identity boundary page when a URL guard resolves a hostname at validation time but the outbound fetcher later connects to a re-resolved raw hostname; prove only with owned rebinding domains, structural lookup harnesses, and synthetic canary private endpoints, never cloud metadata or production internal services.
- Late Statamic/pnpm/Nezha/SNS/regclient waves were added to the existing 2026-06-26 CMS/renderer/HTTP-client/package-manager page; future adjacent items should update that page when they involve CMS preview/export/media SSRF, pnpm repository config/lockfile/patch/alias/integrity/credential boundaries, monitoring dashboard Host/route/profile identity, SNS signing-certificate trust, or registry redirect credential forwarding.
- Final-hour 2026-06-26 pnpm/Nezha/Subsonic/gonic waves also belong on that page when they involve pnpm `configDependencies`/install-engine/self-selection/path-write boundaries, monitoring dashboard unredacted secret list APIs, or media playlist ownership/path traversal checks. Keep proofs to fake tokens, isolated `PNPM_HOME`, scratch registries/repos, disposable dashboard users, and synthetic playlist marker files.
- pyLoad follow-up advisories belong on the existing 2026-06-08 pyLoad/OpenHands/PyJWT/Netty/ONNX page when sessions, GET-based APIs, CNL/local-only routes, package creation, download destinations, archive paths, proxy/TLS settings, or Host/localhost checks cross into download-manager control-plane actions. Keep proofs to disposable pyLoad labs, canary users/packages, owned URLs, paired Host-header decisions, and reversible settings; skip generic click-required CMS XSS unless it adds a stronger trusted-admin-render workflow.
- Froxlor/DNS hosting-panel advisories are promotable when user-editable record values cross into generated BIND-style zone files through newline-preserving validation, raw output concatenation, or unbounded advanced-record data. Keep proofs to disposable zones, harmless canary records, rendered-line/authoritative-answer evidence, and patched negative controls; never steer production MX/NS/A traffic, poison customer zones, or reload production DNS daemons.
- Joomla/Page Builder CK upload, phpUploader metadata exposure, FrontAccounting report SQLi, and `acl` symlink traversal advisories are promotable when uploads, inline hydration state, reporting filters, or privileged pathname helpers cross into executable storage, full metadata disclosure, query construction, or final-target confusion. Keep proofs to benign marker files, synthetic upload rows, seeded accounting canaries, and scratch ACL trees; skip generic same-wave WordPress XSS/broken-access-control items unless they add a more specific reusable boundary.
- Netty HTTP whitespace request-smuggling follow-ups belong on the existing Netty protocol codec boundary page when raw header syntax such as whitespace before a colon can split proxy and origin parsing; keep proofs to lab/canary routes and avoid publishing weaponized desync payloads or cross-user request targeting.
- SimpleHelp OIDC KEVs are promotable when a remote-support appliance accepts ID tokens without cryptographic signature verification; frame validation around OIDC-enabled state, signed-vs-tampered canary identity-token decision tables, and stopping at technician-session establishment in a lab. Never initiate remote support actions, enumerate customer devices, publish full tokens, or collect session cookies.
- Tomcat HTTP/0.9 security-constraint advisories are promotable when invalid request-line method handling crosses servlet method constraints, such as `HEAD` allowed while `GET` is denied for the same URI. Keep proofs to lab canary routes, raw request/response decision tables, and patched negative controls; do not test against admin/export/user-data routes or publish sensitive resource contents.
- Tomcat RewriteValve session-fixation advisories are promotable as updates to the existing Tomcat parser/security-constraint page when rewrite redirects or cross-context rewrites cross URL-based session tracking boundaries. Keep proofs to lab-only cookie-less canary sessions, sanitized `Location`/session-id decision tables, and patched negative controls; never publish live fixation links, collect user cookies, or test production login/admin/payment flows.
- Dgraph/Mattermost/Apache CXF late GHSA waves are promotable when GraphQL helper inputs cross into DQL string construction, OAuth callback parameters widen provider token scopes, or web-service integration settings/XML/LDAP fields cross into runtime loading, certificate disclosure, or XXE. Keep proofs to synthetic graph records, throwaway repositories/tokens, inert JMS markers, seeded LDAP certificate canaries, and owned XML callbacks.
- Concrete CMS and TYPO3 follow-ups should update existing product pages when page names, file-version approvals, Express association order, or frontend registration fields cross into trusted account-page rendering, tokenless workflow state changes, cross-entity mutations, or privileged frontend group assignment. Keep proofs to harmless DOM markers, disposable file revisions/entities/users/groups, and restricted-content canaries.
- Hermes-agent advisory waves are promotable when context scanning, skills guards, or code-execution helpers cross into prompt/tool policy, sandbox, or environment-variable boundaries; keep proofs to disposable profiles, inert canary skills, fake environment markers, and no real prompts, memories, credentials, or production skills. RabbitMQ Shovel/Federation predictable URI obfuscation is promotable as a broker log-secret boundary when proven only with synthetic AMQP URIs and lab exception logs.
- Spring AI Anthropic Skills, MLflow artifact-serving, Airflow FAB/Google provider, and Paymenter checkout advisories are promotable when generated filenames, multipart artifact endpoints, LDAP filters, SSH host-key defaults, or URL-writable checkout config cross into filesystem writes, cross-user model artifacts, auth bypass, worker SSH trust, or privileged provisioning properties. Keep proofs to temp markers, synthetic model blobs, lab LDAP entries, disposable VM fingerprints, and non-billable services; skip adjacent DoS/generic XSS/weak-hash updates unless they add a non-availability operator workflow.
- Anyquery/yutu/TidGi/TsDProxy/Auth0 Symfony/SonicWall SMA1000 waves are promotable when SQL-compatible server-mode modules, MCP file parameters, desktop wiki repository imports, forwarding headers, bearer-token transport locations, or appliance routes cross into SSRF, outside-root writes, startup module execution, backend IP-trust confusion, replay-prone query tokens, or perimeter-appliance SSRF/command wrappers. Keep proofs to owned callbacks, temp marker files, disposable desktop workspaces, lab proxies/backends, fake tokens, and owned/lab appliances; never query cloud metadata, real internal services, production OAuth tokens, customer appliance config, or shell-command payloads.
- Fedify-style SSRF follow-ups and K3s snapshot-restore advisories are promotable when URL guards miss special-use IP ranges before federation/document/media fetches, or compressed backup restore paths write ZIP members outside intended roots. Keep proofs to URL-validator decision tables, owned callbacks, disposable clusters, synthetic snapshots, and temp canary files; skip adjacent DoS/memory-leak/generic stored-XSS items unless they add a stronger operator boundary.
- Fission follow-ups involving `KubernetesWatchTrigger`, `Package.spec.environment.namespace`, `MessageQueueTrigger` secrets, or connector `PodSpec` merges should update the existing 2026-06-10 Litestar/Fission page rather than creating another Fission page. Keep proofs to two-namespace labs, synthetic events, marker-only build logs, fake Secrets, and inert PodSpec overrides; never collect real service-account tokens, workload event payloads, ConfigMaps, or production Secrets.
- Cedar/Express, Koa router, Shiro, KubeVirt, Sigstore Java, and Syncope advisories are promotable when parser differentials, route-prefix middleware gaps, saved-request/session state, VM console sockets, signature time bindings, or identity extension points cross into auth bypass, server fetch, session fixation, host socket access, artifact-verification bypass, sandbox escape, or data-scope expansion. Keep proofs to route decision tables, canary callbacks/sessions, lab sockets, public conformance bundles, and synthetic identity records.
- Kimai/FacturaScripts/App Store Server waves are promotable when billing/time-tracking object IDs, workflow cloning, frontend-hidden selectors, default Symfony `APP_SECRET` values, TOTP sub-step handlers, or OCSP freshness checks cross into cross-scope business-object mutation, session forging, TOTP-only login, or signed-data revocation bypass. Keep proofs to disposable customers/projects/timesheets/users, fake secrets, lab TOTP seeds, and synthetic signed-data fixtures; skip adjacent parser-only CPU DoS such as `json_repair` circular `$ref` unless paired with non-availability operator value.
- Late June 30 Keycloak UMA, Fission, Twig, Paymenter, Fulcio, CefSharp, AdonisJS bodyparser, Oban Web, and Probo advisories are promotable when resource IDs, CRD references/podspecs, template coercion, credit ledgers, OIDC discovery redirects, custom-scheme paths, nested form keys, dashboard events, or return URLs cross into authorization, namespace, sandbox, settlement, SSRF/JWKS, filesystem, prototype, job-worker, or browser authority boundaries. Keep proofs to synthetic UMA resources, disposable Fission namespaces, inert Twig objects, fake credits/invoices, owned issuer/callback hosts, sibling canary files, local prototype markers, lab jobs, and owned redirect domains.
- Open Babel, PyFory, OpenMed, and Spring Security X.509 advisories are promotable when untrusted chemistry inputs, reduce/global deserialization paths, user-selectable model names, or certificate subject strings cross into native parsers, policy-bypassed object restoration, `trust_remote_code` loaders, or authenticated username selection. Keep proofs to lab ASAN traces, inert deserialization canaries, owned model repos, lab CAs, and disposable users; skip adjacent crash-only parser issues unless they add a file-ingestion or identity-boundary workflow.
- Bouncy Castle GOST CTR waves are promotable when CTR counters cross into keystream reuse. Keep proofs to offline disposable keys/plaintexts and known-plaintext XOR tables; skip adjacent QPACK/cache memory-exhaustion issues unless paired with a non-availability boundary, and update existing FlashAttention/Jackson/TinyMCE pages for adjacent checkpoint, JSON binding, or rich-editor sanitizer changes.
- MCP/geospatial/AI-ingestion waves are promotable when URL parser normalization, diagnostic network sinks, search request shapes, AngularJS error rendering, netCDF metadata, or model config fields cross into local-network SSRF, private-network probing, skipped ACL filters, CSTI, native parser execution, or remote model-code loading. Keep proofs to owned callback services, fake records, harmless DOM markers, sanitizer traces, temp-file markers, and disposable model repositories.
- Agent repository/tooling waves are promotable when MCP alternate file paths, branch/reference arguments, IDE project-directory environment variables, exec-tool denylist guards, or registry host matching cross into local file reads, Git option injection, project bootstrap execution, shell execution, or credential forwarding. Keep proofs to fake marker files, dry-run argv logs, disposable project roots, inert command markers, owned registry/callback domains, and fake credentials.
- SurrealDB/CrateDB follow-ups should update the existing 2026-06-19 database/agent boundary page when field errors, graph paths, composite record IDs, live-query state, RPC sessions, table-scrape helpers, or blob HTTP routes cross into hidden fields, stale realtime data, cross-principal control, session leakage, or object authorization bypass. Keep proofs to two-principal labs, synthetic rows/blobs/events/session IDs, and no production dumps or workload termination.
- sigstore verifier follow-ups should update the existing 2026-06-30 auth/supply-chain verifier page when integrated time, identity, certificate OIDs, transparency-log roots, or authenticity constraints are silently ignored. Keep proofs to public/synthetic conformance bundles, disposable artifacts, and fixed-version negative controls.
- Rancher/Fleet waves are promotable when project roles, GitHub App team mappings, GitOps bundle fields, Helm repository URLs, unauthenticated webhooks, cluster-import YAML generators, or namespace labels cross into downstream cluster privileges, credential forwarding, reconciliation triggers, or Pod Security Standards changes. Keep proofs to disposable clusters, fake Helm credentials, marker Secrets/ConfigMaps, lab GitHub teams, and inert workloads only.
- Kyverno CEL HTTP advisories are promotable as Kubernetes admission-controller SSRF boundaries when namespace-scoped policy authors can trigger controller-origin `http.Get()`/`http.Post()` calls into sibling namespaces, cluster services, metadata endpoints, or owned callbacks. Update the existing Rancher/Fleet controller-boundary page and prove only with canary Services, mock metadata endpoints, synthetic error markers, and no service-account token or production internal-service reads.
- Apify/OpenClaw/ORAS/OnGres waves are promotable when actor metadata, MCP SSE redirects, registry `Location`/redirect/realm responses, OCI annotation titles, tar hardlinks, or certificate algorithms cross into bearer-token forwarding, credential leaks, outside-root filesystem effects, or channel-binding downgrades. Keep proofs to fake tokens, loopback registries, temp roots, owned callbacks, disposable CWD markers, and lab SCRAM transcripts.
- Ghost/goshs/Ignite/KubeVirt waves are promotable when preview headers, alternate file-serving protocols, share-token counters, log-path selectors, or export symlinks cross into shared-cache poisoning, mode-policy bypass, one-use-token over-redemption, outside-log reads, or pod-local file reads. Keep proofs to lab caches, harmless markers, tiny canary files, disposable namespaces, and no staff/session/secret data. The current page is `docs/alerts/2026-07-02-ghost-goshs-export-boundaries-ghsa.md`; update it for adjacent Ghost, goshs, diagnostic-log, and VM/export symlink follow-ups rather than creating duplicates.
- Contour cookie-rewrite, Langroid `TableChatAgent`, and Keycloak client-policy/FGAP/Organizations follow-ups are promotable when route YAML crosses into shared Envoy Lua, LLM table prompts cross into dataframe expression execution, or identity feature/policy/scope controls drift from token/API behavior. Keep proofs to disposable namespaces, inert Lua/config canaries, lab dataframes, synthetic roles/clients/orgs/tokens, and no xDS credentials, files, datasets, production clients, or real organization metadata.
- Contour fallback-certificate/JWT follow-ups belong on the existing Contour page when `HTTPProxy` combines fallback TLS certs with JWT providers and no-SNI or unknown-SNI traffic reaches an auth-divergent route path; prove with owned backend markers and SNI decision tables only.
- SFTPGo/SpecifyJS/Casdoor/JWT empty-key waves are promotable when public share selectors, download disposition, URL parsing/redirects/loopback aliases, GraphQL interpolation, empty HMAC secrets, or SAML/OAuth assertion binding cross into outside-share reads, same-origin rendering, SSRF, query injection, forged token acceptance, or unbound identity sessions. Keep proofs to public-share canaries, harmless HTML markers, owned callbacks/loopback services, schema-safe GraphQL fields, disposable tokens, lab IdPs/users, and no production files, internal services, tenant data, real sessions, or credential capture.
- Craft CMS follow-ups should update the existing Craft/PyLoad/Wooey boundary page when folder/asset/entry routes miss peer permissions, source-object checks, destination save checks, or post-mutation authorization; prove only with disposable CP users, marker assets, synthetic entries/sections, and before/after permission matrices.
- Grackle/PowerLine-style agent orchestration advisories belong on the existing agent repository/registry page when task branch/session metadata crosses into Git worktree commands through `shell:true`; prove only with disposable provisioned hosts/containers, argv logs, and inert marker commands.
- Mautic campaign/theme/API/Focus waves are promotable when ZIP campaign imports, Twig theme templates, API Platform owner scopes, or Focus URLs cross into filesystem writes, server-side template runtime, cross-owner CRM objects, or server-originated fetches. Keep proofs to marker ZIP entries, inert Twig canaries, synthetic contacts/reports, and owned callback hosts; never publish web shells, read config/env files, dump customer CRM data, or probe production internal networks.
- Terminal/SFTP clients, snapshot-test libraries, SSR form components, and monitoring sudoers assets are promotable when remote filenames, update-mode snapshot paths, pre-hydration browser defaults, or sudo-allowed binaries cross into local command execution, outside-root writes, credential query leakage, or privileged argument control. Keep proofs to inert filenames, temp marker files, synthetic credentials, fake monitoring users, and disposable VMs only.
- Steeltoe actuator/JWKS waves are promotable when Host-derived management-port checks, low-trust Cloud Foundry actuator roles, unsanitized connection-string keys, or `kid`-only JWKS caches cross into management endpoint exposure, sensitive runtime data, plaintext credential disclosure, or cross-issuer token validation. Keep proofs to synthetic actuator values, fake connection strings, lab CF roles, and controlled test issuers; never dump heap/thread data, environment secrets, production tokens, or customer service bindings.
- Scriban template-runtime waves are promotable when reused `TemplateContext` state, stale `MemberFilter` / `MemberRenamer` accessors, request-dependent `ITemplateLoader` include caches, or incomplete resource-limit controls cross into sandbox, tenant, role, or untrusted-template boundaries. Keep proofs to fixed canary strings, disposable object models, synthetic templates, loader-call counters, and local time-boxed harnesses; never render real customer templates, secrets, account data, or production crash payloads.

## Security / attribution

- GoFiber proxy/auth and Artemis STOMP advisories are promotable when forwarded-IP headers, BasicAuth default authorizers, or broker routing-type hints cross into upstream trust decisions, username timing oracles, or address-topology mutation. Keep proofs to duplicate-header decision tables, synthetic users, disposable brokers, and marker messages; never enumerate production users, spoof real allowlist entries, or consume live queues.
- Cilium/Formie/OpenRemote/Open Babel waves are promotable when pod-local sockets, request-derived form defaults, read-only asset roles, or scientific file parsers cross into Envoy admin access, server-side Twig evaluation, predicted datapoint writes, or native parser memory corruption. Keep proofs to socket reachability metadata, fixed Twig canaries, disposable asset datapoints, and local sanitizer traces; never dump TLS secrets, disrupt cluster traffic, read Craft config, mutate production telemetry, or upload crash files to third-party converters.
- OpenClaw approval/tooling waves and MCP memory-service document-route advisories are promotable when scoped routes, shell wrappers, native chat buttons, hooks, Control UI pairing, trusted-proxy headers, marketplace metadata, browser actions, MCP loopbacks, message reads, or adjacent memory/document route families cross into admin commands, hidden execution, unauthorized approvals, owner-only tools, durable tokens, forged identity, unscanned runtime code, private-network reads, denylist bypass, channel leakage, or unauthenticated memory read/write/delete. Keep proofs to disposable agent profiles, fake tokens, inert commands, marker files, owned canary pages, synthetic channels, and empty memory stores; never collect real prompts, chat history, credentials, node configs, or production memories.
- Later OpenClaw waves should keep updating the existing July 2 agent-boundary page when mutable chat display names, stale chat tokens, provider aliases, node lifecycle events, repo-local `.env`/package roots, sandbox prompt context, browser-tab reuse, action forwarding, Skill Workshop approval state, native command handlers, PowerShell encoded-command aliases, trusted retry endpoint parsing, or chat-native callbacks cross into allowlist, owner-tool, pairing, token, filesystem, URL-authority, exec-policy, or pending-approval boundaries.
- Coder workspace auto-creation, Dulwich submodule traversal, and Kerberos Hub redirect advisories belong on the existing agent repository/registry boundary page when workspace URL parameters, repository metadata, or custom auth headers cross into shell commands, `.git/hooks`, or redirected authorities; prove only with disposable workspaces/repos, inert hooks, fake Hub keys, and owned redirectors.
- Langroid follow-ups should update the existing TableChatAgent page when SQL agents or file tools cross from prompt-shaped `SELECT` or path input into database-host file primitives or outside-`curr_dir` filesystem effects; keep proofs to synthetic database/file canaries and never read host secrets or production datasets.
- Later Langroid waves should keep updating the TableChatAgent page when raw user chat can be parsed as tool JSON, `full_eval=True` restores Python builtins, graph/Cypher agents execute unconstrained generated queries, or SQL validator regexes miss parser-equivalent function spellings. Keep proofs to inert tool markers, disposable dataframes/graphs, mocked query logs, synthetic database/file canaries, and no shell, secret, dataset, or production graph access.
- Recce/9router/SimpleSAMLphp/Keycloak/LaunchServer/Algernon/Spatie/agno waves are promotable when developer dashboards, router/MCP routes, SAML handoffs, file-serving helpers, Windows filename canonicalization, media helpers, or vector metadata cross into filesystem read/write, process authority, wrong-issuer sessions, source disclosure, SSRF/upload bypass, or SQL construction. Keep proofs to disposable analytics projects, fake JWTs/MCP tools, lab IdPs, marker files, owned callbacks, benign uploads, and seeded vector rows; never read real config keys, IdP metadata, source secrets, customer media, or production vector data.
- WeasyPrint/server-side renderer URL-fetcher advisories are promotable as updates to the rich-text import SSRF methodology when an application validates only the initial URL and the renderer follows redirects without per-hop revalidation, or when `presentational_hints=True` lets HTML attributes cross into CSS `url()` fetches. Keep proofs to owned redirectors, owned callbacks, and explicit program-provided internal canaries only.
- flyto-core/MCP agent-server advisories are promotable when exposed HTTP MCP transports or workflow URL modules cross into unauthenticated module dispatch, dangerous-tool execution, or SSRF guard bypasses via IPv6 transition-address normalization gaps. Keep proofs to inert modules, route/auth decision tables, owned callbacks, and lab canaries; never run production shell commands or capture metadata responses.
- Scriban follow-ups are promotable when untrusted templates can mutate live CLR objects through `TypedObjectAccessor`; update the existing Scriban page and prove only with disposable object properties and pre/post render canary values.
- Nginx-UI-style management dashboard advisories are promotable when low-privilege certificate/settings/list/backup APIs accept caller-selected filesystem paths, hidden command fields, client-held backup tokens, self-contained integrity metadata, or sort parameters that later cross into file writes, restore-time configuration tampering, shell/service-control runners, or ORM `ORDER BY` fragments; update the existing Nginx-UI page for adjacent follow-ups and keep proofs to disposable labs, inert marker files/commands, tampered backup marker configs, synthetic rows, and role/route decision tables.
- uutils/coreutils replacement-tooling advisories are promotable when GNU compatibility drift crosses into trusted shell automation, installer writes, temp-file placement, permission exit codes, byte-preserving filters, or process-control parsing; keep proofs to disposable containers, marker files, dry-run wrappers, byte fixtures, and mocked syscall evidence, never destructive `rm`, mass-signal `kill`, or production path overwrite tests.
- Better Auth advisory waves should update the existing July 7 Better Auth/Aider/netfoil page when SCIM ownership, stale sessions, OAuth code or refresh-token races, SSO endpoint registration, OIDC/MCP metadata, redirect URI consent sinks, implicit account linking, organization invitations, or resource indicators cross into provider takeover, session persistence, duplicate token minting, SSRF, unsafe algorithm/PKCE negotiation, auth-origin script sinks, account takeover, role attachment, or audience escalation; keep proofs to disposable users, owned domains, fake IdPs/JWKS, canary clients/tokens, and synthetic orgs.
- Coder developer-platform waves are promotable when workspace app hostnames, browser-set forwarding headers, agent app metadata, tailnet routes, provisioner app IDs, CLI-opened app URLs, generated SSH config, OIDC claim parsing, privileged password routes, or AI Bridge proxy auth/TLS state cross into CORS data reads, cross-app routing, public exposure, traffic interception, app rebinding, token leaks, local SSH config injection, account takeover, role escalation, or LLM proxy misuse. Keep proofs to disposable Coder deployments, synthetic users, fake IdP claims/tokens, inert apps, owned callbacks, temporary SSH configs, and route/decision tables.
- Late Coder workspace-agent redirect advisories should update the July 1 agent repository/registry page when per-agent API redirects can cross into sibling-agent file read/write authority; prove only with synthetic workspaces, fake marker files, and per-hop authority decision tables.
- EGroupware/XWiki/New API waves are promotable when nested course role metadata, eTemplate overrides, rich mail image URIs, Jetty `/skin/` resource paths, notification URL hostnames, or GET identity-binding routes cross into upload/file writes, server-side evaluators, local file reads, decode-order traversal, server-originated internal fetches, or account binding state changes. Keep proofs to disposable labs, marker-only files/templates, owned callback domains, mock private canaries, and two disposable accounts; never read production files, publish shell payloads, hit real metadata/internal services, or bind real identities.
- ArchiveBox AddView-style crawler advisories are promotable when request-supplied per-job config crosses into process environment, downloader extra args, plugin hooks, or binary selectors. Keep proofs to disposable archives, owned URLs, inert wrapper logs, and temp marker files; never publish command payloads, read archive data, dump environments, or touch production browser profiles/credentials.
- DNS rebinding research is promotable as a local-service/private-network browser-pivot methodology when same-origin DNS answer changes cross into loopback, private admin/dev services, or agent dashboards; keep proofs to owned domains, lab browsers, route/auth decision tables, and synthetic canary endpoints, never arbitrary internal probing or real token/config/file reads.
- Joomla page-builder and ColdFusion KEVs are promotable when unauthenticated extension upload or pathname traversal crosses into executable web-root/runtime context; keep proofs to extension/version fingerprinting, inert marker uploads, disposable ColdFusion canaries, and route/storage evidence, never web shells, production CFML writes, credentials, or real filesystem listings.
- Weblate/oasdiff/KEDA/Goploy/ha-mcp/aiosmtplib/Kite waves are promotable when URL guards, OpenAPI `$ref` loaders, libpq connection strings, deployment namespace IDs, MCP add-on root routes, SMTP envelope values, or cluster selector headers cross into SSRF, local file reads, credential redirection, outside-namespace file/git control, unauthenticated tool policy changes, protocol command injection, or unauthorized cluster inventory. Keep proofs to owned callbacks, temp marker files, fake PostgreSQL/SMTP listeners, disposable namespaces/repos/add-ons/clusters, and route/decision tables; never read real secrets, redirect production credentials, modify live deployment remotes, invoke dangerous MCP tools, or dump cluster object details.
- Go `x/crypto/ssh` advisory waves are promotable when SSH certificate restrictions, `@revoked` CA/signature keys, partial-success MFA permissions, destination-constrained agent forwarding, public-key parameter parsing, or SSH peer message handling cross into bastion/Git/CI/deploy authorization boundaries. Keep proofs to disposable SSH CAs, test users, inert `force-command` markers, temp agent keys, owned hosts, and local harnesses; never forward production agents, test real enterprise CAs, or run broad unauthenticated DoS probes.
- phpseclib X.509 AIA SSRF and LangChain filesystem-search/loader advisories are promotable when certificate validation or agent file tools cross into outbound-fetch or sandbox filesystem boundaries; keep proofs to owned AIA callbacks, approved synthetic internal canaries, temp workspaces, symlink/sibling marker files, and never metadata endpoints, arbitrary private hosts, secrets, notebooks, model weights, or customer files.
- Apache Airflow KubernetesExecutor and auth-manager token advisories are promotable when worker pod specs or logout flows cross Kubernetes namespace read access, Execution API JWT visibility, or residual API-token validity boundaries; keep proofs to disposable DAGs, fake Variables/Connections/XComs, redacted token-prefix evidence, and no production task tokens, workflow state changes, or secret reads.
- Serena/Joro/Nuclio/DSpace/async-tar/pip/Casdoor/Waku/NL Portal/Sharp waves are promotable when browser-reachable loopback dashboards, serverless cron trigger metadata, repository templates/URI schemes/output paths, archive PAX parsing, package entry-point names, local storage roots, RSC server actions, object IDs, or quick-create commands cross into agent memory/plugin execution, shell command construction, file/template reads or writes, scanner/extractor differentials, cross-origin state changes, or authorization drift. Keep proofs to disposable browser profiles, inert memory/plugin markers, lab namespaces, synthetic templates/files/packages/archives, two-user canaries, and no shell payloads, real memories, production plugins, citizen/customer records, or sensitive files.
- Later July 8 Waku/Skipper/lxml follow-ups should update the existing integration/deploy/MCP boundary page when redirect helpers, OPA body authorization filters, or HTML cleaners trust path-only docs, `Content-Length`-dependent body parsing, or ordinary URL attributes while external `Location` values, chunked/HTTP2 bodies, or namespaced SVG URL attributes reach the sink. Keep proofs to owned redirectors, lab Skipper routes/mock upstreams, offline sanitizer fixtures, and no OAuth-code capture, production policy bypass, or credential-harvesting payloads.
- CarrierWave/TypiCMS/OpenStack Ironic-style waves are promotable when MIME denylist entries are treated as regexes without escaping, allowlisted SVG uploads are stored unsanitized on same-origin public media paths, or bare-metal console/image preparation paths let tenant-controlled metadata or image files influence conductor/provisioning helper execution. Keep proofs to harmless upload/SVG markers, fake content types, lab Ironic nodes, inert wrapper logs, and disposable images; never upload web shells, credential-harvesting SVGs, run production BMC commands, or place payloads/persistence in customer images.
- Gittensory/MetaGPT/Craft CMS/css_parser/Note Mark/Claw-style waves are promotable when REST-vs-MCP route parity, agent message mapping, CMS preview config arrays, CSS import redirects, stored export slugs, soft-delete flags, or embedded component APIs cross into object-scope bypasses, deserialization, event-handler injection, SSRF/local-file reads, outside-root writes, deleted metadata disclosure, or unauthenticated action surfaces. Keep proofs to disposable labs, synthetic profiles/notes/files, owned redirectors, harmless callbacks, and route decision tables; never collect financial data, prompts, environment dumps, real CMS files, customer notes, or production component state.
- Sylius/YesWiki-style waves are promotable when e-commerce route families or wiki form builders miss object ownership, channel, lifecycle, template, SQL, deserialization, or formula-evaluation invariants. Keep proofs to disposable shops/forms, fake payment methods, marker redirect paths, inert serialized fixtures, syntax canaries, and harmless HTML markers; never process live payments, touch customer orders, dump production tables, run gadget chains, or publish command-execution payloads.
- Treat third-party sources as untrusted until verified.
- Prefer official docs, source repositories, and primary project references.
- Keep usage guidance scoped to authorized testing, lab use, and defensive research.
- Tesla HTTP-client, psd-tools, Rattler, OpenRun, Casdoor, and sigstore-go advisory waves are promotable when redirect-following clients, multipart builders, embedded asset filenames, package-channel metadata, path-only redirect reconstruction, token exchange, or transparency-log thresholds cross into credential relay, protocol smuggling, outside-root writes, external redirects, cross-organization tokens, or single-authority threshold bypasses. Keep proofs to fake headers, owned redirectors, local multipart listeners, synthetic PSD/package fixtures, lab identity realms, fake logs, and inert artifacts; never capture live tokens, read real files, overwrite runner paths, or test production signing infrastructure.
- Netty DNS, Starlette `Host`, and pip entry-point follow-ups belong with the July 10 HTTP/client/package boundary page when out-of-bailiwick CNAME/NS data, malformed `Host` URL reconstruction, or wheel entry-point names cross into resolver-cache poisoning, path-guard bypass, or outside-script-dir writes. Keep proofs to owned lab DNS zones, local framework decision tables, disposable virtualenvs, and marker files only.
- API Platform Core JSON:API/HAL serializer-cache and morgan `:remote-user` log-forging advisories belong with the July 10 HTTP/client/package/cache boundary page when long-lived response-shape caches leak per-user guarded properties or Basic-auth usernames cross into unescaped access-log lines. Keep proofs to two-user synthetic API resources, long-running PHP lab workers, fake Basic-auth usernames, and disposable log files only.
- File Browser proxy-auth, SiYuan local-app, Authorizer OAuth, and Pimcore CMS waves are promotable when trusted proxy headers, browser-extension origins, render templates, double-decoded publish paths, marketplace/synced content, OAuth redirect URIs, or CMS route metadata cross into identity, local API, note DB, workspace file, renderer, token relay, object-permission, SQL, or deserialization boundaries. Keep proofs to disposable apps, fake proxy users, synthetic notes/files, harmless DOM markers, owned redirectors, lab OAuth tokens, seeded CMS canaries, and no real notes/tokens/assets/gadget chains.
- Kimai-style invoice PDF SSRF advisories are promotable as updates to the rich-text import SSRF methodology when Markdown or rich-text billing fields cross into mPDF/HTML-to-PDF image fetches. Keep proofs to owned image callbacks and disposable invoices/quotes; never probe metadata/internal services or use real customer/payment documents.
- Joomla form-builder/event-extension upload KEVs belong with the July 8 Joomla page-builder/ColdFusion KEV page when public or extension-owned upload routes accept dangerous file types; frame as extension upload fields crossing into web-reachable/executable storage and prove only with harmless marker files.
- DesktopCommanderMCP/Code Index MCP follow-ups belong with the July 7 agent/deploy/SSRF/identity boundary page when MCP file-reader URLs or regex search inputs cross into server-side fetches or shared runtime resource consumption; use owned callbacks, tiny disposable repositories, and bounded timeout evidence only.
- Later July 10 waves covering File Browser hook auth, SiYuan attribute-view renderers, MCP Atlassian attachment uploads, SafeInstall agent guards, BabelDOC CMap loading, MLflow AI Gateway, Windmill scoped tokens, and NotrinosERP HR documents are promotable when login credentials, synced cell data, MCP `file_path` values, agent shell wrappers, PDF CMap names, gateway secret fields, resource-scoped tokens, or upload filenames cross into shell commands, trusted desktop DOM/Electron sinks, server-side file reads, unguarded package-manager execution, pickle deserialization, environment-secret relay, out-of-scope script reads, or web-served executable files. Keep proofs to inert markers, disposable MCP/agent/PDF/MLflow/Windmill/ERP labs, fake callbacks/tokens/env vars, and no real host secrets, package malware, production credentials, script contents, or web shells.
- Late July 10 Clauster/PrestaShop/Excon/secure_headers follow-ups are promotable when dashboard auth flags, storefront slider cache values, redirect-followed headers, or CSP directive values cross into unauthenticated agent-dashboard control, PHP unserialize cache sinks, credential-header relay, or CSP policy injection. Keep proofs to disposable dashboards/stores/Ruby harnesses, fake project directories, marker-only cache values, fake headers, and harmless CSP canaries; never run real agent bridges, write web shells, capture live tokens, or harvest CSP reports from users.
- TSDProxy and melange/apko follow-ups belong with the July 10 HTTP/client/package/cache boundary page when proxy-injected identity headers leak internal management trust tokens to upstream apps, or APK clients verify signed control metadata without binding installed data-section contents. Keep proofs to lab proxy deployments, fake tokens, harmless management route decisions, owned APK repos/caches, disposable rootfs installs, and marker-only package files; never replay real proxy tokens, change production proxy config, or install substituted packages on production hosts.
- Apache Shiro Jakarta EE `Referer` redirect advisories belong with the existing return URL scheme-bypass methodology when login flows fall back to a client-controlled header after authentication. Keep proofs to lab accounts, harmless `Referer` destinations, response-chain/browser-navigation evidence, and no real-user login links or credential collection.
- Daphne/WebSocket handshake advisories are promotable as methodology updates when HTTP upgrade parsing differs across the server, handshake library, framework adapter, and application-visible scope; prove with harmless canary headers, raw-byte captures, and patched/direct negative controls. Treat adjacent unlimited-frame/message-size issues as limit-check context only unless an approved lab explicitly allows bounded DoS testing.
- Decidim/Apollo/GeoNode-style multi-tenant civic/config/geospatial waves are promotable when host-selected organizations, signed download redirects, global object IDs, admin route families, role-scoped CSV census records, stored push endpoints, delegated HTML content blocks, search ranking SQL, release IDs, AccessKey app-ID parser differentials, or rich-text metadata cross into tenant authorization, bearer-file replay, authorization-data mutation, deferred SSRF, trusted-origin browser execution, SQL expression, config disclosure, config-auth bypass, or trusted-render state-change boundaries. Keep proofs to synthetic orgs/users/files/census rows/endpoints/content blocks/releases/app IDs/namespaces/DOM markers, redact JWTs/signed URLs/release IDs/push keys/AccessKey material, and never collect real identity documents, exports, notification payloads, config secrets, CSRF tokens, or account data.
- NukeViet/DIRAC-style CMS and grid-control advisories are promotable when forwarded proxy headers reach pre-auth outbound-fetch helpers, stored attachment metadata is later used by cleanup/delete filesystem sinks, CMS filters sanitize before decode or escape for the wrong browser context, authenticated reporting/FileCatalog/PilotManager parameters reach SQL or dynamic evaluation, or pilot bootstrap fetches executable artifacts over unverified channels. Keep proofs to owned callbacks, synthetic CMS users/comments/articles, disposable canary files, inert DIRAC harness markers, fake pilot archives, and no internal probing, production file deletion, shell payloads, token/proxy reads, production pilot tampering, or real visitors.
- Trail of Bits Rust Testing Handbook updates are promotable as source-assisted audit workflow when they give a repeatable ladder across unit tests, Miri-style dynamic analysis, proptest-style generators, coverage, mutation testing, Kani-style model checking, and dependency review; frame reports around reachable trust-boundary invariants rather than tool output alone.
- n8n-MCP/Woodpecker CI/Anyquery server-mode waves are promotable when workflow backup snapshots, gRPC agent metadata, or SQL-compatible server commands cross into tenant history, agent identity, or filesystem writes. Keep proofs to synthetic workflow snapshots, lab agent IDs/jobs, disposable SQLite marker files, and no real workflow credentials, CI secrets, repos, or service paths.
- Koel Subsonic and MantisBT waves are promotable when compatibility media APIs or legacy SOAP/config routes skip primary validators, identity binding, or workflow/config sink checks: Subsonic radio/podcast URLs crossing into SSRF, native validation chains that keep network-fetching validators running after `SafeUrl` failures, NAT64/6to4 transition-address gaps in SSRF guards, SOAP cookie-string plus caller-supplied username crossing into impersonation, admin config values reaching PHP `eval()`/SQL `ORDER BY`, or REST/SOAP status/note/version updates bypassing stricter workflow, billing, reminder, or release-lifecycle thresholds. Keep proofs to owned callbacks, synthetic streams/feeds, lab bug reports/users, inert config markers, fake billing windows, and no metadata probing, real bugtracker data, credential dumps, web-shell writes, real billable-hours fraud, or weaponized payloads.
- TensorZero/FiftyOne/Obsidian/ToolHive/safeurl/downloader/MCP documentation-server waves are promotable when internal object-storage helper parameters, remote MCP auth-discovery metadata, local AI/data apps, vault REST paths, URL safety libraries, service-specific crawler host matching, or default documentation Web UIs cross into file reads, host-side outbound fetches outside sandbox/egress isolation, browser-readable localhost data, encoded-separator traversal, IPv6 transition-range SSRF bypasses, API-key relay, or unauthenticated corpus administration. Keep proofs to disposable gateways, owned MCP servers/callbacks, local app profiles, synthetic vault/files/datasets/docs, owned browser origins/callbacks/object stores, fake credentials, marker-only LAN/container clients, and lab NAT64/IPv6 fixtures; never read real notes, notebooks, datasets, home-directory secrets, cloud metadata, object-store credentials, live downloader API keys, production document corpora, or host-side environment variables.
- systeminformation/ViewComponent/django-haystack/OpenFeature/OpenStack/Fory waves are promotable when host inventory parsers, Rails component render hooks/caches, search-result deserializers, Kubernetes operator annotations, Neutron shared-network roles, serializer type filters, or RPC TLS clients cross into shell execution, trusted HTML/context reuse, Python evaluation, cross-namespace spec disclosure, trusted service-port behavior, Java hook invocation, or broker impersonation. Keep proofs to disposable Linux/Rails/Django/Kubernetes/OpenStack/Java labs, synthetic config/search/spec/port/cert fixtures, inert markers, and no production network config, real tenant traffic, feature-flag tokens, RabbitMQ credentials, or destructive cloud operations.
- Apple `container` pf rule-injection advisories are promotable when domain/label arguments cross from constrained sudo or automation into root-owned `pf` grammar. Keep proofs to disposable macOS labs, marker-only rules, anchor diffs, and restoreable `pf` state; never redirect or block production traffic.
- Axios `NO_PROXY` IPv4-mapped IPv6, Go `x/crypto/ssh` certificate/agent-constraint, and FortiSandbox command-injection waves are promotable when URL canonicalization, SSH certificate/agent metadata, or appliance HTTP parameters cross into proxy routing, authorization constraints, forwarded keys, or OS command wrappers. Keep proofs to fake proxy logs, disposable SSH certificates/keys, lab appliances, and marker-only commands; avoid cloud metadata, real bastions, private keys, and production appliance command execution.
- Envoy Gateway/Kuma/MCP Python SDK waves are promotable when custom Gateway API backend references skip target-namespace `ReferenceGrant` consent, extension-policy validation or xDS auth exposes controller resources, service-mesh CLI/dataplane profiles send tokens over unverified HTTPS, local MCP HTTP/SSE/WebSocket transports lack browser-origin protection, or MCP session/task IDs miss principal/owner binding. Keep proofs to disposable namespaces/backends, synthetic files/resources, fake mesh tokens, owned browser origins, canary MCP tools/tasks, and negative controls; never route production traffic across tenants, capture real tokens, invoke dangerous MCP tools, collect service-account tokens, or read local files.
- ArcadeDB/Nuclio/Pheditor waves are promotable when database handler families, SQL-defined functions, trigger scripts, import sources, serverless build specs, handler paths, default editor credentials, or terminal allowlists cross into cross-database access, user-code execution, SSRF/file reads, build-time execution, outside-root writes, or shell execution. Keep proofs to two disposable databases, marker rows/series, synthetic files, owned callbacks, inert build logs, and temp marker commands; never read host secrets, cloud metadata, customer records, or write production functions/web roots.
- Nuclio-style serverless dashboard waves are promotable when function build metadata or handler module paths cross into build-time code execution or filesystem writes; keep proofs to lab dashboards, canary functions, temp paths, and inert build markers only.
- NLTK `nltk.data.load()` advisories are promotable on the July 15 local AI/data app boundary page when `nltk:` resource strings are validated before URL decoding and encoded separators/traversal segments can reach files outside NLTK data roots. Keep proofs to synthetic temp canary files and never read `/etc`, `/proc/self/environ`, notebooks, app source, cloud config, credentials, or service-account material.
- JWT/JWS algorithm-confusion advisories are promotable as a reusable exploit-path methodology when JOSE headers, key type, `kid`/JWKS selectors, issuer/audience, or token-family purpose are not bound before claim trust. Keep proofs to disposable users, synthetic claims, owned callback JWKS hosts, negative-control decision tables, and redacted tokens; never forge real admin/customer identities, brute-force secrets, or collect production data.
- Axios `NO_PROXY`, Go `x/crypto/ssh`, and vLLM runtime-channel updates were grouped as proxy/certificate/AI-worker boundary checks: prove IPv4-mapped IPv6 proxy-routing decisions with fake proxies, SSH certificate/agent constraint drift with disposable keys and decision tables, and vLLM PyNccl/ZeroMQ/prompt-embedding parser exposure only in isolated labs with synthetic markers.
- vLLM follow-up waves should update the existing July 17 Axios/SSH/vLLM page when ASGI `Host` URL reconstruction, OpenAI API-key route checks, audio downmixing, prompt-embedding parsing, or worker runtime channels cross into auth bypass, hidden model input, memory safety, or state exposure. Keep proofs to malformed-Host decision tables, synthetic audio fixtures, lab model endpoints, and harmless markers; skip standalone resource-exhaustion-only vLLM advisories unless they add a reusable trust-boundary validation pattern.
- Prompty, MCP memory, and Flask-Reuploaded-style waves are promotable when prompt frontmatter, prompt file references, MCP memory import paths, or upload `name` overrides cross into parser code execution, outside-root file reads, parse-error byte leaks, or dangerous-extension validation asymmetry. Keep proofs to disposable prompt bundles, synthetic marker files, lab MCP servers, temp upload roots, and inert dangerous-looking filenames; never read real workstation secrets, service-account JSON, user memory sessions, customer uploads, or publish executable web shells.
- Skipper/CloudTAK/AngleSharp waves are promotable when OPA body truncation, unauthenticated route aggregation services, GIS/map proxy URL parameters, or server-side HTML parser/formatter mismatches cross into policy fail-open, cluster-wide topology disclosure, full-read SSRF, or sanitizer-vs-browser mXSS. Keep proofs to lab Skipper/OPA deployments, synthetic Kubernetes routes/cache names, owned callbacks/local canaries, and inert DOM markers; never dump production route tables, query metadata/internal services, expose real service names, or deliver browser payloads to real users.
- VMware Avi/PicoClaw/Halo waves are promotable when load-balancer control-plane route families, agent `web_fetch`/context builders, or migration backup download endpoints cross into authn/authz bypass, RCE preconditions, SSRF, untrusted control-sphere inclusion, or outside-root file reads. Keep proofs to Avi route/role/status matrices, owned callbacks, synthetic local services, inert context markers, disposable backup marker files, and no production control-plane exploitation, metadata probing, real backups, or non-marker file reads.
- NestJS/Astro framework waves are promotable when path canonicalization skips route middleware, spread-object keys become server-rendered attribute names, or deployment adapters broaden image host/path allowlists. Keep proofs to trailing-slash decision tables, inert `data-*` DOM markers, generated-config diffs, owned image origins, and no production auth bypass, executable browser payloads, or internal fetch targets.
- n8n/Langflow follow-up waves should update the existing June 24 n8n and June 19 Langflow boundary pages when public webhook/flow inputs, provider trigger signatures, webhook response headers, local Git paths, code runners, cached sandboxes, database-node fields, or knowledge-base names cross into credentialed actions, same-origin content, workflow execution, files, code, cross-project state, query structure, or outside-root writes. Keep proofs to disposable multi-user labs, owned callbacks, synthetic repositories/files/databases, inert code/DOM markers, and patched negative controls; never use production credentials, repositories, databases, buckets, or tenant data.
- Axios prototype-pollution read-side gadgets should update the existing July 17 Axios/SSH/vLLM page when an own empty or partial `auth` object inherits polluted Basic-auth subfields. Require a separate host-process pollution primitive, use fake credentials plus a loopback capture server, and distinguish request tampering from unproven credential disclosure; skip adjacent recursion-only DoS entries as standalone guidance.
- Composer repository/package advisories are promotable when invalid transitive package names escape `vendor/`, or root/repository-supplied Perforce metadata reaches shell-built commands. Keep proofs in disposable roots with owned repositories, inert argv recorders, outside-`vendor` marker files, source-vs-dist controls, and patched comparisons; never target startup files, SSH keys, cron paths, developer homes, real mirrors, or repository credentials.
- Cloudreve/Pillow/Better Auth/Astro/File Browser follow-ups are promotable when missing OAuth client metadata makes scope checks disappear, remote-download URLs reach restricted final destinations, filenames reach Windows viewer shells, malformed image row geometry reaches returned pixels, ordinary org members can register SSO providers, transition values break from generated CSS into HTML, or scoped-file create/cleanup paths follow escaping symlinks. Keep proofs to disposable users/clients/orgs/filesystems, owned callbacks, marker-only Windows and symlink paths, synthetic media/canary buffers, fake IdPs, and inert DOM markers; distinguish user authority from OAuth-client scope, crash from disclosure, denied responses from cleanup side effects, and framework use from attacker-controlled sink reachability.
- File Browser archive-rewrite, Astro decode-depth, and Composer `bin`/verbose-log follow-ups should update their existing July boundary pages: prove archive-time backslash-to-slash traversal with entry listings only, middleware/router canonicalization drift with representation matrices, package `bin` permission changes with disposable marker files, and URL-username logging with fake tokens. Do not call permission-only `chmod` RCE or verbose-log exposure remote disclosure.
- Protobufjs Text Format, webpack dev controls, Guzzle cookie/proxy routing, Directus cache/import, LightRAG auth/CORS, and Mistune include/render waves are promotable when parsed keys, browser origins, client state, cache identities, URL classifiers, guest-token branches, or Markdown directives cross into inherited policy values, local state changes, wrong authorities, cross-share responses, loopback fetches, API-key bypass, files, or DOM contexts. Keep proofs to per-object canaries, editor wrappers, fake credentials, two-share fixtures, synthetic localhost services/documents/files, and browser decision tables; do not infer bearer attachment, file disclosure, global prototype pollution, or legacy-scheme execution without a proven target gadget.
- Pillow TGA RLE, Axios option-construction, File Browser identity/share, late Astro adapter/render, and Guzzle cookie/redirect waves are promotable when packed image rows, interceptor-cloned or nested client options, normalized usernames, serialized share records, generated CDN regexes, raw path separators, spread keys, RSS fields, transition directives, pipeline middleware, redirect fragments, or host-only cookies cross into output memory disclosure, proxy/header/body changes, shared home directories, returned bypass capabilities, broader image fetches, route/auth drift, XML/HTML contexts, missing origin checks, referer disclosure, or cross-host cookie send. Keep proofs to seeded heap markers, fake proxies, two-user homes, disposable shares, owned image origins/redirects, raw-path matrices, fake cookies, and inert parsed DOM/XML/state markers; skip availability-only parser/runtime entries and sparse advisories without an exact bounded source-to-sink workflow.
- rclone Remote Control advisories are promotable when unauthenticated `--rc-serve` paths create inline backends and turn `GET`/`HEAD` into process-context command or local-file effects; prove only with disposable rclone processes, inert temp-file markers, synthetic files, and browser/direct-client decision tables, never production files, real remotes, or weaponized URLs.
- WordPress REST batch-route plus `WP_Query` advisories are promotable when route interpretation exposes an extension-facilitated SQL boundary; prove batch-vs-direct routing, parameter type transitions, and synthetic result-set changes separately, and stop before RCE, file, user-table, or option-table effects. Loofah helper/browser, Temporal namespace ID/name, MCP localhost rebinding, Vault auth-header relay, LlamaIndex SQL construction, and `jsrsasign` DSA-domain waves are promotable as developer-control boundary checks with inert parser, tenant, browser-origin, fake-token, synthetic-query, and disposable-key evidence.
- .NET SDK/ASP.NET Core/WPF waves are promotable when shared container-build paths cross user boundaries through link following, Negotiate principals cross into LDAP role queries, or attacker-reachable XAML crosses into privileged parser code execution. Keep proofs to disposable multi-user builders and OCI layers, synthetic directory users/roles, benign XAML markers, and fixed-version comparisons; skip adjacent availability-only SignalR/XML entries and never run tainted images or open crafted XAML on real workstations.
- pgJDBC channel-binding and `js-toml` duplicate-key advisories are promotable when required SCRAM channel binding silently negotiates a non-PLUS mechanism or falsy scalars become truthy structures through duplicate table keys. Keep proofs to disposable database roles/lab certificates and marker-only TOML policy branches; distinguish `channelBinding=require` from expected `prefer` fallback, and do not call per-object type confusion prototype pollution. Excon redirect-header retention already lives on the July 10 HTTP client boundary page; update that page rather than duplicating it.
- Hono adapter follow-ups belong on the June 24 Hono/n8n page when repeated API Gateway v1 or VPC Lattice headers are substring-de-duplicated before policy logic, or encoded backslashes split differently across URL routing and Windows static-file resolution. Keep proofs to deployed adapter fixtures, synthetic repeated values, disposable Windows static roots, and route/middleware/path decision tables; do not claim external-root traversal for the confirmed `serve-static` issue.
- Hono SSR memo-lifetime and proxy response-header follow-ups belong on the May 9 render/cache page when ambient request context survives into later SSR output or `Connection`-nominated extension headers survive proxy forwarding. Use two-principal warm-instance markers and owned mock origins with inert headers; never use real account data or claim cache poisoning/authorization impact without a downstream consumer.
- Nexus Repository, ZenML, panel, and appliance waves are promotable when delegated permissions, configuration properties, lifecycle state, shared artifacts, alternate APIs, webhook/template fields, or unauthenticated appliance parameters cross into stronger repository, runtime, session, SQL, file, network, or process authority. Use disposable roles/repos/artifacts/appliances and denied sinks; never mint usable admin sessions, change passwords, retrieve tenant/panel data, load hostile pickle, scan internal services, or execute production commands.
- Late package-build, URI, and Hono SSR waves are promotable when Unicode filename normalization defeats sdist exclusions, dependency version metadata reaches Docker-bundling shell commands, URI policy parsers disagree with network consumers about IDN/authority hosts, async JSX context crosses concurrent requests, or `cx()` marks untrusted class text pre-escaped. Keep proofs to local archive listings, inert bind-mount markers, owned destinations, barrier-controlled two-request harnesses, and harmless DOM markers.
- Gitea/GitPython/Sigstore OCI/Vitest/SharpCompress/SVGO/DOMPurify/Jackson follow-ups are promotable when repository redirects, stale branch approvals, revocation residue, Git option grammars, registry-host credential lookup, browser-runner provider commands, archive directory/symlink handling, SVG namespaces, custom-element hooks, or JSON naming/view rules cross into internal fetches, merge policy, continuing access, helper execution, token relay, files, executable render contexts, or restricted fields. Update existing Gogs/Flowise/developer-control pages where adjacent; keep proofs to disposable repos/accounts/registries/test runners/archives/renderers and marker-only fields/files.
- Large Gitea waves should extend the existing Budibase/Gogs/Gitea forge-boundary page and be grouped by outbound fetch/transport trust, token/session/OAuth decisions, Actions/LFS/repository authorization, and attachment/oracle boundaries. Mark availability-only parser/package items processed without publication; use owned callbacks, fake tokens/IdPs, disposable repos/orgs, marker artifacts/LFS/attachments, and never real CI secrets or private source.
- Late URL/schema/sanitizer waves should extend the July 21 developer-control page when parser hosts, clone URL variables, database metadata, SVG/URI helpers, Jackson views, or xDS policy translation cross into network destinations, process environment, generated code, browser interpretation, write-side binding, or authorization decisions. Keep proofs to owned listeners, fake environment markers, disposable schemas, inert DOM fields, synthetic beans/RPCs, and fixed-version decision tables.
- ProjectDiscovery `pd-agent` internal network scanning launches are promotable as tool-specific operator workflows when they preserve source-vantage reachability, narrow template selection, replayable request/response evidence, and segmentation negative controls. Keep auto-discovered private/Kubernetes ranges outside scope until explicitly approved, and never publish generated enrollment commands or organization credentials.
- Wagtail `TableBlock` advisories are promotable when page-author-controlled table class metadata crosses into executable attributes in a higher-privileged viewer's page origin. Require actual `TableBlock`/authoring reachability and prove only with harmless DOM markers in disposable author/viewer sessions; do not infer unauthenticated visitor impact.
- n8n follow-up waves should update the existing June 24 Hono/n8n/Flowise page when Project Viewer agent chat, Git clone path races, HTML `srcdoc` preview, Resource Locator URLs, or JWT header construction cross into stronger node tools, community-node paths, same-origin browser execution, unsafe navigation, or credential artifacts. Keep proofs to inert tools/repos/DOM markers, instrumented navigation, and fake service-account keys; never use production connectors, executable repositories, real browser sessions, or cloud credentials.
- Check Point SmartConsole application-token bypass KEVs are promotable when untrusted Management Server reachability and unrestricted Trusted Clients form the documented preconditions. Keep validation to two-vantage reachability, Trusted Client decision tables, vendor-confirmed build evidence, and redacted application-token audit events; never obtain administrative tokens or change policy on customer systems. When Check Point's CVE record and live support article disagree on Jumbo Hotfix take boundaries, record the discrepancy and require vendor confirmation rather than asserting a threshold.
- Agentic benchmark rogue-path reports are promotable as methodology updates when valid-looking solves come from environment variables, mounted files, local sockets, shared trace/control services, neighboring challenge networks, or public source instead of the assigned target. Require per-run proof provenance, canary surfaces, precise task scope, network/filesystem/tool enforcement, and bounded turn/time/cost budgets.
- Dompdf local-resource advisories are promotable when raw string-prefix chroot checks admit sibling paths or repeated `@font-face` loads produce a stable synthetic file-existence differential. Keep proofs to disposable sibling directories, benign resource markers, bounded workers, and fixed-version controls; never enumerate production files or deliberately exhaust shared render workers.
- Dompdf data-URI SVG follow-ups should update the July 22 Dompdf page when a nested image is re-resolved by `php-svg-lib` without Dompdf's chroot policy. Prove only with a synthetic outside-root image and separate nested-parser policy loss from raw-path prefix bugs.
- Late n8n identity/credential/node waves should update the June 24 n8n page when token issuer/subject/email/role claims, API scopes, cross-project folder references, pagination, nested workflows, expression-backed credential types or external-secret references, AI/MCP endpoints, Git operations, mail content objects, image formats, computer-use search patterns, or legacy database-node interpolation cross account, project, credential, process, network, filesystem, or query boundaries. Keep proofs to fake issuers/tokens/credentials, two-project canary folders, synthetic secret providers, owned callbacks, inert process markers, disposable adjacent files/repos, and scratch database rows.
- Netty HTTP bridge/text-protocol/CORS/XML/OCSP waves should update the June 11 codec page when H2 authority translation, legacy WebSocket handshakes, multipart/STOMP/PROXY delimiters, null origins, XML factories, or OCSP identity/freshness/event ordering cross routing, framing, backend, parser, or TLS trust decisions. Use raw-byte fixtures, mock peers, owned XML callbacks, local CAs, and marker-only post-handshake data.
- JupyterLab image/new-tab, settings-import, and extension-policy waves should update the June 19 JupyterLab page; Next.js Server Action/rewrite/cache/middleware waves should update the May 11 Next.js page; LiteLLM guardrail/MCP/OIDC-file/Skills waves should update the June 8 LiteLLM page. Jetty path-parameter, H2/H3 authority, keep-alive trailer, and Digest encoding advisories belong on the July 22 Jetty page. Keep proofs to inert DOM/packages/tools/files, owned callbacks, synthetic routes/users, raw-byte captures, and fixed-version decision tables.
- DOMPurify state and context waves are promotable when profiles, functional allowlists, hooks, persistent config, Trusted Types policy, DOM-return modes, hostile live nodes, or second parsing contexts cross into broader policy or executable DOM. Keep proofs to same-process disposable harnesses, harmless DOM markers, fresh-instance controls, exact sink traces, and patched comparisons; distinguish retention from execution, string input from live objects, and first-parse sanitizer output from downstream reparsing.
- PostCSS source-map, PhpSpreadsheet `WEBSERVICE()`, and Auth.js identity waves are promotable when CSS annotations, approved spreadsheet fetch origins, truthy auth objects, email normalization, or global OAuth check cookies cross into host files, unapproved redirect destinations, fail-open route checks, changed magic-link recipients, or cross-provider account linking. Keep proofs to synthetic files, owned redirect/mail endpoints, fake providers, disposable accounts, and hashed flow artifacts; skip adjacent parser/resource exhaustion items and distinguish process-local reads, deployment misconfiguration preconditions, normalization differentials, and provider-specific flow binding.
- React Router navigation/RSC/hydration waves should update the existing June 3 React Router page when mixed slash/backslash forms, relative colon-bearing paths, unstable RSC error redirects, or serialized SSR error subtypes cross into browser destinations, executable schemes, meta-refresh sinks, or browser-global constructor lookup. Keep proofs to owned origins, harmless dataset/counter markers, disposable RSC/manual-hydration fixtures, and fixed-version representation matrices; require the application-specific input overwrite path and do not infer XSS or constructor reachability from package presence alone.
- c3p0-style deserialization advisories are promotable as classpath-composition checks when JavaBeans introspection exposes JDBC connection getters to an automatic property-reading carrier. Require a reachable deserializer, carrier, c3p0 object, and compatible runtime dependency; prove with `Introspector` output plus a no-network marker `DataSource` counter, never weaponized JDBC/JNDI gadget streams or production database connections.
- Caddy FastCGI, ImageMagick operation-policy, and Electron updater/AppImage advisories are promotable when exact auth-header deletion collides after CGI normalization, image operations omit configured path-policy checks, redirect handlers preserve case/alias variants of credential headers, or generated runtime paths contain empty components that resolve to the launch directory. Keep proofs to synthetic identity fields, denied marker files, fake updater tokens on owned origins, and inert libraries in disposable launch directories; skip adjacent parser/resource-only items without a stronger boundary.
- ImageMagick `-script`, APNG encoder, and external-delegate policy-bypass follow-ups belong on the July 24 Caddy/ImageMagick/Electron page. Test denied reads and writes separately with marker-only scripts, synthetic APNG outputs, and owned delegate fixtures; skip adjacent memory leaks, allocation-failure UAFs, and one-byte debug-profile disclosure.
- Kiota advisory waves are promotable when OpenAPI descriptions, `.kiota/workspace.json`, AI plugin extensions, external `$ref` values, dependency recommendations, names, comments, or literals cross into generator network requests, outside-repo writes, shell handoffs, generated source, or downstream manifest paths. Keep proofs to owned callbacks, temp sibling schemas/outputs, fake commands, inert source markers, and disposable plugin resolvers; separate generation-time, compile/import-time, and downstream-host effects.
- Better Auth passwordless/Stripe/SCIM follow-ups belong on the July 7 Better Auth boundary page when pre-created passwords survive mailbox proof, middleware authorizes a different organization than a billing handler targets, or SCIM provider IDs/lifecycle fields cross identity namespaces. Ray WebDataset unsafe default decoding belongs on the existing May 5 Ray ingestion page. Use owned aliases, fake billing/IdP fixtures, synthetic users, inert pickle/torch markers, and patched decision tables only.
- PyMdown `b64` and Trix parser advisories are promotable when untrusted Markdown image paths cross into build-process file reads, or paste/drag metadata crosses attachment classification into an unsafe rich-text model URL. Require actual extension/input-channel reachability; prove with synthetic image canaries and instrumented harmless navigation markers, and distinguish editor retention from server-persisted viewer impact.
- GitPython section-name, Velocity.js property-read, Backstage experimental OAuth glob, and Mongoose update-casting waves are promotable when repository names, template reads, full-URL patterns, or dotted update keys cross into trusted Git config, dynamic function construction, redirect authorities, or process-global prototypes. Use parse-only inert Git keys, no-shell counters, owned OAuth codes/origins, and fresh-process prototype snapshots; require exact feature/input reachability and patched controls.
- PostCSS previous-map traversal follow-ups belong on the July 23 PostCSS page: 8.5.12 only restricts target extension, while 8.5.18 confines `.map` paths; prove full `sourcesContent` disclosure with synthetic outside maps. Prompty Nunjucks SSTI belongs beside the July 24 Velocity.js template boundary and requires attacker control of the `.prompty` body plus the TypeScript renderer, proven with inert counters only.
- Open WebUI v0.10.0 channel/realtime waves should update the May 8 model/channel/knowledge page when object IDs, socket sessions, revoked JWTs, background automations, direct capability routes, or same-origin Pyodide cross user, channel, lifecycle, or delegated-execution boundaries. Keep proofs to two-user labs, marker messages/files/models, inert browser counters, and transport decision tables; never collect tool keys, real prompts, or invoke admin Functions. Fastify static canonicalization, kin-openapi/Camel fail-open authentication, and cel-go JSON-tag field exposure can share a framework-boundary page when proven with synthetic routes, marker files, fake tokens, and canary structs.
- Late Open WebUI terminal/model/file/fetch advisories belong on the May 8 page; Budibase role/identity/SQL items belong on the June 22 automation page; Cloudreve WOPI/share/scope items belong on the July 20 page; and OpenAM class-loading/WebAuthn/consent items belong on the June 22 container/OpenAM page. Keep proofs to mock upstreams, two-user/two-app fixtures, disposable drive objects, no-network marker classes, and parser/authorization decision tables.
- yt-dlp shortcut output, Spring Integration remote-server path writes, and MCP URI-to-SQL advisories are promotable as downstream artifact/integration boundaries. Prove generated shortcut grammar without opening it, outside-root writes only in disposable client roots, and SQL structure changes only against read-only scratch schemas.
- Poweradmin/Kite/Hubuum/SM2 waves are promotable when identity collation, split parent/child authorization, post-RBAC path normalization, runtime RNG selection, or HTTP transport/base-path handling crosses into wrong accounts, DNS records, Kubernetes resources, predictable key material, or credential-bearing requests. Keep proofs to fake IdPs, disposable DNS zones/namespaces, deterministic synthetic keys, owned listeners, and fake tokens; never alter production DNS, read cluster Secrets, or use recovered real keys.
- FrontMCP/AWS MCP/etcd/OpenList/Oh My Posh/OmniFaces/http4s blaze waves are promotable when recurring fetches bypass initial URL policy, startup policy fails open, watch intervals exceed exact-key grants, path components are authorized before canonicalization, repository/path text reaches prompt templates or terminal controls, JSF resource/channel tokens cross browser or server boundaries, or trailers/parser laxity changes trusted HTTP fields and message boundaries. Keep proofs to owned callbacks, inert tools, synthetic keyspaces/files/repos/channels, harmless DOM/terminal markers, and one-connection parser fixtures; skip availability-only siblings and never query metadata, production etcd, tenant files, or shared-user HTTP traffic.
- Browser-automation and static-root advisories are promotable when URL policy covers explicit navigation but misses clicks/other browser actions or unsupported schemes, or when lexical root checks run before symlink resolution. Keep proofs to disposable browser profiles, loopback-only canaries, synthetic files, and temporary roots; require the authenticated browser/readback or symlink-placement precondition and never probe metadata/private services or real host files.
- Desktop agent render-to-native chains are promotable when untrusted skills or retrieved content can steer model output into a raw-HTML webview sink and that window holds broad Tauri/native capabilities. Prove instruction influence, harmless DOM execution, bridge authority, and a disposable marker-only native effect as separate edges; never use credential access, persistence, callbacks, or destructive commands.
- Agent messaging and approval waves are promotable when mutable display names satisfy stable-ID allowlists, unsigned loopback callbacks supply approval identity, approval cards omit applied args/env, anonymous reader identities reach MCP tools, or registration routes update existing assets. Keep proofs to synthetic contacts, inert approvals, fake MCP payloads, temporary workspaces, and two-user device assets; record source/version discrepancies explicitly.
- Schema-to-code generators are promotable when extensions such as `customBasePath` break out of generated import syntax. Separate generation-time source emission from later file import and same-process dynamic-model execution, using inert assignments and patched rejection controls only.
- WordPress identity and integration waves are promotable when webhook proofs, site-registration identities, client-selected roles, portable download tokens, uninvoked REST permission callbacks, empty default integration keys, or checkout return URLs cross into account sessions, capabilities, protected artifacts, booking objects, web-accessible writes, or external origins. Keep proofs to synthetic low-role users, marker packages/bookings/text files, fake keys, Stripe test mode, and owned origins; never target real administrators, customer bookings/files, executable uploads, or live payments.
- SSSD/CDI/Kimi Code/SmartSign/Thrift waves are promotable when directory search scope or GPO paths, Kubernetes clone subresource permissions, agent fetch DNS/redirect decisions, signed-document CPS/XML metadata, or TLS binding hostname checks cross into host policy, root-owned writes, cross-namespace volumes, internal fetches, native handlers, preview-time XXE, or wrong-host sessions. Keep proofs to disposable directory trees/GPOs/PVCs, owned canaries, synthetic signatures/certificates, inert handlers, and local CAs; never alter production auth policy, clone tenant volumes, query metadata/internal services, read local files, or intercept real credentials.
- Package-provided web editors are promotable when default route groups expose request-controlled content writes into PHP or other runtime-loaded hooks. Prove route/middleware reachability, disposable-file replacement, and the later load edge separately with hashes and no-op counters; never overwrite production payment/application hooks, invoke real transactions, publish command payloads, or infer execution from a changed file alone.
- Hostname/LAN-transfer/template-runtime waves are promotable when IDNA conversion merges an allowed raw host with a denied canonical authority, unauthenticated transfer names escape a download root, or stock unauthenticated template data reaches a dynamic evaluator. Keep proofs to owned names, sibling-directory text markers, and instrumented no-op evaluator counters; never write startup/application paths, transfer executables, or publish/run encoded eval payloads.
- Casdoor/Logto/OIDC::Lite/Cal.com/phpMyFAQ waves are promotable when query/body object selectors, SSO proof and lifecycle fields, token-selected algorithm lists, webhook team IDs, or privileged group/path/package settings cross tenant, principal, verifier, delivery, or application-root boundaries. Keep proofs to disposable tenants and identities, local verifier harnesses, owned webhook receivers, inert groups/files/archives, and fixed-version decision tables; never link real accounts, receive customer events, delete live configuration, or upload executable packages.
- VeloCloud Orchestrator On-Prem KEVs are promotable when an unauthenticated web request reaches functionality intended only for internal use and crosses into a host command boundary. Require exact on-prem product/build and network-reachability evidence; keep command proof to isolated marker-only labs, never pivot to managed Edge devices, and record Arista's current `7.0.x` affected-threshold versus resolution-table discrepancy.
- Regular Labs Joomla extension waves are promotable when article creator/modifier identity, source representations, AJAX token/object checks, database-replacement routes, or forwarded client-IP values cross into code evaluation, include paths, privileged handlers, broad database operations, or location rules. Keep proofs to inert evaluation counters, canonical-path evidence, synthetic objects, rollback-only temporary tables, and owned proxies; require exact extension/edition/version because initial GitHub/NVD records may omit affected and fixed builds.
- Kubernetes proxy identity advisories are promotable when caller-supplied `Impersonate-*` headers survive a trusted proxy transition or an agent-facing listener joins clients without certificate/token identity validation. Prove with narrow synthetic groups, marker-only RBAC, isolated agent pools, and registration evidence; never inject real admin groups, read Secrets, or intercept production control-plane traffic.
- Sandbox-runtime rootfs advisories are promotable when a workload can recreate a hidden pmem/block device and mutate a more privileged guest filesystem despite read-only or discard-write hypervisor intent. Separate live guest mutation, host-image persistence, guest-root execution, and host escape; prove with offline-mapped fixed-size markers or inert lab-only counters, never standard guest binaries, callbacks, or production images.
- Multi-archive extractor advisories are promotable when one extraction persists a directory symlink and a later extraction reuses the destination. Include sibling-prefix roots in fixed-version checks because raw string-prefix realpath guards may leave an incomplete fix; prove only with a benign marker in a disposable sibling directory. For zip-lib CVE-2026-17524, the July 28 unreviewed GHSA says `<1.1.0`, but the verified Linux sibling-prefix fixture remained positive through `1.2.0` and was rejected from `1.2.1`; preserve that source-versus-fixture discrepancy rather than silently repeating the advisory range.
- MCP/WordPress identity and restore-state waves are promotable when non-loopback MCP listeners lack caller identity, session-wide OTP proof selects a different phone/account, reset or subscription identifiers lack owner binding, low-role profile routes replace site-wide integration tokens, or public restore-state secrets authorize outside-root copy/delete operations. Prove with inert MCP counters, two synthetic users/customers, fake OAuth values, temporary roots, and marker-only restore state; preserve active-restore and multi-request preconditions and never invoke cloud tools, real payments, or production filesystem operations.
- WordPress nonce/option chains, payment-status replay, and device virtual-filesystem advisories are promotable when low-role or public nonces cross into arbitrary option reads/writes and privileged login routes, authentic provider responses are accepted for a different payment object, or unauthenticated file APIs reach internal virtual paths. Keep proofs to fake connection values, plain-text options, mocked ticket orders, and pre-seeded device canaries; preserve configuration, timing, parent-scope, firmware, and source-identifier discrepancies, and never use live payments, administrator accounts, device parameters, or executable files.
- Pocket ID and similar OIDC refresh-lifecycle advisories are promotable when a token issued during an authorized state continues rotating after client revocation, account disablement, or required-group removal. Keep proofs to disposable users/clients, redacted token lineage, and status-only resources; test each state transition independently and never reuse production refresh tokens.
- Workflow/CI/client-library advisories are promotable when special-use destination ranges, untrusted lint inputs, bundle module names, TLS backend flags, server-supplied redirect authorities, or secure-to-insecure redirect schemes cross into SSRF, scan-time evaluation, outside-root writes, hostname-check bypass, automation-client network pivots, or credential forwarding. Keep proofs to owned CGNAT listeners, no-op evaluator counters, temporary sibling roots, local test CAs, two-listener mock automation servers, and fake credentials; never query metadata/internal services, execute source-controlled commands, overwrite real files, intercept mail, or redirect production sessions.
- Pocket ID reauthentication follow-ups belong on the existing identity/tenant lifecycle page when one-time or signup-token access is accepted as proof for a passkey-required client, especially where session cookies are presence-checked rather than validated. Prove login method, session binding, reauthentication-token issuance, and disposable-client grant as separate edges.
- Registry-pack provenance and signed-token purpose advisories are promotable when deleting lock/trust metadata makes an installed pack easier to load, or when one action's valid signed token is accepted at another sink. Prove fail-open package loading with inert artifacts in a disposable home and cross-purpose replay with short-lived lab tokens plus marker-only same-object effects; never run host hooks, expose signing material, upload executable content, or overclaim cross-object access.
- Webhook/OAuth client waves are promotable when custom handler paths lose their token mapping, protocol-relative or cross-origin redirects retain bearer/signed request material, Host controls identity callback URLs, or PKCE verifiers derive from non-cryptographic PRNG state. Keep proofs to local counters, fake IdPs, two owned listeners, synthetic credentials, and deterministic test seeds; never collect real events, tokens, signatures, codes, or predict live verifiers.
- Agent context-compression advisories are promotable when task, memory, summary, or retrieved-content state is reintroduced as a fresh user-role turn and loses provenance. Prove with recorder-only compaction fixtures, inert instruction-shaped markers, role/provenance diffs, and zero tool dispatch; do not infer policy override or tool execution from transcript placement alone.
- Java cluster-channel and Kubernetes Windows-node operator advisories are promotable when unauthenticated cluster messages reach deserialization, CSR approvers accept a required organization plus an extra privileged organization, or bootstrap SSH sessions omit host-key binding. Prove with no-gadget listener counters, fake non-privileged CSR organizations, stub signers, two-key lab listeners, and synthetic bootstrap markers; never deliver gadget chains, request `system:masters`, mint administrator certificates, or collect real node credentials.
- Fission trigger-path and capability-policy follow-ups belong on the existing Fission builder page when CLI-only validation, missing CEL/Go admission checks, or incomplete capability denylists let tenant CRDs claim controller routes or preserve dangerous pod privileges. Prove with CLI/API decision matrices, disposable routes, rendered-pod diffs, and no-side-effect syscall counters; never alter shared node clocks.
- Rust HTTP proxy/parser waves are promotable on the existing Undertow methodology page when de-chunking plus forwarded `Transfer-Encoding`, nonstandard transfer codings, bare CR/LF, or header serialization creates a two-hop message split. Identity proxies that strip dash-form headers but preserve underscore aliases belong with the same hop-by-hop normalization method; prove with local raw-byte recorders and fake principals only.
- WebSphere/Liberty request-smuggling advisories belong on the existing Undertow proxy-parser page. When IBM identifies CWE-444 but not the exact differential, preserve that uncertainty, map the complete edge-to-origin chain, replay a standards-based framing corpus with four recorders, and use APAR/fix-pack builds as negative controls rather than assigning CL.TE/TE.CL or auth-bypass impact by inference.
- Apache Traffic Server and LMS/WordPress workflow waves are promotable when protocol conversion, origin coalescing, header/authority parsing, SCORM state, second-factor proofs, object IDs, cloud selectors, callback names, or payment messages cross parser, identity, object, file, execution, or order boundaries. Keep proofs to local byte recorders, owned TLS names/listeners, synthetic learners/bookings/orders, fake credentials, inert archives, and no-op callbacks; never desync shared pools, extract real training/database data, upload shells, call dangerous PHP functions, or alter customer orders.
- Application SQL consoles and tunnel/forward-auth gateways are promotable when read-only query filtering fails to bind relations to an intended collection namespace, tunnel members can preserve caller-supplied identity headers, or bearer presence substitutes for token validation. Prove with synthetic out-of-scope tables, fake principals, canary upstreams, and disposable device rows; never query credential catalogs, live user tables, production device inventories, or real administrator routes.
- Code-generator and multi-protocol file-server residual waves should update existing Kiota/codegen and goshs pages rather than create duplicates. For datamodel-code-generator, separate URL/DNS/redirect fetch, local include, source emission, and later import; for goshs, test bulk routes, SFTP auth-handler fallbacks, WebDAV MOVE semantics, and multipart final paths independently with owned callbacks and disposable marker files.
- Terraform MCP, Langflow vector namespace, and SuperPlane automation waves are promotable when streamable-HTTP sessions/credentials/authorities, FAISS namespace IDs, workflow UUIDs, or webhook titles cross user, tenant, origin, or SMTP-message boundaries. Keep proofs to fake tokens, mocked APIs, two-user vector/workflow markers, and local non-relaying SMTP recorders.
- Skill installers that dereference repository symlinks and design-token converters that accept prototype-path segments are promotable supply-chain boundaries. Prove installer disclosure only with disposable outside-source canaries; prove token pollution in fresh processes with inert prototype markers and require a separate downstream gadget before claiming higher impact.
- WordPress custom approval-post and global-settings advisories are promotable when ordinary post capabilities expose an action nonce and client-selected role to a low-role owner, or `edit_posts` authorizes site-wide payment options. Test custom-post capability mapping, nonce provenance, target-user binding, role allowlists, global option scope, and sink-level capability checks separately; prove only with synthetic users, harmless canary roles, and reversible settings.
- WordPress importer and compromised-plugin-release advisories are promotable when accepted logical file type is not bound to a server-chosen persisted suffix, or a release-specific added file connects unauthenticated request input and a static fallback credential to identity/cookie sinks. Separate validation, filename persistence, web reachability, handler selection, package provenance, loader, authentication, and callback edges; use inert files and no-op counters, and never publish embedded credentials or callback destinations.
- WordPress payment-amount and second-order SQL advisories are promotable when hidden checkout fields become gateway/verification amounts without a server-owned price lookup, or low-role metadata is later reinterpreted as SQL by a duplicate/copy action. Prove with local gateway and query recorders, explicit unit conversions, inert delimiter-shaped keys, and parser diffs; never contact live payment providers, complete underpayments, execute extraction queries, or collect database contents.
- Malicious server-rendered template advisories are promotable when exact package artifacts connect an obfuscated block to a reachable render path and browser-side DOM, outbound, or command-effect sinks. Separate artifact provenance, server render, generated message, client script execution, DOM visibility, outbound attempt, and command effect; deobfuscate offline, redact embedded authorities/commands, and prove behavior only with synthetic data plus recorder-only browser APIs.
- Build-input/identity/admin-proxy waves are promotable when untrusted Bison grammars select report executables or outside-root outputs, unsigned OAuth claims select Airflow identities, Keycloak group names or IdP permissions drift from immutable group/organization authority, authenticated Atlas users reach admin routes, or Kyuubi UI paths select outbound authorities. Keep proofs to recorder-only build sinks, disposable identity realms, no-op admin handlers, and owned listeners; never run grammar-selected shells, forge production identities, mutate real metadata, or proxy to internal services.
- swagger-typescript-api follow-ups belong on the existing Kiota/codegen page when OpenAPI paths, enum values, server URLs, or external references cross into generated source, module/constructor/method execution phases, generator fetches, or cross-origin authorization forwarding. veraPDF embedded rich-text/XFA XML is promotable as a compound-file secondary-parser boundary; keep both workflows to inert generated-source markers, fake headers, synthetic PDF text, owned callbacks, and phase-specific fixed controls.
- AgentCore import, Quarkus, Prebid, Req, Dynatrace MCP, and archive follow-ups are promotable when cloud collaborator metadata, encoded paths, bidder parameters, multipart metadata, write-tool approvals, duplicate ZIP members, or remote-session tar streams cross into generated Python, authorization routing, outbound authorities, wire grammar, persistent tenant documents, or operator-side writes. Keep proofs to AST/no-op markers, synthetic routes, owned listeners, local byte recorders, fake tenants, and disposable sibling directories; preserve Penelope's 0.19.3-versus-0.20.0 fix discrepancy.
- Logging operator, tenant storage, proot-distro, ZITADEL, netfoil, and Easy!Appointments waves are promotable when CRD strings, blob keys, archive link metadata, contact-verification secrets, blocked DNS answers, booking capabilities, provider IDs, OAuth callback state, or CalDAV URLs cross into generated configuration, outside-root paths, sibling containers, verified identity, localhost traffic, foreign objects, token writes, or outbound authorities. Keep proofs to inert config counters, disposable roots/containers, owned contact channels/listeners, and synthetic bookings; separate disclosure, mutation, callback, and final-destination edges.
- Spring LDAP, Framework WebSocket, and HATEOAS advisories are promotable when empty secrets become RFC 4513 unauthenticated binds accepted as application authentication, predictable session IDs are used as authority, or Collection+JSON/UBER reflection binding bypasses Jackson write controls. Prove with a local LDAP server, known foreign lab session IDs, synthetic setters, and fixed-version decision tables; separate server bind acceptance, ID predictability, property binding, and application authorization/impact.
- Agent confirmation, Unicode archive, and control-plane identity advisories are promotable when forged continuation events lose tool/agent/argument provenance, filesystem-equivalent ZIP names collide with symlink destinations, credential-free sessions reach command dispatch, or static appliance accounts create a chaining foothold. Prove with recorder-only tools, disposable APFS canaries, a stub command bus, and vendor/customer-supplied lab credentials; never dispatch operational commands, publish credentials, access firewall data, or infer a privilege-escalation chain from low-role login alone.
- GitLab CI/import/AI waves and MCP/package/URL/config waves are promotable when mass-assigned schedule fields select another user's CI state, approval races merge without current rules, registry redirects leak scoped credentials, import routes drift from project policy, AI retrieval/tool tokens cross project or governance scope, MCP stateless caches reuse another client's credential, client headers choose a token-bearing backend, malicious indexes choose outside-root download paths, URL parsers disagree on socket authority, or CRLF changes generated configuration grammar. Prove with disposable projects, fake tokens, owned listeners/indexes, marker-only pipelines/files, no-op tools, and offline configuration consumers.
- EV charging-controller waves are promotable when OCPP Agent, JupiCore, MQTT, Modbus, firmware-update, shutdown-firewall, or low-role configuration paths cross service authentication, backend authority, image authenticity, lifecycle exposure, or local-to-root boundaries. Keep proofs to disconnected bench controllers, fake station IDs, owned callbacks/brokers, inert firmware fixtures, no-op command recorders, and synthetic listeners; never reconfigure, reboot, flash, or execute commands on an in-service charger.
- OpenSSH client-side X11 forwarding advisories are promotable when a second local UID can pre-bind a predictable Linux abstract X socket and receive a forwarded channel. Prove with disposable users, a fake SSH server, an empty X display, and one inert recorder marker; never intercept real windows, input, clipboards, credentials, or user sessions.
- Uniswap v4 hook research is promotable as a smart-contract audit methodology when application authorization, full `PoolKey` identity, accounting, address permission bits, dependency failures, or nested callback state can fail while `PoolManager` settlement still succeeds. Prove with local disposable deployments, synthetic tokens and liquidity, independent ledgers, and marker-only balance deltas; never transact against public pools or optimize extraction.
- Kubernetes operator, edge-identity, and ACME validator waves are promotable when tenant-editable CR URLs receive privileged pull-secret/service-account/OAuth credentials, plain-HTTP routes preserve caller-supplied client-certificate headers, or challenge redirects change the final fetch destination. Prove with fake credential markers, synthetic principals, two owned listeners, and separate connection/disclosure evidence; never collect live tokens or probe internal services.
- Flyto-core follow-ups belong on the existing flyto MCP/SSRF page when per-module or per-redirect guards drift, callback/provider authorities receive service credentials, pre-dispatch interpolation bypasses denied environment modules, or caller-selected output roots defeat sandbox confinement. Prove with owned listeners, fake credentials/environment values, and disposable sibling directories only.
- MCP Ruby SDK transport waves belong with existing MCP session guidance when a known session ID is not bound to its authenticated principal or loopback Streamable HTTP accepts foreign `Host`/`Origin` requests. Prove with two synthetic principals, recorder-only tools, isolated browser/DNS fixtures, and fixed-version controls; session-ID acquisition remains a separate precondition.
- OliveTin follow-ups belong on the existing action-runner page when custom argument-type prefixes bypass Shell-mode safety or synchronous execution routes return output despite `logs:false`. Prove with no-op argument recorders and synthetic output markers; never use command-execution or secret-bearing actions.
- Spring MVC/WebFlux resource and session waves are promotable when versioned filesystem resolution escapes configured roots, shared static-resource caches cross handler authorization/location context, or in-memory session rotation races preserve an attacker-known ID after authentication. Prove with sibling canary files, two-handler cache-order matrices, owned sibling hosts, deterministic session barriers, and synthetic users only; never read real files, retrieve user resources, or race production logins.
- URL-policy and tenant-object waves are promotable when validator preprocessing changes authority before a different URL is fetched, DNS/redirect destinations drift after approval, application-layer policy and backend path normalization differ, global IDs cross tenant/channel scope, or OAuth hostname suffix checks omit DNS-label boundaries. CentreStack-style appliance clusters are promotable when static token material, encrypted account IDs, custom session delimiters, storage deserialization, XML secondary fetches, or filter-key grammar form a replayable chain; prove every edge separately with owned listeners, marker tenants, no-op side-effect recorders, and mock queries, never reusable keys, production tokens, OS users, real config files, or executable writes.
- Spring default-view and SpEL waves belong on the existing Spring Web boundary page when catch-all routes derive `redirect:`/MVC `forward:` view names or restricted evaluation still invokes arbitrary zero-argument methods. OpenRemote crosstab SQL follow-ups belong on the existing Cilium/Formie/OpenRemote page and should model stored asset display names as a second-order query-compiler input. Prove with owned redirect listeners, inert internal routes, recorder-only expression roots, synthetic assets, and parser-error/query-structure evidence; never invoke privileged methods or extract database contents.
- Rails Active Storage/libvips, degit, Samba/Zeppelin LDAP, and Superset association waves are promotable when untrusted uploads select unfuzzed native operations, repository locators reach shell command strings, LDAP attribute/filter grammar crosses trusted query authority, or child-write permission is reused for caller-selected parents. Keep proofs to instrumented operation/process sinks, synthetic directory attributes, and two-user dashboards; never read environments, derive directory credentials, execute repository-name commands, or access real dashboards.
- Spring multipart/JSP-tag/Kotlin Router follow-ups belong on the existing Spring Web page; distinguish raw-byte parser disagreement, browser attribute-context escape, and route-filter coverage with isolated connections, inert DOM markers, and recorder filters. Langflow MCP environment/build-job follow-ups belong on the existing Langflow ownership page and should use recorder children plus synthetic two-user jobs only.
- Apache Kyuubi multipart filename traversal belongs on the existing Kyuubi control-boundary page, while Kanboard alternate numeric IPv4 SSRF belongs on the existing URL-policy page. Prove with disposable sibling writes and owned isolated callback addresses; never overwrite files, target metadata, or probe production internal services.
- AWS Amplify Studio component-schema advisories belong on the existing Kiota/code-generation boundary page when binding properties, fields, collection children, or event names cross into generated JSX/identifier contexts. Preserve the partial 2.20.3 versus complete 2.20.4 fix distinction and prove only with synthetic components plus structural or recorder-only markers.
- SGLang, Tika/Leantime, and control-plane route waves are promotable when optional inference APIs, runtime model loaders, distributed-weight routes, secondary parser/import filenames, default signing authority, unattached middleware, capability composition, default database consoles, or stale sessions cross into execution, disclosure, outside-root reads, unguarded operations, or retained privilege. Keep proofs to toy models/tensors, recorder-only loaders, owned listeners, canary files, fake keys, no-op handlers, disabled interpreters, and disconnected lab sessions; never publish default credentials or load executable serialized objects.
- MeshCentral/SFTPGo/RapidRAW/Leapp and OpenClaw transcript waves are promotable when certificate-mode branches skip WebSocket origin policy, symlink-parent permissions replace dereferenced-target checks, preset file paths select UNC authorities, managed-node reports select controller-local files, or lower-trust transcripts reach privileged dashboard `innerHTML`. Keep proofs to owned browser origins, temporary SFTP roots, recorder-only file opens, synthetic controller markers, and inert DOM counters; never capture session keys, cookies, NTLM exchanges, controller credentials, or live admin actions.
- Google MCP Toolbox, package-manager, and Node permission-model waves are promotable when OAuth audience defaults, alternate tool route families, empty query-analysis results, HTTP redirects, package-name operands, or diagnostic report paths cross into protected tool dispatch, denied datasets, final request authorities, package-manager option grammar, or outside-grant writes. Keep proofs to fake tokens, no-op tools, mocked query clients, owned listeners, argv recorders, and disposable report files; never replay real tokens, query production data, install packages, probe internal services, or overwrite operational files.
- Keycloak identity-policy follow-ups are promotable when URI policy uses text prefix/suffix checks, signed introspection responses retain claims for inactive foreign-audience tokens, broker token exchange omits tenant/domain restrictions, group ancestry uses string prefixes, client revocation ignores a newer `not-before`, or wildcard callback flows admit duplicate query/fragment parameters. Keep proofs to owned domains, synthetic clients/groups/tokens, recorder service providers, and decision tables; never use real IdP accounts or retain assertions, codes, or claims.
- WordPress late-July follow-ups belong on the existing payment/device boundary page when plugin-seeded static API proofs reach upload sinks, caller-supplied OTP material replaces the enrolled subject secret, payment state is accepted without provider verification, spoofable source headers steer reset mail, predictable backups become public, stored input reaches delayed deserialization, bounded transaction proofs expose foreign receipts, or public REST routes mutate contacts/campaign revisions without permission callbacks. Keep proofs to inert upload/object recorders, disposable users/orders/contacts/campaigns, fake mail providers, and synthetic backups; never publish static credentials, create executable uploads, deliver reset mail, retrieve customer receipts, or download real site artifacts.
- August 1 WordPress follow-ups belong on the same payment/device boundary page when generic content capabilities reach delayed membership-role assignment, public form values reach configured mail attachment paths, or a renderer validates one approved script descendant but returns the whole markup container. Keep proofs to harmless custom roles, role/mail/browser recorders, sibling path canaries, and inert DOM markers; never grant privileged roles, attach or read host files, deliver email, or execute browser payloads.
- FormGent-style attachment deletion belongs on the existing WordPress payment/device boundary page when confinement depends on `realpath()` of an upload root that may not exist before the first upload. Test absent, present, file, and symlink root states with a delete recorder and disposable sibling canaries only; never delete configuration, trigger reinstallation, or claim takeover from path acceptance alone.
- August 1 WordPress identity/payment waves belong on the existing payment/device boundary page when nonces, OTPs, post-registration login, OAuth `state`, reset-link hosts, forwarding headers, role fields, order/ticket IDs, or stored attachment paths cross into another subject, privileged role, business action, delegated provider call, or filesystem sink. Prove with two-user fixtures, mocked providers, no-op session/refund/role/file recorders, and sibling canaries; never create privileged sessions, mutate real commerce objects, dispatch conversion events, or touch host files.
- Zeppelin, Kyuubi, 389 Directory Server, and RHACS waves are promotable when note/folder paths, configuration aliases, replication extended-operation fields, or OpenShift compatibility labels cross into outside-root filesystem operations, disallowed engine paths, privileged LDAP filter oracles, or policy identity loss. Keep proofs to disposable notebook/path markers, recorder-only Spark configs, synthetic directory attributes, and harmless lab deployments; never move real notebooks, enumerate directory secrets, or hide operational workloads.
- Dynatrace MCP advisories are promotable when unauthenticated HTTP transports, identifier-like tool parameters, or approved workflow metadata cross into credentialed tool dispatch, DQL grammar, or persistent runtime templates. Prove with inert tools, parser/AST recorders, synthetic events, and disconnected workflow destinations; separate authentication, approval, query-contract, persistence, and rendering evidence, and never query real observability data.
- SDK URL, Capsule, Sylius, NLTK, local-dev-server, OnionShare, workflow-import, and Wings waves are promotable when path/query values, alternate controller representations, independent payment/order IDs, specialized reader paths, foreign browser origins, symlinks/upload streams/includes/object names, or tenant templates cross into tokened requests, cluster authority, foreign objects, outside-root files, loopback responses, writes, or node configuration. Keep proofs to mocked transports, inert cluster objects, synthetic orders, owned listeners/origins, marker files, and fake node fields; never access real Vault endpoints, customer data, cluster RBAC, local secrets, or daemon credentials.
- Kubernetes admission-webhook address and ServiceAccount-selector advisories are promotable when tenant-writable annotations or objects choose an outbound authority before a controller mints or forwards credentials. Prove outbound dispatch with an owned recorder and prove token mint/forwarding separately with a fake Kubernetes client plus inert sentinel; never target metadata/internal services, mint real privileged tokens, retain bearer material, or replay credentials.
- Jodit/Thumbor/pgAdmin/automation-gateway waves are promotable when parser namespaces and reparsing, URL signatures and decoding order, route-family permissions and backend parser protocols, or caller-supplied identity headers cross into trusted DOM, files/fetches, database tools, hooks, or event dispatch. Prove with inert DOM markers, owned image hosts, sibling canary files, two-user database labs, parser/argv recorders, and no-op automation rules; never execute browser, filesystem, database, shell, credential, or operational-workflow payloads.
- NocoBase/REDAXO/Savon/LimeSurvey waves are promotable when structured low-code filters, multi-segment upload names, remote WSDL operation names, survey token arrays, or request authorities cross into SQL grammar, executable-handler filenames, generated Ruby source, or reset-link hosts. Prove with parser/query/source-generation recorders, inert media files, owned mailboxes, and redacted link-host matrices; never execute stacked SQL, upload code, evaluate generated source, collect live reset tokens, or mutate accounts.
- Jinjava/Apostrophe/sanitize-html/PyJWT/model-loader/sigstore/Hiawatha waves are promotable when template type construction, CMS script fields or patch keys, omitted URI-attribute policy, Host-derived fetches, JWKS schemes, local model artifacts, signed timestamps, or dual HTTP framing fields cross into deserialization, trusted JavaScript, process-global authorization, outbound/key authority, code/pickle loading, key-validity policy, or parser differentials. Keep proofs to recorders, synthetic CMS content/JWKS/models/keys, owned listeners, and isolated connections; never execute template/model code, read local/internal data, forge production tokens, poison shared caches, or capture user traffic.
- File-browser media helpers, agent inline-file modes, and GraphQL recovery outputs are promotable when alternate handlers skip scoped-path normalization, symlink-aware confinement differs by mode, or deprecated fields expose internal identity state despite constant public success responses. Keep proofs to sibling text canaries, patched readers/transports, synthetic users, and local mail sinks; never read host or tenant files, contact real model providers, or enumerate third-party accounts.
- AI workflow upload-to-loader chains, scheduler trigger-time authorities, and stale logout sessions are promotable when unauthenticated artifacts reach unrestricted object loaders, caller-supplied trigger addresses override stored executors, or pre-logout bearer state remains authoritative. Prove with patched deserialization recorders, owned HTTP listeners, and synthetic two-client sessions; never execute pickle gadgets, probe internal services, or capture real cookies.
- Guzzle cookie/Referer/proxy-auth, Better Auth callback/session, Traefik rewrite, OpenRemote mixed-realm, Wazuh PR-workflow, and FreeRDP drive/TLS/proxy-redirection waves are promotable when client state, origins, rewritten paths, object collections, repository metadata, shared paths, certificate identities, or redirected target addresses cross authority boundaries. Keep proofs to fake cookies/proxy credentials, disposable users/realms/routes/runners, sibling file canaries, synthetic CAs, and raw-byte recorders; never collect real sessions, CI secrets, RDP files, or proxy traffic.
- Privileged daemon PID-file symlink advisories are promotable when an attacker-replaceable path reaches a pre-privilege-drop create/truncate sink. Prove with syscall recorders and random canary files in disposable labs, test parent-component replacement separately from final-component `O_NOFOLLOW`, and never target operational files or overclaim privilege escalation from truncation alone.
- WordPress social-login and plugin file-selector waves belong on the existing payment/device boundary page when unverified IdP claims reach account lookup, or an authorized/public attachment or SVG nonce is reused for a caller-selected path. Prove with invalid-signature synthetic tokens, no-op session sinks, two-selector object matrices, and patched file readers; never mint privileged sessions or read host/tenant files.
- WordPress appointment/file/media/board waves belong on the existing payment/device page when valid request proofs or low-role access to object A are reused for object B, public routes spend stored provider authority, or verified OTP state is detached from the later account selector. Keycloak client-policy waves belong on the existing Keycloak page when omitted fields, public-to-confidential updates, request-supplied time, or user-creation group IDs bypass canonical final-state policy. Keras HDF5 external-link advisories belong on the model-parser page and should use secondary-file-open recorders plus synthetic HDF5 datasets only.
- Vikunja, ArcadeDB, Better Auth, and FreeRDP follow-ups are promotable when generic numeric principal IDs, independently selected parent/child route objects, missing MCP engine principals, cluster forwarding tokens, trigger host objects, repeated-slash route normalization, passkey IDs, or server-controlled channel lengths cross into another user's credentials/data, database/server authority, disabled routes/rate limits, authentication-factor deletion, or client memory-safety sinks. Keep proofs to disposable users/projects/databases/passkeys, fake cluster tokens, recorders, synthetic RDP channels, and sanitizer-first harnesses; never enumerate production IDs, mint privileged users, delete real factors, replay cluster tokens, or develop memory-corruption exploits.
- Transformers named-chat-template advisories belong on the model-parser page when repository-controlled `chat_template` names cross into sidecar filenames during tokenizer or processor `save_pretrained()` workflows. Prove with local synthetic metadata, write recorders, canonical-path matrices, and disposable sibling canaries only; never download unknown model repositories, overwrite existing files, or turn a file-write primitive into execution.
- Bouncy Castle certificate, CMS/OpenPGP, authenticated-decryption, LDAP, DH, and keystore waves are promotable when a successful cryptographic API result is detached from the intended certificate, canonical name, validation clock, signer count, content bytes, policy callback, tag check, filter grammar, peer value, or integrity strength. Prove with locally generated keys/objects, affected-versus-patched fixtures, fake directory or buffer recorders, and one changed binding per test; never use production certificates, mail, keystores, ciphertext, directory data, or plaintext-recovery automation.
- Bouncy Castle follow-up waves should update the existing August 3 crypto-boundary page when IES KDF splits, OpenPGP final tags, AEAD nonces/tag lengths, or RSA digest comparison detach successful verification from authenticated inputs. Keep ASN.1/MLS/PKCS#12 allocation and iteration findings as bounded parser-cost fixtures under process limits rather than standalone availability pages.
- NLTK package-integrity follow-ups belong on the existing NLTK/ML archive page when downloaded package bytes are written or extracted before checksum validation; prove ordering with an owned mirror and inert corpus, and keep this distinct from archive-member path traversal. FlowIntel-style escaped Vue interpolation and MFP image/document/cache authorization are promotable when a second interpreter or caller-selected job object survives the first trust decision; use inert DOM expressions and synthetic no-content printer jobs only.
- Report-renderer and desktop-updater advisories are promotable when rich-text resource references reach a privileged PDF fetcher, or permissive TLS plus unsigned metadata and unchecked installers form an update execution chain. Prove with owned callback/local-service/file-open recorders or isolated-VM process-start recorders; never probe production internal services, read host files, intercept real updates, or execute payloads.
- Angular SSR raw-content and transfer-cache advisories belong on the existing hydration page when server serialization reparses bound text or structured request collections collapse to ambiguous cache keys. SAML/XML signature, repository-config, SSH-channel, CMS/publish-route, and shell-backed read-ACL waves are promotable when successful verification, trusted metadata, authentication, or low-role access becomes detached from the exact object, state, or sink later consumed; keep proofs to generated identities, synthetic content, and config/callback/query/command recorders.
- PostCSS previous-map follow-ups belong on the existing PostCSS/PhpSpreadsheet/Auth.js page when confinement is conditional on `from` or another base-path option; test omitted, empty, and valid base states with synthetic `.map` canaries. Angular i18n event-attribute advisories belong on the existing hydration page and should be proven through inert translation/compiler-output recorders, not handler execution.
- ProjectDiscovery behavioral agent audits should update the agentic DAST benchmark methodology when they separate discovery, recognition, execution, sink reachability, proof provenance, alternate target exploits, fixture shortcuts, side channels, and harness interactions; retain raw trajectories and compare them to final reports rather than trusting solve rate.
- N-central/OpenEMR/Ghidra/389 Directory Server waves are promotable when incomplete auth fixes, alternate OAuth grants, client scope approval, imported data, persisted analyst-project paths, or failed binds cross into sessions, tokens, evaluators, native process launch, or retained connection identity. Keep proofs to route/state matrices, synthetic users/FHIR objects, patched SQL/eval/process/session sinks, disposable projects, and same-connection LDAP recorders.
- GNU tar hardlink and incremental-restore advisories belong on the archive-extraction page when CWD-relative targets, pre-existing symlinks, or rename TOCTOU cross the extraction root. Use disposable filesystems, deterministic barriers, and denied final syscalls; XML::Sig all-skipped verification follow-ups belong on the existing SAML boundary page and must record verified-count separately from enumerated signatures.
- Undici shared-cache follow-ups are promotable when malformed qualified `private` directives cross typed parsing into shared storage and later cross-user delivery; prove with two synthetic callers, body/header canaries, cache instrumentation, and no real credentials. fast-uri backslash-authority advisories belong on the URL-canonicalization page and require a policy-parser versus final-transport destination recorder.
- GitPython filesystem-option, `ip-address` literal-classification, and undici serializer/retry/cookie waves are promotable when wrapper kwargs, ambiguous address forms, body metadata, retry state, or structured cookie fields cross into file reads/writes, final network destinations, raw HTTP framing, or extra cookie attributes. Use argv/file/transport/raw-byte/parser recorders with disposable canaries; never touch host files, internal services, real sessions, or shared HTTP connections.
- Guzzle noncanonical-host/cookie-domain, aiohttp WebSocket-upgrade, Sequelize Oracle escaping, Black cache-path, GitPython `Commit.count()`, and Python `cryptography` PKCS#7 waves are promotable when policy and transport authorities diverge, upgrade parsers disagree on message slots, string prefixes suppress SQL escaping, formatter/Git options select filesystem sinks, or decryption failures expose an adaptive oracle. Use owned destinations, raw-byte/parser/SQL/file recorders, generated keys/envelopes, synthetic rows, and disposable paths; skip adjacent availability-only parser, compression, CORS ReDoS, and numeric-length items.
- Python `cryptography` wildcard name-constraint advisories are promotable when a constrained intermediate can issue a wildcard whose match set escapes the permitted subtree; prove with generated chains, exact reference identities, affected-versus-fixed verifiers, and no network connection. Keep duplicate-certificate path-building amplification as a bounded subprocess cost fixture, not an authentication-bypass claim.
- Shlink/NiFi/DuckDB-AWS/AI-corpus-command-wrapper/Omada waves are promotable when redirecting title fetches, sort directions, exported telemetry, parent/child IDs, proposed parameter values, function-level redaction options, corpus selectors, unverified AI-provider TLS, structured command fields, or adoption transcripts cross into internal fetches, query/spreadsheet grammar, foreign assets/components, fake cloud credentials, foreign corpora/tool calls, shells, or the wrong provisioning peer. Prove with owned endpoints, query/export/process/tool recorders, synthetic contexts/corpora/credentials, and disconnected reset devices; never query production data, evaluate formulas, capture real tokens, run commands, recover keys/credentials, or race operational adoption.
- Sensor-proxy/Camaleon/Microweber follow-ups are promotable when operator-selected controller hosts reach elevated install/process sinks, specialized draft controllers drop parent authorization and object scope, or method/regex/normalization stages preserve tag markup for later public/admin HTML reparsing. Prove with owned inert controller responses, patched privileged sinks, two-user draft matrices, and detached non-executable DOM recorders; never redirect operational sensors, alter production drafts, execute JavaScript, or collect sessions/CSRF tokens. Adjacent GL.iNet RPC-wrapper command-injection records should update the existing command-wrapper page and stop at a patched process recorder.
- Node.js hidden-header forwarding desync, stale SQLite iterators, WildFly domain repository traversal, Rocket.Chat custom-sound LFI, and Zyxel WLAN portal/export advisories are promotable when parser-visible headers diverge from runtime framing, cached statement rebinding outlives iterator scope, trusted slaves select controller files, filesystem-backed public routes escape storage roots, or portal/admin CGI state crosses network/command boundaries. Prove with isolated parser traces, synthetic tenant rows, patched file/process readers, disposable domain/storage roots, and owned canary WLANs; never desynchronize shared traffic, read host files, bypass production portals, or conflate portal admission with appliance administration.
- LINE mobile profile templates and Zyxel ZLD configuration CLI advisories are promotable when remote profile content reaches an application-privileged script runtime or an administrator-supplied path selects a configuration file outside the approved root. Prove with patched evaluator/bridge and canonical-path/config-parser recorders, disposable accounts/appliances, inert markers, and no script execution, client-data access, outside-file reads, or configuration actions.
- Privileged desktop IPC, static firmware TLS keys, and WordPress provider/object waves are promotable when a user-readable shared key reaches a `SYSTEM` file service, firmware-shipped private material repeats device identity, public routes spend stored cloud authority, or one token/object selector authorizes another sink object. Prove with patched IPC/file/provider readers, public-key fingerprints, benign upload-handler mappings, and two-user marker objects; never perform outside-root file operations, use private keys, access live provider data/logs, upload executable content, or alter real subscriptions/RSVPs.
- Flowise low-code boundary waves are promotable on the existing August 4 page when feature-gated file routes omit operation/workspace authorization, structured node options replace SQLite paths or reach TypeORM module selectors, public prediction overrides replace trusted session/history context, mapped addresses split SSRF policy from transport, CSV data or parser selectors reach generated Python/pickle sinks, caller options replace sandbox security defaults, URL fields cross into generated JavaScript, public refresh routes spend stored OAuth credentials, or customer IDs select foreign provider objects. Prove with two-workspace/session fixtures, canonical-path/network recorders, parser/AST/deserializer/module sinks, fake OAuth fields on owned listeners, and mocked provider objects; never delete files, deserialize unknown data, load code, target internal services, capture secrets, or enumerate customer IDs.
- Flowise follow-ups belong on the same August 4 page when a shared CRUD route accepts either flow-type permission without binding it to the resolved object's type, or when child-process environment recreates behavior blocked only in CLI arguments. Use no-op delete/package/process recorders, synthetic flows, owned empty indexes, and affected-versus-fixed launch matrices; never delete flows, install packages, or execute commands.
- Flowise OAuth/credential/history/S3/Pyodide follow-ups belong on the same August 4 page when alternate OAuth phases lose workspace scope, provider tools spend caller-selected credentials, schema field types drive secret redaction, history or runtime variables bypass tenant/permission scope, remote object keys become local paths, or Unicode normalization changes code after validation. Use fake credentials/providers, first-foreign-row canaries, denied filesystem sinks, and parser-only normalization differentials; never collect real secrets/history, write files, or execute interpreted code.
- Notebook/data-server/OPC-UA/proxy waves are promotable when notebook metadata redirects an environment-credentialed client, raw paths or remote expressions reach file/evaluator authority, mixed batches or copied role maps detach authorization from dispatched OPC UA objects, or alternate IP spellings split proxy policy from final peer selection. Keep proofs to fake API keys, temporary asset roots, patched evaluators/readers, synthetic OPC UA methods/sessions, coarse oracle differentials, and isolated canary peers; never open unknown notebooks with live credentials, read host files, execute expressions, recover passwords, invoke operational methods, or reach real internal services.
- Open WebUI follow-ups belong on the existing RAG, auth/session, and model/channel pages when NAT64 encodings, browser subresources, Vega resources, foreign-client OAuth tokens, terminal file previews, alternate chat feature flags, or channel write grants cross into internal fetches, browser-origin access, sessions, provider dispatch, or another author's messages. Use owned NAT64/browser/provider fixtures, mock OIDC clients, inert DOM markers, and no-op message sinks; skip generation-cancellation-only availability records.
- Open WebUI DNS-rebinding, inline-model knowledge, cleanup-parent, terminal WebSocket role, shared-folder cascade, tool-source projection, and KaTeX fallback follow-ups belong on those same pages; prove with owned DNS peers, two-user synthetic objects, no-op delete/session/read sinks, source-presence hashes, and inert DOM parsing only. Ghost mapped-address, upload-type, import-render, and archived-offer waves are promotable together when proofs stop at owned listeners, inert same-origin parser markers, synthetic imported posts, and zero-value provider stubs.
- Bedrock AgentCore crafted content blocks, A2UI agent-driven browser actions, and OpenMeter stored attribution fields are promotable as structured-data-to-interpreter boundaries when proofs stop at inert tool, `window.open`, and SQL compiler recorders. Label unreviewed GitHub/NVD mirrors as validation seeds, confirm primary records, and do not invent package ranges or fixed versions.
- Ghost follow-up waves belong on the August 4 Ghost page when feature-specific fetchers, DNS rebinding, theme/backup paths, federated content, Admin sessions, staff field projection, magic-link responses, or donation checkout metadata cross into internal requests, outside-root writes, active DOM, fixed authenticated sessions, password-derived fields, account oracles, or stronger gift entitlements. Use owned DNS/ActivityPub/provider fixtures, patched filesystem/session/API sinks, synthetic identities, redacted field-presence evidence, and no real payments, hashes, sessions, files, or internal targets.
- Odysseus-style embedding endpoint advisories are promotable when authentication-only routes let non-admin users replace a server-wide model backend and turn its health check or later embedding calls into SSRF or cross-user data relay. Prove with disposable two-user labs, hashed synthetic text, owned no-content embedding recorders, and patched connectors; preserve deployment-specific private/LAN endpoint policy and never probe metadata/internal services or relay real prompts, RAG documents, memories, or vault text.
- CPython archive advisories belong on the existing archive-extraction page when hardlink fallback relocates a validated symlink, Windows drive-qualified ZIP names escape the selected root, or hardlink processing drops the requested metadata filter. Prove with final-location/path and metadata-sink recorders, disposable sibling canaries, affected-versus-fixed builds, and denied final syscalls; distinguish path escape from ownership-policy drift.
- OpenStack Swift/Neutron and WordPress authority waves are promotable when unsigned semantic headers survive signed-request validation, compatibility headers inherit the wrong principal, copied routes omit target ownership, or public/subscriber nonces reach reset-link, export, configuration, or role-assignment sinks without an independent capability check. Prove with synthetic cross-tenant objects, canonical-request and downstream-principal traces, two-user target matrices, local mail sinks, fake secret hashes, and no-op copy/mutation/user-creation recorders; never retrieve foreign objects, change routing/passwords, export live secrets, or create privileged accounts.
- Encoded-route, trusted-context, Kubernetes-controller, stale-JWT, tenant-object, and agent/process-wrapper waves are promotable when an early policy sees different path/identity/authority data than a later router, reconciler, resolver, file sink, or shell. Use raw-to-canonical path traces, authenticated-versus-body claim matrices, patched token/apply/network sinks, two-tenant markers, denied file syscalls, and argv recorders; never read host files, mint privileged tokens, apply cluster resources, query metadata, retrieve foreign data, or execute commands.
- Developer-workspace/outbound-fetch/object/interpreter waves are promotable when tokenless HTTP routes, decoded plugin paths, repository preferences, URL validators, stored credential tests, generic route proofs, scripts, process fields, SQL identifiers, or post-sanitization transforms cross into filesystem, final-peer, credential, tenant-object, interpreter, shell, query, or DOM authority. Use disposable roots, owned DNS/HTTP/IMAP peers, fake credentials, two-tenant markers, and denied argv/SQL/DOM/file sinks; never read/write host files, query metadata/internal services, collect credentials, retrieve foreign records, or execute scripts, commands, SQL, or active HTML.
- Template/signing-key/upload/alternate-route waves are promotable when data fields become server-side template grammar, clean deployments share signing authority, privileged upload fields select executable types or outside-root destinations, sibling mutation handlers omit authorization, or content changes parser context. Use patched compilers/loaders/mutation/file sinks, offline fake-principal verifiers, and detached script-disabled DOMs; never execute templates, forge real artifacts, upload code, mutate records, or run active HTML.
- Electron renderer/session/protocol waves are promotable when page-world objects, frame origins, scheme privileges, redirect targets, protocol response sessions, extension targets, path strings, parent signatures, or GPU geometry cross into preload, device, local-loader, cache, tab, platform-open, startup, or shared-memory authority. Use disposable Electron fixtures, owned origins/redirectors, paired session markers, fake permissions, patched sinks, ad-hoc signatures, and metadata-only GPU harnesses; never read host files, open devices, load unknown extensions, inherit real TCC/keychain authority, execute renderer/Node/shell code, or extract process memory.
- Electron child-window follow-ups belong on the same August 5 page when untrusted `window.open()` feature strings reach non-allowlisted `BrowserWindow` options with main-process file or network authority. Prove with raw-to-parsed option traces and patched constructors/resource recorders; do not open files, contact unowned URLs, or infer Node.js access, disclosure, or SSRF from option injection alone.
- Electron iframe-sandbox, DevTools, and context-bridge follow-ups belong on the same August 5 page when OpenURL/external-protocol paths lose frame sandbox state, dock/reveal parameters reach frontend or platform-shell authority, or copied object prototypes influence preload decisions. Prove with patched popup/platform-open/evaluator/object-copy recorders, owned origins, inert files, and affected-versus-fixed matrices; keep popup, application launch, DevTools script control, native execution, prototype callbacks, and host-capability access as separate claims.
- TeamCity agent-polling KEVs are promotable when unauthenticated protocol traffic reaches object decoding before agent identity is established. Derive routes from disposable-agent traffic, owner logs, proxy config, or exact-branch source rather than guessed exploit paths; prove only with a patched inert deserializer on affected-versus-fixed isolated labs, and never send gadget chains to operational CI servers.
- ProjectDiscovery offensive-agent behavioral audits belong on the existing agentic DAST benchmark page: score discovery, recognition, execution, sink reachability, proof, and reporting separately; distinguish valid alternate target paths from fixture shortcuts, side channels, and harness interaction; retain trajectory/provenance evidence instead of trusting solve rate or the final agent narrative.
- Jenkins controller/plugin waves are promotable when weak global/item permissions, workspace paths, stored credential selectors, CSRF, Groovy, or remoting types cross into file, connector, compiler, or object-reconstruction authority. MarkLogic waves are promotable when proxy framing, ODBC/SAML identity proof, REST/query roles, or URL policy diverge from the final backend decision. Prove with disposable controllers/agents, synthetic workspaces/credentials/users/documents, raw-byte traces, and patched file/credential/script/deserializer/query/session sinks; never read operational workspaces, capture credentials, run Groovy/gadgets, desynchronize shared traffic, or issue production privileged queries.
- PortSwigger HTTP Terminator research is promotable as a durable HTTP desync campaign methodology: separate micro-inspired hypothesis generation, deterministic cross-request evaluation, weaponization, and human-reviewed cascades; preserve clean-request and pipelining controls, parser traces, synthetic victim markers, and affected-versus-fixed evidence. Never use live users, queued credentials, high-volume shared-target probing, or an agent's own confidence as proof. rclone translated-link, `serve restic`, and metadata waves belong on the existing rclone boundary page and must use patched symlink/path/file/ownership/mode recorders rather than outside-root writes, deletes, privileged files, or execution.
- Nuxt route-rule and runtime payload-cache advisories are promotable when router/key normalization or path-only SSR payload caches detach current-request authorization from the page or `_payload.json` response. Prove with mixed-case routes, two-user synthetic markers, middleware/cache traces, and affected-versus-fixed versions; never retrieve real SSR profile, tenant, billing, or token data.
- rclone encoding/archive/private-repository/serve-S3/protocol/redirect follow-ups belong on the existing rclone boundary page. Record post-decoding canonical paths, authenticated-user-to-prefix bindings, parser frames, and redirect authority tuples with denied sinks and fake credentials; never write outside disposable roots, access backups, execute PowerShell, send mutating FTP commands, or expose reusable tokens.
- Traefik CRD/Gateway API advisories are promotable when HTTP/TCP provider allowlists or `ReferenceGrant` namespace provenance diverge from the final transport/middleware resolver. Use disposable namespaces, fake transport identities, inert headers, and patched configuration/dial sinks; never reference production Secrets, certificates, routes, or services.
- Traefik rewrite/auth/cache follow-ups belong on the existing route/transport/namespace page when post-routing path transforms, header aliases, forwarded-fact derivation, shared-backend filter keys, cross-namespace backend kinds, route identities, or BasicAuth singleflight keys detach the final decision from the original route or principal tuple. Use raw-to-final traces, inert headers, patched dispatch/auth/config sinks, and collision fixtures; never access protected routes, real identities, tenant backends, Secrets, or usable password hashes.
- Nuxt server-island and DevTools follow-ups belong on the existing August 5 Nuxt page when forwarded props reach dynamic component/template authority or unauthenticated HMR RPC reaches editor process launch. Prove with patched component/compiler/spawn recorders and inert markers; never compile or execute attacker content or expose a real developer workspace.
- Langflow validation, component-digest, generated-code, knowledge-base, memory-session, and build-cache follow-ups belong on the existing Langflow workflow page. Gitea migration/OAuth fetch follow-ups belong on the existing Gitea boundary page; prove final-peer and persistence edges with owned peers, fake data, and denied sinks only.
- PortSwigger CRLF-powered desync follow-ups belong on the existing HTTP desync campaign page. Map raw input through normalization into upstream bytes, separate header injection, request splitting, injected framing, response assignment, browser serialization, and connection scope, and use only synthetic parser/status/body markers. Never demonstrate with live queues, sessions, cookies, account actions, cache mutations, high-rate browser loops, or shared users.
- Quay repository-mirror and VuFind access-control advisories are promotable when sibling mirror handlers apply different final-peer URL policy or a denial response fails to terminate controller dispatch. Prove with owned no-content registry peers, queue/connector recorders, synthetic VuFind objects, patched action/sink recorders, and affected-versus-fixed control-flow traces; never probe internal/metadata services or access, mutate, export, or message real records.
- Agent control-plane and tool-policy waves are promotable when operating modes skip route authentication, providers re-inject explicitly denied tools, approval classifiers parse less syntax than the final shell, file-send/manual tools accept outside-root paths, or URL checks warn without blocking the connector. Use synthetic management objects, final tool-surface diffs, denied process/file sinks, temporary canary roots, and owned no-content peers; never read host files, persist conversations, execute commands, or probe internal services.
- Remote artifact identifiers such as digests, hashes, keys, or object IDs are promotable when a server-supplied value becomes a client filename without full schema validation or final-path confinement. Establish malicious-server or impersonation preconditions, use owned mock peers and denied write sinks, and never create outside-root files or infer execution from destination control alone.
- Agent child/reviewer tool surfaces, alternate command handlers, passive artifact loaders, and AI attachment fetchers are promotable when construction-time allowlists are widened at runtime, guarded shell semantics are skipped by a sibling route, message-derived paths reach privileged file IPC, or fetched document markers reach model context. Use fake tools, denied spawn/file sinks, owned no-content peers, and patched context builders; never execute commands, read host files, probe private services, or feed retrieved content to a live model.
- Agent message/configuration, child-agent, wrapper-approval, sender-identity, and workspace-write follow-ups belong on the August 6 agent authority page when unauthenticated routes select pre-approved providers, paths enter prompts or attachments, children inherit stronger authority, wrapper paths outlive approved payloads, mutable chat fields satisfy elevated allowlists, or dangling symlinks escape final writes. Polaris/WSO2 authority waves are promotable when logical catalog or token permission reaches broader storage credentials, JWT algorithms, or Admin REST routes. Keep proofs to synthetic objects/users, patched provider/file/process/API sinks, and no live model, messaging, object-store, or identity data.
- Apache CXF OAuth/OIDC, WSDL-import, and JMS waves are promotable when signed/encrypted protocol objects outlive one-time, revocation, claim-binding, or scope decisions; imported XML loses top-level parser hardening; or broker write access reaches native object reconstruction. Use disposable clients/issuers/brokers, owned XML callbacks, inert objects, and patched token/parser/deserializer sinks; never replay real codes/tokens, read files through entities, or enqueue gadget chains.
- Proxied CONNECT, developer-dashboard route, object-lifecycle, and no-follow-helper waves are promotable when H2/H3 body semantics poison a shared H1 pool, absent default credentials expose process/file routes, parent visibility or caller URLs substitute for child/owner policy, or a safe descriptor is later consumed by a link-following syscall. Use synthetic cross-client markers, denied argv/file/metadata sinks, two-user objects, and disposable symlinks; never poison shared queues, execute commands, read host files, retrieve foreign content, or mutate file metadata.
- SQL-backed namespace, serializer/WebSocket object, import-URL, and HTML-sanitizer waves are promotable when flattened prefixes lose segment boundaries, nested IDs bypass request-scoped querysets, room joins omit object visibility, import URLs reach an unchecked final peer, or surviving elements/attributes/CSS become active only in the host browser context. Use two-tenant markers, patched mutation/group/network/DOM sinks, owned peers, and script-disabled fixtures; never retrieve foreign data, run jobs, probe internal services, submit forms, or claim XSS without an executable sink.
- Build-cache, CLI-wrapper, CMS, and renderer-state waves are promotable when remote artifacts, generated SSH argv, canonicalized child paths, crawler defaults, shared JavaScript state, sibling CSS selectors, OAuth claims, alternate content routes, upload handlers, or template values cross into file, peer, object, identity, DOM, or method authority. Use denied file/process/mutation sinks, fake credentials, owned peers, two-user fixtures, and script-disabled DOMs; never overwrite host files, intercept real SSH, collect credentials, read foreign content, execute uploads, delete data, or claim XSS/RCE beyond the final synthetic sink.
- Embedded-renderer, sanitizer, URL-normalization, and CMS post-transform waves belong on the August 6 build/CMS/render page when vendored parser versions evade manifest inventory, parse/serialize/reparse states diverge, browser URL normalization outlives a raw-string check, or generic model binding, decode/merge, class hierarchy, and sibling routes widen authority. Use inert document/DOM/configuration markers and patched script, process, password, persistence, and method sinks; never execute document scripts/templates/commands, change usable passwords, or run active content in a privileged origin.
- CSS sanitizer and webmail research is promotable as a first-class methodology when parser/CSSOM mutation, host-DOM associations, application-added CSS gadgets, image-proxy final-peer decisions, clipboard ingestion, or human-versus-model representations cross the message boundary. Use harmless style/DOM markers, owned no-content peers, fake model contexts, and patched UI/resource/navigation sinks; never build credential/token extractors, keyloggers, realistic login overlays, or live-agent prompt-injection proofs.
- Craft preview/interpolation/class-factory/draft-label and repository blame-wrapper follow-ups belong on the August 6 build/CMS/render page. Prove with two-user synthetic assets, patched secret/class/file/process sinks, detached DOMs, and inert metacharacter filenames; never read secrets/files, enumerate blind values, execute commands, or perform privileged browser actions.
- h2 duplicate-`Host` advisories belong on the HTTP desync campaign page when repeated HTTP/2 authority fields survive into an HTTP/1.1 bridge. Capture field order, route choice, emitted raw bytes, origin decision, and affected-versus-fixed behavior; do not claim smuggling without deterministic synthetic cross-request response reassignment.
- Flowise prefix-whitelist, Assistants credential, and document-store follow-ups belong on the existing August 4 Flowise page when public-route prefix matching admits longer credential-bearing paths, caller UUIDs select another workspace's provider authority, or view-only roles reach ingestion/refresh routes. Use exact-route grammar matrices, two-workspace fake credentials/stores, mocked providers, and no-op ingestion sinks; never enumerate credential IDs, contact real providers, ingest retained documents, mutate vector stores, or generate billable embeddings.
- Flowise SSRF deny-list and redirect follow-ups belong on the existing August 4 Flowise page when provider-specific metadata ranges are omitted or only the initial URL is authorized. Use isolated namespaces, owned redirectors, synthetic destination classes, and denied connector recorders; never contact real metadata or internal services.
- WordPress integration/commerce/identity waves belong on the existing July 28 payment/device page when unauthenticated handlers replace cloud accounts, request quantities determine totals, social-login callbacks lose browser binding, alternate REST routes bypass content gates, token/SAML algorithms come from attacker input, payment-completion routes trust client state, or reset routes lose subject binding. Prove with fake integrations, synthetic objects, mocked gateways/IdPs, and patched session/password/file/query/deserialization sinks; never import attacker templates, retrieve customer data, execute POP chains, include host files, create paid orders, or mint sessions.
- NLTK cross-package downloader collisions belong on the July 31 library/tenant/commerce page; distinguish inside-root shared-namespace overwrite from archive traversal and prove persistence with two synthetic packages plus a fresh-interpreter lookup. Phoca Commander path records belong on the July 8 Joomla extension page and must cover read, upload, copy, move, and delete with denied final sinks. WordPress payment-notification/token and CAPTCHA-cache records belong on the July 28 payment/device page; prove server-owned merchant/amount/object binding and single-use proof consumption with mocked providers only.
- Smarty, passkey, and webmail authority advisories are promotable when template resource wrappers or symlinks change final file identity, client-supplied WebAuthn request options plus stale credential counters make assertions replayable, or path-like mail fields reach alternate streams, UNC peers, archive/file operations, and response headers. Use patched file/session/network/header sinks, fake authenticators, synthetic mail users, and owned no-content peers; never read host files, replay real assertions, capture or relay NTLM material, or mutate operational mail data.
- GitPython 3.1.58, Postiz, SuperTokens, libvirt, OPeNDAP, DOMPurify, Joomla, and WordPress follow-ups are promotable when wrapper checks diverge from final argv/config/path authority, late route decoding escapes media roots, tenant selectors detach from session identity, XML values become generated service grammar, redirects preserve denied peers or credentials, detached sanitizer subtrees retain queued events, or commerce/shortcode/content proofs lose object binding. Use disposable repos/files/tenants/configs/orders, fake headers/proofs, and denied process/file/network/DOM/state sinks; never execute Git helpers/hooks, read secrets, access foreign tenants, launch generated services, forward real credentials, or complete real commerce actions.
- Astro Vercel ISR, API Platform relation IRI, go-git reference/worktree, Craft category structure, and Nuxt DevTools metadata follow-ups are promotable when public route selectors bypass edge-only policy, wrong resource classes cross writable relations, peer refs or symlinks escape repository roots, read-derived session grants authorize writes, or header-derived localness disagrees with the TCP peer. Use synthetic routes/classes/refs/trees/paths, patched render/assignment/file/mutation sinks, and loopback-vs-LAN decision tables; never retrieve protected pages, persist wrong-typed objects, write repository metadata, mutate taxonomy, or turn workspace metadata into file/RPC access.
- Package-manager source arguments belong on the August 6 build/CMS/render page when wrappers splice package specifiers into shell-backed preflight or install commands. Trace every subprocess, including existence checks, with inert grammar markers and denied process sinks; never execute payloads, contact unowned registries, or expose registry credentials.
- Keras archive and `DiskIOStore` follow-ups belong on the existing model-parser page when archive members are checked against CWD instead of the extraction destination or layer names become filesystem components. Django cache/signed-cookie follow-ups belong on the May 8 Django page when header grammar, cookie presence, or non-injective `(name, salt)` derivation detaches cache/signature authority from a principal or purpose. Use disposable roots/caches, fake principals, application-produced signatures, and denied file/session/header sinks; never write outside temp roots, prime production caches, expose signing keys, or claim header injection without a downstream serializer.
- Keras `TorchModuleWrapper`, Lambda safe-mode, HDF5 VDS, and TAR-symlink follow-ups belong on the model-parser page; compare every public deserializer and ambient-policy state with denied reconstruction/file sinks. Showdown metadata-title and generated table-ID records belong on the Markdown/render page; prove parse/serialize/browser context transitions with detached-DOM markers only.
- Consul core file/session/listener waves are promotable when Connect CA auth configuration selects server credential files, transaction operations omit dedicated-route ACLs, or custom Envoy listeners normalize paths differently from L7 intentions. Nanobot follow-ups belong on existing agent policy/fetch pages when shell allow-lists parse less grammar than the sink, login shells reconstruct ambient environment, MCP resources/prompts evade `enabledTools`, or provider-returned image URLs bypass shared egress checks. Grav webhook and TestLink attachment follow-ups belong on the existing CMS permission page. Keep proofs to mock Vault peers, lab sessions/services, temporary homes, fake MCP/providers, inert jobs, and two-project marker attachments.
- Windows client and appliance-WebUI waves are promotable when low-privilege inputs reach privileged command construction, non-default installation roots inherit user-writable ACLs before a SYSTEM consumer, or client-controlled `Host` values reach navigation, script, or cache identity. Prove with affected/fixed disposable VMs, read-only ACL-to-consumer joins, denied process/file sinks, owned hostnames, private caches, and script-disabled DOM markers; never replace product binaries, execute elevation payloads, disrupt live VPNs, poison shared caches, or test operational firewall management planes.
- Calico policy/debug waves are promotable when HTTP prefix checks disagree with downstream path normalization, `DeleteCollection` skips per-tier authorization, or opt-in pprof listeners bind beyond loopback. Use inert routes, two synthetic tiers, denied mutation sinks, and header-only debug checks; never bulk-delete policies or collect heap, goroutine, token, or production workload data. OpenAPI/MCP wrapper follow-ups belong on existing build/agent pages when untrusted metadata or tool fields cross into install or process authority; require denied final process traces.
- MCP wrapper and WordPress plugin waves belong on existing agent-policy and July 28 WordPress authority pages when structured command/date arguments reach shell-backed wrappers, journey IDs become filenames, local file selectors feed AI-provider bodies, guest cookies detach from upload ownership, loosely typed API keys authorize privileged actions, public nonces authorize workflow nodes, or client prices become canonical bookings. Use denied process/file/account/query/delete/workflow sinks, fake provider/API material, inert deserialization classes, and local commerce adapters; never execute commands, read or relay files, create accounts, grant paid downloads, delete uploads, send mail, or complete bookings.
- MCP project selectors and server-rendered client-template follow-ups belong on the August 6 agent authority and August 3 CTI-Transmute pages when an identifier selects an outside-root recursive scan or escaped text is reparsed by a Vue-style runtime compiler. Prove with disposable React trees, denied enumeration/read/compiler sinks, mounted-vs-unmounted controls, and inert delimiter markers; never read source files or execute JavaScript in a privileged origin.
- Agent session/workspace/memory/callback/delete path follow-ups belong on the August 6 agent authority page when identifiers or caller-selected roots reach file lifecycle operations. Build operation matrices across read, append/create, replace, and delete with disposable roots and denied final sinks; never open, mutate, or remove sibling canaries, and do not infer remote MCP reachability from local source records.
- MCP path/process/fetch and WordPress route/management/query/cart follow-ups belong on the existing August 6 agent authority and July 28 WordPress pages. Trace generated filenames across lifecycle sinks, structured fields to final argv, URL policy through redirects/final peers, framework decoding to canonical static paths, alternate REST routes to both serializers and side effects, remote-controller proofs to one installation/network, column selectors to generated SQL, and prior-order IDs to the active cart. Use denied sinks, owned no-content peers, synthetic tables/orders, and fake keys only.
- MCP webhook URLs, resource URIs, and generated output names belong on the August 6 agent authority page. Trace webhook verification/delivery/retry, resource scheme dispatch and response relay, and generated filenames through final network/file sinks; distinguish destination control from response disclosure and use owned no-content peers plus disposable canary roots only.
- Apache Ranger control-plane waves are promotable when JDBC URLs, plugin classes, script fields, download routes, URL role selectors, log tokens, or client TLS settings cross into runtime connectors, class/script engines, protected exports, privileges, replayable credentials, or mismatched peers. Use mocked JDBC/TLS peers, fake policies/tokens, and denied loader/script/query/file/session sinks; never connect to production databases, retrieve retained exports, execute scripts/classes, or collect real logs. GNU cpio hard-link follow-ups belong on the archive-extraction page when `--no-absolute-filenames` normalizes member names but not link targets; prove with a sibling canary and denied `link`/`linkat`.
- Node.js Unicode-dot TLS follow-ups are promotable when certificate wildcard/label-depth parsing diverges from resolver/SNI/connector hostname normalization. OpenSign/Bitwarden/Attendize/CTI history follow-ups are promotable when document capability signing, organization/event selection, or missing parent objects detach final read/write/mutation authority from the session principal. Use local TLS peers, synthetic objects, and denied signer/serializer/mutation sinks; never retrieve files, mint tokens, alter workflows, or forge retained records. CTI graph-renderer follow-ups belong on the August 3 report-renderer page, and OpenCart extension ZIP traversal belongs on the archive-extraction page.
- MCP range-config, provider-file, attachment-fetch, and generated-image follow-ups belong on the August 6 agent authority page. Trace separate read/write roots, local file bytes into provider bodies, fetched bytes into downstream attachments, and caller-selected output directories through denied file/network/provider sinks; never transmit canary bytes or create live attachments.
- WordPress authentication/reset/fetch/file/booking/commerce follow-ups belong on the July 28 payment/device page when blocked users retain alternate credentials, global REST filters match attacker-shaped URI substrings, scheduled routes mint sessions, reset proofs or budgets use caller-selected keys, public fetchers relay final responses, empty upload policies admit executable-looking names, file commands omit per-operation scope, or booking/payment callbacks lose owner, browser, merchant, amount, and object binding. Use synthetic users/files/bookings, owned no-content peers, mocked providers, and denied session/password/network/file/payment sinks; never mint sessions, read or delete files, target internal services, or complete bookings/payments.
- Archive-builder source symlink and Jenkins extraction target-symlink follow-ups belong on the archive-extraction page; distinguish outside-root reads during archive creation from persistent links and later dereference during extraction. Multicluster-global-hub follow-ups belong on the May 7 cluster control-plane page when Kafka certificate identity diverges from a self-asserted CloudEvent source or shared migration topics expose bootstrap material across hubs. Use sibling canaries, denied file/database/topic sinks, fake nonfunctional kubeconfigs, and two-hub labs only.
- Feast/OpenShift AI feature-store and operator waves are promotable when default-no-auth services, serialized UDFs, feature repositories, gateway identity headers, Secret/ServiceAccount selectors, DSN options, or alternate workflow APIs cross into deserialization, controller identity, token, file, database, or pod authority. Use two-namespace marker-only clusters with denied deserializer/Secret/token/connector/pod sinks; never execute repository/UDF code, mint tokens, read Secrets, enable live local-file transfer, create privileged pods, or intercept model traffic.
- Airflow multi-team secrets-backend fallbacks belong on the June 30 AI/Airflow page when a scoped miss permits a team-qualified ID to reach a team-agnostic provider lookup. Flowise private-file download follow-ups belong on the August 4 Flowise page; Accelerate `weight_map` secondary-file paths belong on the model-parser page; Python `unearth` traversal belongs on the archive-extraction page. Use fake provider/storage clients and denied file sinks only.
- TP-Link Aginet appliance waves are promotable when ISP-customized web routes, low-role operations, embedded firmware keys, USB HTTPS symlinks, or web fields cross into privileged handlers, administrator mutations, configuration cryptography, device files, or system commands. Use exact model/firmware evidence, synthetic accounts/configuration, disposable USB images, and denied mutation/file/process sinks; never test ISP infrastructure, decrypt retained backups, read device secrets, create privileged accounts, or execute commands.
- SAP Approuter waves are promotable when request headers, session integrity fields, cross-site login state, backend routing, callback certificates, tenant selectors, token content, or WebSocket messages cross into session, component, tenant, credential, or backend authority. Pair adjacent BusinessObjects spreadsheet external-reference and MII alternate-route records with that edge workflow; use disposable users/tenants, private test certificates, fake credentials, synthetic files/business objects, owned no-content peers, and denied network/file/operation sinks only.
- Crafty Controller, Velociraptor, Axis, mcp-grafana, and OpenShift AI dashboard follow-ups are promotable when import/upload names, VQL connector plugins, service-account roles, ACAP package identity, override headers, or internal listener tokens cross into file, network, device-runtime, package-install, or Kubernetes API authority. Use disposable roots, owned no-content peers, inert packages, fake tokens/credentials, and denied file/network/process/identity sinks; never install executable ACAP content, transfer evidence, target internal services, write application files, or invoke live cluster/device operations.
- CTI-Transmute follow-ups belong on the August 3 report-renderer page when MISP URLs, alternate table/tag/chart/graph renderers, report exports, or reaction IDs cross final-peer, HTML, serializer, or mutation boundaries. EAP/WildFly ORB/IIOP/PicketLink/AJP follow-ups belong on the August 4 WildFly page; prove only with synthetic identities, fake ORB/JNDI services, locally generated certificates, and denied class/bind/session sinks. MRTG PID-file symlink follow-ups belong on the August 1 privileged PID-file page and must stop at a denied ownership syscall against a sibling canary.
- OpenSign contact/tenant/audit, Attendize attendee/order, CTI account-action, Prefect Git-branch, Portainer proxy-path, authentik SCIM, Windmill sibling-route/null-owner, and OpenShift Console fetch/catalog follow-ups belong on their existing product pages. Use synthetic objects, owned repositories/peers, and denied signer/serializer/mutation/process/proxy/install sinks; never return records, mutate workflows or identity, invoke Docker/Git, reach internal services, or install charts.
- ZITADEL token-exchange, Keycloak first-broker-login stale link-proof, and Nezha Host-header redirect regression were promoted as a shared identity-provider authority page (Sept 16): prove token↔client provenance and scope containment separately on any token-exchange-enabled IdP, test which lifecycle events revoke cross-browser link proofs (link success AND manual unlink), and re-test config-gated fixes in their empty/new-default state because upgrade paths land in the regressed config. WP Import Export Lite delegated import/export capability wave folded into the July 28 WordPress page: enumerate callback-name, output-path, delete-path, fetched-URL, and query-fragment sinks on any admin-delegable import/export capability, and re-run the original SSRF/upload matrix on routes with a security history (incomplete-fix follow-ups).
- Trail of Bits Sept 15 1Password AI-patching-benchmark rebuttal is defender/patch-validation framing and was processed without publication under the no-defensive-guidance rule; keycloak role-revocation hygiene (ZITADEL multi-role delete skip) and emp3r0r pre-auth polling-session DoS likewise tracked, not published.
- CISA KEV CSV direct download that works from this host: https://www.cisa.gov/sites/default/files/csv/known_exploited_vulnerabilities.csv with a browser User-Agent (the /sites/default/files/2026-09/ path and the HTML page return 403 via Akamai). Catalog at 1710 entries, newest dateAdded 2026-09-14 as of the 2026-09-16 07:15Z scan.
- September 16 late wave (09:30Z) promoted as two pages: Pepperl+Fuchs ICE IODD web-UI cluster + Arista EOS gRPC management plane (feature-named viewer/upload routes skip core auth; per-gRPC-service authz engines drift from CLI/NETCONF policy — test each gRPC service, rule-collision pairs, and revoke-state separately), and GitLab protected-variable scope / Octopus feed traversal / Ash filter oracle / ZenHive dedup malleability (per-pipeline-type variable-scope matrix with presence-only projections; delegated CRUD that becomes later file writes; authorization nil-ing must cover every expression node type; dedup gates must key on canonicalized forms). Airflow Akeyless team-scope + FAB session-invalidation PUT-vs-PATCH follow-ups folded into the June 30 Airflow page; Appointment Hour Booking per-item capacity, Eventin payment-to-order replay, and Formidable Forms request-derived shortcode folded into the July 28 WordPress page. Kernel stable mirrors, Unbound/ReTrap DoS set, and appliance hygiene singles tracked, not published. KEV CSV unchanged (1710 entries, newest dateAdded 2026-09-14) as of the 2026-09-16 10:20Z scan.
- September 16 23:15Z scan: the 21:50-22:09Z djust completion wave (mount-path `__import__` of client-supplied dotted module path before auth/subclass check behind fail-open unset `LIVEVIEW_ALLOWED_MODULES` → unauthenticated arbitrary-module import + import-time code execution, CVE-2026-61599/GHSA-7prp-2623-8g45, plus siblings CVE-2026-61594/61592/61591/61589/61588/61596/61597) was folded into the existing Sept 16 djust page rather than a duplicate page. Pattern to reuse: dynamic-resolution sinks (`__import__`, class loaders, RPC method dispatch) where the gate runs after the import and the allowlist is `if allowed:` fail-open; post-import "not a subclass" rejects are not controls, and distinct error strings are an importable-name oracle. When a maintainer threat-model entry understates a default-open guard as "probing", re-derive impact. KEV JSON unchanged (2026.09.16, 1713 entries) and all blog feeds unchanged as of 23:15Z.
- September 17 06:30Z WPScan wave folded into the July 28 WordPress boundary page: reusable auth-boundary patterns are issuance provenance vs existence-match (Login with QR / Private Feed Key accept ANY stored user-meta value as the login code), session side effects on failed-authentication branches (Pressengine), authorization parameters read from the request itself (PuppyFW capability-from-body), validation endpoints that mint the credential they check with requester-controlled derivation (wpShopGermany), low-entropy/unkeyed-hash email tracking signatures recoverable offline (Newsletter), and the wp-admin public-namespace triad (is_admin() true + admin_init fired + PHP_AUTH_* populated are all unauthenticated by default; Paid Downloads admin-post.php upload). Chunked-upload validators that never inspect the reassembled file belong on the upload-boundary audits (Gravity Forms Multi Uploader). Canva Desktop deeplink double-decode, Qualcomm/aptx memory-safety block, Acer NitroSense, and GeoVision DLL hijack were tracked, not published. KEV unchanged (1713 entries, newest 2026-09-16); all blog feeds unchanged as of the 08:15Z scan.
- September 17 00:31Z wave promoted as two pages: Craft CMS cluster (HMAC signatures bound to no purpose or parameter name let low-privilege signed envelopes reach unsandboxed Twig SSTI RCE, one vendor fix commit added its own signing oracle; GraphQL relationship fields returning raw User elements below the user-data scope; DB-outage installer fail-open expanding `${VAR}` via `App::env()`; read-only view granting reorder write flag) and Nodemailer cluster (RFC 5322 comment atom-concatenation and missing UTS-46 normalization creating validator/sender domain differentials; legacy `resolveContent()` signature silently dropping `disableFileAccess`/`disableUrlAccess`). n8n Git-node relative-URL walk-up sandbox escape and client-supplied push-path cross-project deletion folded into the June 24 n8n page; AVideo CloneSite crontab command injection via loopback-blind CSRF, pass-the-hash login, and basename-collision CSRF exemptions folded into the Sept 5 AVideo page. Reusable patterns: audit every HMAC-signed surface for purpose/name binding and diff fix commits for new oracles; treat validate-then-delegate pipelines (mail addresses, URLs, git remotes) as parser-differential candidates; enumerate alternate public entry points that drop sandbox options. nanobot/joi/QRing/quay-builder items tracked only. KEV unchanged (1713, newest 2026-09-16).
- September 17 09:33Z wave folded into existing pages: standalone plugin PHP entry points that never bootstrap WordPress are fully unauthenticated surfaces (To Do List Member name-only upload endpoint with list/delete, CVE-2026-86801) and pre-bootstrap handlers bypass core request escaping (Yo username SQLi, CVE-2026-87963) — both belong on the July 28 WordPress page as the bootstrap-layer extension of the wp-admin public-namespace triad; directly reachable plugin-bundled PHP files (upload/handler/gateway/callback/ipn/api.php) are first-class recon targets. flatpak-builder `use-git-am` host `post-applypatch` hook execution (CVE-2026-86320) belongs on the June 12 developer-control page: any tool that shells out to Git over attacker content without `-c core.hooksPath=/dev/null`/`--no-verify` runs repository-internal hooks as host code. KEV CSV unchanged (sha256 dca46759a5bc74d5, 1714-line file/1713 entries, newest dateAdded 2026-09-16) and all blog feeds unchanged as of the 09:45Z Sep 17 scan.
- September 17 12:32Z wave: FatPipe MPVPN/WARP/IPVPN EOL firmware 10.1.2r60p100 published as a new appliance page (preauth root command injection via AuthFormServlet/xtremed + auth_user_pass stack overflow; management interface is default-disabled so reachability+version evidence is the finding on unpatchable EOL trains; same exposure-state framing as Ivanti Sentry/PeopleSoft UEM pages). Grav Clockwork-profiler + sandbox global-flag fail-open follow-ups folded into the Aug 25 Grav page (feature-named debug endpoints with path-substring routing are recon targets; per-source vs global sandbox-flag guards and reflection filters bypassing facade redaction). AVideo credential-issuance trio folded into the Sept 5 AVideo page (uniqid-derived pairing codes + unauthenticated microtime oracle, null==null parameter-less MFA verification bypass, anonymous userVerifyEmail enumeration + recovery-token minting). Snowflake CLI repo-config multi-statement SQL injection folded into the June 12 developer-control page. Dell OMA memory-safety block, Redis cluster-bus OOB, BIND/Corosync/kernel DoS set, and hygiene singles tracked, not published. KEV CSV unchanged (1713 entries, newest dateAdded 2026-09-16) and all blog feeds unchanged as of the 13:15Z Sep 17 scan.
- September 17 14:48–14:59Z wave promoted as one page (request-derived identity/guard composition + blind-SSRF oracle): reusable patterns are auth relays that validate the token against a caller-supplied issuer/app id (TinaCMS `clientID` — send your own freely-registered app and test whether the victim site accepts), injected visibility guards composed with the caller-chosen AND/OR operator (Vendure `filterOperator: OR`), `innerHTML`→`textContent` as a fake sanitizer that still fires `onerror` on detached nodes, ORM injection in the dictionary/EAV filter **key** position (Marten), and blind-SSRF outer-status 500/200 target-liveness oracles (Nuxt OG Image). Folds: Sanic post-terminal-chunk trailer smuggling + oRPC reflected request `Vary` onto the Sept 15 parser page; LiteLLM top-level-key blocklist bypassed by one nesting level (`user_config.api_base`) onto the June 8 page; Redocly `split` document-name-to-path traversal (sweep `../` in component names/`lang` values) + cyclonedx-npm Windows-only shell-fallback `--workspace` injection (test metacharacter canaries on both platforms) onto the June 12 developer-control page. DoS/crash-only siblings tracked, not published.
- September 17 16:29–17:19Z waves promoted as one page (policy-guard bypass axes): reusable patterns are stdlib traversal that never enters the sandbox guard (`string.Formatter.get_field`/`str.format_map` real `getattr`/`getitem` — test both hook-shadowing and guard-avoiding functions on any Python sandbox; blocked-on-`str` guards miss `str` subclasses), entry-point authorization that does not re-run on reference expansion (Umbraco picker/MNTP direct-401-vs-expanded-200 decision table on any headless content API), client-side safety flags a hostile server ignores (MariaDB `allowLocalInfile=false` vs `0xfb`; AsyncHttpClient creds-on-plaintext-CONNECT when you control the proxy leg), separate unauthenticated management ports next to an authed API (Kestra :8081 `/env` — sweep port+1 + `/env`/`/loggers`/`/heapdump` on Micronaut/Spring products), pre-auth template-engine `http()` functions in YAML-import workflow platforms, SCP/remote-shell path arguments as command injection regardless of client quoting, and lexical-vs-kernel symlink/`..` splits (oras-go, Junrar intermediate mkdirs, Grav watermark lexical collapse). Folds: Grav Flex `dynamicDataField` registered-handler bypass of the patched `dynamicData()` validator onto the Aug 25 Grav page (enumerate alternate dispatchers reaching a patched sink), Skipper vendor `truncated_body` mitigation fail-open on oversized chunked/H2 (re-derive how truncation flags are computed per transport) onto the July 17 Skipper page, RabbitMQ amqp091-go `readLongstr` silent-success cursor desync + unescaped TLS-path URI re-parse onto the Sept 15 parser page. DoS-only siblings (ExifReader iloc, AsyncHttpClient decompression, vLLM GPU decode) tracked, not published. KEV CSV unchanged (1713 entries, newest dateAdded 2026-09-16) and all blog feeds unchanged as of the 17:20Z Sep 17 scan.
- September 17 20:23-20:59Z Grav-centric wave was folded (no new page) onto four existing pages: 10 Grav GHSAs onto the Aug 25 Grav page (four Twig-sandbox read primitives — sandbox policy governs only object members, so raw config arrays `system`/`site`/`theme`, unfiltered `offsetGet`, allow-listed `media_directory()` raw-path leg, and the `config_denied_paths` missing-`system` denylist all escape it; dual-form `[class,method]` callable bypass of `isSafeDynamicCall()` closes the dynamic-data ladder to login-to-RCE with `Installer::unZip` to docroot + config-file-as-own-blueprint; bare-function denylist omission of `error_log`; `preg_match /u` returns false on invalid UTF-8 subject and truthiness checks collapse false==clean silencing every detectXss rule; GHSA-269c quote-aware fix itself introduced the unpaired-quote-in-unquoted-attribute tokenizer gap), CoreDNS UPDATE-relay transport-parity item onto the April CoreDNS TSIG page (build transport × policy decision tables; UDP/TCP reject pre-dispatch, DoH/DoH3/DoQ/gRPC relay), @platejs/core live-document HTML deserialization onto the Sept 16 sanitizer page (parse-time behavior fires before the sanitizer's node model exists — test DOMParser/template inertness), and the djust 1.1.1/1.1.2 template-layer fixes onto the Sept 16 djust page (dual-engine differential escaping harness; safe grants keyed by name/view leak across with/for rebinding and WS renders). Tracked only: CakePHP FunctionsBuilder argument-position SQLi, detail-free Chamilo CStudio 9.8 unauth RCE (revisit when technical detail lands), Jupyter Referer 5xx-log token leak, OTel-Go env-TLS-pin ignore, DoS-only set. KEV CSV unchanged (sha256 dca46759a5bc74d5, 1713 entries, newest dateAdded 2026-09-16); PortSwigger hash change this scan was encoding churn only — newest entry What's in a tag name (Aug 25) already processed; ProjectDiscovery/ToB/Brutecat/disclosed.org unchanged as of the 21:0xZ Sep 17 scan. Fold-only waves do not touch docs/index.md (precedent 250974f); alerts/index.md carries the highlight line.
- September 18 03:30Z unreviewed wave promoted as one page (Nextcloud app-layer authority drift): reusable axes are app-level fetch paths that skip the platform's central SSRF helper with explicit local-address allowances (Circles keyId fetched pre-trust from unauthenticated POST endpoints — enumerate outbound calls and check each actually calls the guard), alternate protocol surfaces re-resolving identity from the request URI instead of the session (WebDAV locks from absolute path, cross-user + lock-token disclosure; run the REST-vs-DAV-vs-OCS verdict table as a second low-priv user), config/settings sub-resources skipping the ownership check their parent resource enforces (Deck board-config), shared artifacts evaluated with the viewer's rather than owner's configuration (Photos smart albums = targeted enumeration oracle), and security controls gated on a request parameter's presence (Approval etag check — always test parameter ABSENT, not just forged; twin of the null==null AVideo MFA bypass). yt-dlp-web-ui params→argv injection folded onto the Aug 5 command-wrapper page: any user-reachable "extra arguments" field on a CLI front-end is argv injection until a flag allowlist (not denylist) is proven; sweep `-o`/`--config-locations`/`-P` filesystem flags. Tracked only: HGiga OAKlouds detail-free pair, django-page-cms pair, NASA CryptoLib SPI-selected-SA-without-GVCID-binding (revisit if space-link scope), Acronis LPE trio, MS/Azure Patch-Tuesday sync wave, parser/DoS singles. KEV CSV unchanged (sha256 dca46759a5bc74d5, 1713 entries, newest dateAdded 2026-09-16) and all blog feeds (PortSwigger, ToB, ProjectDiscovery, Brutecat, disclosed.org) unchanged as of the 05:30Z Sep 18 scan.
- September 18 06:31Z wave promoted as one page (WordPress core 7.1.1) + fold: reusable axes are core-level alternate-transport authorization parity (XML-RPC publishing customize_changeset bypassing edit_css — enumerate every transport writing one object type and diff gates; machinery post types are historically transport-local), text-rewriting HTML filters as XSS boundaries (wpautop() unauthenticated stored XSS reproducible on DEFAULT installs — moderation off by default + previously-approved requirement bypassable — validate submitted/stored/rendered/DOM bytes separately with inert structural canaries, version-fingerprint first), writer/parser re-tokenization differentials (set_modifiable_text abrupt-closing sequences; sibling of Grav detectXss/Plate parse-before-sanitize), URL-triggered state-changing side effects (crafted-URL theme install/preview = inactive-theme surface activation), and core-level low-role IDOR sweep (contributor+ overwrite/slug disclosure, sub-resource parent-check leak, comment reparenting — run the plugin IDOR sweep against core routes too). Plugin fold onto the July 28 WP page: export-disclosed installation-wide secret -> import -> admin (Ai1wm CVE-2026-81810, check export/import capability separation), hardcoded-salt+timestamp tokens (Easy Appointments, entropy audit like Fluent Forms), document-renderer remote fetch as SSRF whose output artifact is the response channel (CF7 Generate PDF — audit PDF/DOCX/email renderers), unauthenticated attachment surfaces as admin-viewer sinks (VikBooking chat), nonce-verified-only-when-present (Filter Gallery, parameter-ABSENT axis). Tracked not published: ManageEngine DataSecurity Plus agent auth bypass + technician Reports SQLi, ShortPixel POI without local POP chain, HGiga OAKlouds pair still detail-free (revisit when technical detail lands), django-page-cms admin-helper pair, NASA CryptoLib TC auth downgrade, Poppler/GPAC/O-RAN/Biggop singles. KEV CSV/JSON unchanged (catalogVersion 2026.09.16, 1713 entries, newest dateAdded 2026-09-16; note: cisa.gov CSV now Akamai-blocked for plain curl UAs — the .json feed works with a browser UA) and all blog feeds unchanged (PortSwigger Aug 25, ToB Sept 15, PD Aug 16, Brutecat Jun 11) as of the 07:15Z Sep 18 scan.
