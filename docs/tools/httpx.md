# httpx

The canonical `httpx` documentation now lives at [HTTP Probing with httpx](../skills/httpx.md).

Keep this compatibility path stable for older links. Use the skill page for inputs, commands, output handling, and safety notes.
