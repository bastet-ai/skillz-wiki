# Scope parameters that are accepted but never enforced: kcp identity-header smuggling, Convoy list-vs-get IDOR, Perses query-param tenancy, Moquette Will-path ACL gap, and Obot OAuth audience confusion

Source: hourly offensive-security scan of GitHub Security Advisories, 2026-09-18 (waves published 2026-09-18T17:15Z–17:59Z).

One theme ties the durable records: **the authorization layer and the data layer disagree about what scopes the request.** A URL path parameter that gates the permission check is dropped before the query runs; a query parameter silently overrides the path scope; a protocol side-channel (MQTT Will, proxy identity header) reaches a sink the main path guards; an HTTP method collapses onto the wrong action; a signed digest field is never re-verified. Each is a separate, immediately testable audit axis.

## Advisory table

| Product | GHSA | CVE | CVSS | Boundary |
| --- | --- | --- | --- | --- |
| kcp front-proxy | [GHSA-c8w2-fgvx-vhv4](https://github.com/advisories/GHSA-c8w2-fgvx-vhv4) | [CVE-2026-61682](https://nvd.nist.gov/vuln/detail/CVE-2026-61682) | **9.9** | Front-proxy **appends** `X-Remote-User`/`X-Remote-Group`/`X-Remote-Extra-*` instead of **replacing** them, and never strips client-supplied copies. The shard trusts these headers as verified identity over the mTLS front-proxy leg → any ordinary credential asserts `X-Remote-Group: system:masters` and owns every workspace on the shard. Complete multi-tenant isolation break |
| Convoy | [GHSA-p5vg-v7mj-f6q4](https://github.com/advisories/GHSA-p5vg-v7mj-f6q4) | [CVE-2026-81505](https://nvd.nist.gov/vuln/detail/CVE-2026-81505) | high (no patch ≤ v26.6.2) | `GET /api/v1/projects/{projectID}/sources/{sourceID}`: authorization resolves `{projectID}`, then `FindSourceByID(ctx, projectID, id)` **accepts `projectID` and never uses it** — the SQL has no `project_id` predicate at all. Any caller authorized on *any one* project reads *any* project's Source by ID, including **plaintext AMQP/Kafka/SQS broker credentials** in `pub_sub.*.auth.password`. The list endpoint scopes correctly; only single-item get drifts |
| Perses (filesystem DB) | [GHSA-vr5f-w35q-98jp](https://github.com/advisories/GHSA-vr5f-w35q-98jp) | [CVE-2026-63445](https://nvd.nist.gov/vuln/detail/CVE-2026-63445) | high | List endpoints bind the `project` **query parameter** into the resource `Query` and use it to build filesystem paths; path validation (`Flatten`) runs only for Create/Update **bodies**, never list queries → `?project=../projects` reads arbitrary YAML/JSON from the host |
| Perses (project scoping) | [GHSA-cjgj-2fwf-4c2w](https://github.com/advisories/GHSA-cjgj-2fwf-4c2w) | [CVE-2026-63458](https://nvd.nist.gov/vuln/detail/CVE-2026-63458) | high | `GET /api/v1/projects/team-a/dashboards?project=finance-secret` — caller is authorized on the **path** project, then the **query** param re-points the data lookup at another tenant → viewer on one project lists every project's dashboards/datasources/variables. Defeats all project-level isolation on project-scoped reads |
| Perses (datasource proxy) | [GHSA-4227-9989-jrhx](https://github.com/advisories/GHSA-4227-9989-jrhx) | [CVE-2026-63199](https://nvd.nist.gov/vuln/detail/CVE-2026-63199) | high | Proxy authorizes the caller on the **Datasource** scope, then resolves and **decrypts any Secret named in the request body** with no Secret-scope check. Hold only `GlobalDatasource:create` → attach a Secret you cannot read to a datasource you control → the proxy returns it in plaintext. Classic confused deputy: the server's credential access is steered by the caller's body |
| Moquette MQTT broker | [GHSA-9jjc-fw8x-fmwx](https://github.com/advisories/GHSA-9jjc-fw8x-fmwx) | [CVE-2026-85058](https://nvd.nist.gov/vuln/detail/CVE-2026-85058) | 7.5 | All three QoS PUBLISH handlers call `authorizator.canWrite()`, but the **Will-message path** (`fireWill()` → `publishWill()` → `publish2Subscribers()`) skips it entirely. Set an ACL-restricted topic as your Will Topic in CONNECT (anonymous is the default), drop the TCP connection with RST, and the broker publishes your payload to the protected topic **on its own behalf** |
| Obot (MCP platform) | [GHSA-xwmw-prc4-v3cr](https://github.com/advisories/GHSA-xwmw-prc4-v3cr) | — | 8.8 | OAuth **dynamic client registration unauthenticated + unrestricted redirect URIs**, authorization **auto-completes with no consent screen** for a logged-in victim, and the minted token carries the victim's **full group set** — the field that authorizes API paths — while the server validates issuer **but not audience**. One victim click → bearer token valid across the victim's whole API, not just the requested MCP server |
| Obot (MCP platform) | [GHSA-jgh3-fggc-mcpm](https://github.com/advisories/GHSA-jgh3-fggc-mcpm) | — | 7.6 | Remote MCP server URL validated only as parseable http/https; the controller **auto-fetches OAuth discovery metadata during reconcile** (no user interaction beyond registration) with no loopback/link-local/RFC1918/169.254.169.254 guard on the default config, and **reflects the response body in error messages → non-blind SSRF** straight to cloud IAM creds. The one egress flag is off-by-default, loopback-only, and absent from the metadata path |
| Obot (MCP platform) | [GHSA-pr6h-vr44-xq8j](https://github.com/advisories/GHSA-pr6h-vr44-xq8j) | — | medium | Registry auth authorizer **rejects a fixed set of known-protected prefixes and default-allows everything else**; the registry mounted at a top-level prefix outside the set stays anonymous-readable. Fix for a prior CVE protected one prefix, not the new one — denylist-of-protected-routes drift |
| AnyCable Pusher REST API | [GHSA-5p54-whvp-x327](https://github.com/advisories/GHSA-5p54-whvp-x327) | [CVE-2026-63405](https://nvd.nist.gov/vuln/detail/CVE-2026-63405) | medium | HMAC `stringToSign` **includes the `body_md5` query param verbatim but the server never computes `md5(actual body)`** to compare, and there is no `auth_timestamp` staleness check. Capture one signed request (LB logs) → replay the same query string with an entirely different body, forever |
| zot OCI registry | [GHSA-qg67-7m6v-qg25](https://github.com/advisories/GHSA-qg67-7m6v-qg25) | [CVE-2026-61833](https://nvd.nist.gov/vuln/detail/CVE-2026-61833) | 8.1 | Bearer handler maps every non-GET/HEAD method to the `"push"` action (`action = "pull"; if method != GET && != HEAD { "push" }`) — DELETE is never `"delete"` — **and** the fine-grained `DistSpecAuthzHandler` middleware is bypassed entirely for bearer-authenticated requests. Push-scope-only token deletes arbitrary manifests/blobs, violating the Docker Distribution token spec |

Folds on this run:

- **process-compose MCP SSE DNS rebinding (CVE-2026-77339/GHSA-5gm3-9crp-6g3v)** → folded onto the [Sept 15 MCP DNS-rebinding page](2026-09-15-mcp-dns-rebinding-alternate-auth-path-and-sandbox-boundaries-ghsa.md): SSE is the **default** transport when `transport` is omitted, the `X-PC-Token-Key` middleware is installed on the Gin REST router only — the separately-started MCP listener **does not inherit it** — and the registered control tools start/stop/restart/scale processes and truncate logs. New axis for the rebinding matrix: when a tool has *two* listeners (API + MCP), test auth per-listener, not per-product.
- **md-editor-v3 XSSPlugin bypass (CVE-2026-84992/GHSA-3rm2-h79c-8qw6)** → folded onto the [Sept 16 sanitizer-gaps page](2026-09-16-rmcp-oauth-resource-spoofing-mdc-sanitizer-gaps-and-vllm-route-guard-parity-ghsa.md): the `highlight` callback interpolates the fenced-code **language/info string** into `class="language-${language}"` and an **unquoted** `language=${language}` attribute unescaped; the shipped `XSSPlugin` filters only `html_block`/`html_inline` **parser tokens**, so renderer-generated HTML is invisible to it. Payload ```` ```x"><details/open/ontoggle=alert(1)> ```` executes with the "XSS plugin" enabled. Rule: a sanitizer that inspects parsed tokens does not cover markup the **renderer** constructs — fuzz the info-string/attribute-construction path separately from the raw-HTML path.
- **ToolHive `host.docker.internal` lateral movement (CVE-2026-58197/GHSA-qg2g-g9w3-m5h8, 8.8)** — default `network` permission profile is `insecure_allow_all: true`, so a containerized MCP server reaches the ToolHive control-plane MCP endpoint and every other managed proxy on host localhost, which are themselves unauthenticated. Added to the MCP cluster note below: test the **container-to-host-local hop** on any MCP runner (ToolHive, similar supervisors) before theorizing an escape.

Tracked without publication: Mnemosyne sync server decodes JWT header/payload without verifying the HMAC signature — any well-formed token, including `alg:none` and empty signature, authenticates as any `user_id` (CVE-2026-59163/GHSA-xcw4-53cc-hv32, 9.1; decode-instead-of-verify is already covered by the existing JWT-decision-table guidance — sweep signature verification explicitly whenever a custom auth wrapper parses JWTs by hand); Paymenter downgrade double-spend race — unlocked `Service::upgradable` eligibility read + no `DB::transaction`/`lockForUpdate` lets N concurrent downgrades each refund (CVE-2026-71537/GHSA-5gmm-hjfj-8ff7; the payment-flow TOCTOU axis is worth a scripted concurrent-replay pass on any billing target you hold scope on); File Viewer legacy `.doc` renderer emits hyperlink targets with HTML escaping but **no URL-scheme allowlist** → `javascript:`/`vbscript:`/`data:` links live in the embedding origin (CVE-2026-91127/GHSA-3753-m2x2-q623 — same escaped-but-not-scheme-allowlisted link class as the Sept 16 MDC scheme-miss item, folded into the Sept 16 sanitizer page this run); AnyCable telemetry hardcoded `authToken="secret"` + raw `os.Args` (incl. `--secret=`) shipped to a third-party endpoint (CVE-2026-63406/GHSA-w72w-9qmj-c9qm — operator hygiene, no target workflow); AnyIO `extra_groups` variable mix-up + IDNA 2003 TLS-hostname divergence; adm-zip declared-size DoS; Bouncy Castle lazy-ASN.1 nesting-guard reset; kcp/zot siblings. Note: the kcp advisory's CVE identifier CVE-2026-61682 **collides with the CVE assigned to the Sept 18 Semantic MediaWiki stored XSS** — a GitHub-advisory-side duplicate assignment; when citing this CVE, disambiguate by product.

## Why this is worth an operator page

- **Identity headers over an "internal" mTLS leg are only as trusted as the proxy's strip behavior.** kcp's shard trusts `X-Remote-*` because it only accepts the front-proxy connection — but the front-proxy appended instead of replaced. Sweep every reverse-proxy/aggregation hop on a target: send your own `X-Remote-User`, `X-Remote-Group`, `X-Forwarded-User`, `X-Auth-Subject` style headers and diff the downstream verdict. This is the header twin of Sept 18's Nextcloud request-URI-identity drift and the classic `X-Forwarded-For` identity steering.
- **Compare list vs single-item authorization, always.** Convoy's list endpoint scoped by project; the get-by-ID endpoint's service function took the project ID and dropped it before SQL. For every collection you can list, fetch one foreign-tenant item by ID through the same route pattern and diff. A parameter present in the handler signature but absent from the query is grep-able in any open-source target (`FindSourceByID(ctx, project.UID, sourceID)` → `WHERE s.id = $1`).
- **Query parameters that shadow path scope are tenant-isolation holes.** Perses authorized the `{projectID}` in the URL and then let `?project=` re-point the lookup. For every path-scoped collection route, append the common scope words as query params (`project`, `namespace`, `tenant`, `org`, `group`, `scope`) and diff result sets against the path value.
- **Validation asymmetry between write and read paths leaks paths and rows.** Perses validated the project string on Create/Update bodies but not list queries — traversal landed in the read path. When you find a validated input on POST, immediately test the *same field* on GET/list/search endpoints.
- **Confused-deputy decrypt: authorize the wrapper, then name the secret.** If any endpoint that authorizes scope A will resolve/decrypt/attach an object from scope B named in your body, you own a secret oracle. Enumerate endpoints where a body field names a credential/secret/config object and check the response path returns the plaintext (datasource "connectivity check" features are favorite hosts for this).
- **Protocol side-channels re-implement publish.** The broker checks ACLs on client PUBLISH but not on the Will it publishes *itself* after an abrupt disconnect. For any broker (MQTT/Kafka hooks/webhooks/AMQP), list every way a message reaches subscribers (direct publish, Will/LWT, retained messages, delayed/queued redelivery, system events) and diff which paths hit the authorization call.
- **Signed-digest fields that the server never recomputes are signature bypasses with extra steps.** AnyCable's `body_md5` was in the HMAC string but never compared against the body. When an API signature scheme includes a body digest, hash a different body yourself and replay; also test `auth_timestamp` staleness independently.
- **Method-to-action mapping collapse.** zot turned DELETE into "push" and skipped the fine-grained authorizer for bearer tokens. When a token has narrow scopes, replay it against every HTTP method on the same route — especially destructive verbs — and check whether a *different* authorization middleware applies per auth mechanism (bearer vs basic).
- **OAuth audience confusion via dynamic client registration.** Unauthenticated DCR + unrestricted redirect + no consent + a token carrying the resource-server's *authorization groups* + issuer-checked-but-audience-ignored = one-click token theft. On any OAuth-capable platform with DCR enabled: register a client with your own redirect, craft the authorize URL, check whether a consent screen actually renders, and (with your own second lab account) inspect which claims/groups the minted token carries vs its `aud`, then test it against the general API, not just the MCP endpoint.
- **Reflected-error SSRF beats blind SSRF.** Obot's metadata-sync SSRF returns the fetched body inside error messages and fires automatically on reconcile. When registering attacker-controlled URLs into any platform (MCP servers, webhooks, OAuth issuers, remote configs), test whether platform-generated error text echoes upstream response bytes.
- **Default-allow authorizers are denylists of protected routes.** Obot's second advisory: the authorizer enumerated known-protected prefixes and allowed everything else; every new top-level mount is anonymous until someone remembers to add it. Enumerate top-level route prefixes on authed products and re-check 401-vs-200 on each — especially mounts added recently.

## Validation workflow (authorized scope only)

!!! warning "Disposable tenants and synthetic objects only"
    Build two disposable projects/workspaces/tenants with a canary credential object in the victim tenant and prove cross-tenant read with a *synthetic* broker password or canary secret. For header smuggling, forge identity headers only against lab front-proxies you own and record authorization verdicts — never assert `system:masters` against shared infrastructure. For OAuth proofs use two of your own lab accounts and an owned redirect domain; revoke tokens immediately. For MQTT, run a lab broker with fake ACL topics; never Will-publish into production topics.

1. **List-vs-get IDOR sweep.** For every collection route: enumerate as user A in tenant A, then fetch one foreign item ID via the single-item route from tenant B; diff status + body. Grep open-source targets for service functions that take a scope/tenant parameter and never reference it in the query.
2. **Query-shadow probe.** For every path-scoped collection, append `?project=`, `?namespace=`, `?tenant=`, `?org=` pointing at a foreign scope; compare payloads against the path-scoped positive control.
3. **Read-path validation probe.** Take any input the API rejects as malformed/invalid on POST and send it raw on GET/list/search; watch for traversal artifacts (`../` list results, path echoes, 500s with path fragments).
4. **Broker side-channel matrix (MQTT labs).** Per ACL policy: direct PUBLISH verdict, Will-message verdict (CONNECT with restricted Will topic + TCP RST), retained-message verdict, subscription verdict. Moquette proved the Will leg is the one implementers forget.
5. **Method/scope replay.** With a narrowly scoped bearer token, replay DELETE/PUT/PATCH against routes where the token scope should deny, and compare bearer vs basic-auth verdicts on the same route.
6. **Digest-replay test.** Capture any signed request with a body-digest parameter; resend the same query string with a mutated body; also resend with an old `auth_timestamp`.
7. **Front-proxy header strip check.** Send forged `X-Remote-*`/identity headers through each proxy hop chain in scope and diff downstream identity-derived decisions (lab targets only).
8. **DCR/audience audit (owned accounts).** Register a DCR client, walk the authorize flow with a second lab account, record consent-screen presence, decode the minted token's groups/aud, and test the token against the general API surface.

## Reporting checklist

- State the exact scope parameter that was ignored (path vs query vs service-layer argument) and show the positive control (same request, in-scope object) beside the cross-tenant result.
- For credential leakage, quote only the canary value and name the JSON field; never paste live broker passwords.
- For header smuggling, include the proxy hop diagram and the raw request/response verdict table.
- For races (Paymenter-style), report concurrency count, observed N× credit delta, and whether idempotency keys exist.
- Version/commit pin each verdict (Convoy has **no patch** as of v26.6.2 — mark as disclosure-pending).

Related pages: the [Sept 18 Nextcloud app-layer authority drift](2026-09-18-nextcloud-app-layer-authority-drift-circles-keyid-ssrf-webdav-locks-and-deck-config-ghsa.md) (request-derived identity + sibling-route authz drift), the [Sept 15 MCP DNS-rebinding cluster](2026-09-15-mcp-dns-rebinding-alternate-auth-path-and-sandbox-boundaries-ghsa.md) (process-compose/ToolHive fold target), the [Aug 6 agent tool-policy boundary page](../alerts/2026-08-06-agent-tool-policy-file-fetch-boundaries-ghsa.md) (MCP platform trust boundaries), and the [Sept 16 rmcp OAuth metadata page](2026-09-16-rmcp-oauth-resource-spoofing-mdc-sanitizer-gaps-and-vllm-route-guard-parity-ghsa.md) (OAuth binding misses; md-editor-v3 fold target).
